---
phase: 03-mdns-responder
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/spike_mdns_candidates.py
  - scripts/mdns_spike_tests/test_candidates.py
  - scripts/mdns_spike_inputs/zeroconf_recovery.py
  - scripts/mdns_spike_tests/test_recovery.py
  - .planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json
autonomous: false
requirements:
  - MDNS-10
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "No closeout work starts until the user grants one fresh, case-bounded active-work allowance; exhausted earlier budgets are never treated as available time (D-14, D-15)."
    - "The evaluated zeroconf revision has candidate-specific evidence for the remaining malformed/flood/resource, Windows-simulation, configuration/interface and supported-operation failure cases, without inheriting direct-responder results (D-01–D-10)."
    - "Silent listener loss remains an accepted limitation: running reports lifecycle state, not independently measured network health (D-08)."
    - "The MDNS-10 decision is go only when every required zeroconf gate is demonstrated for the exact evaluated version and retained inputs; otherwise it is evidence-valid no-go or provisional."
    - "MDNS-01–09 and MDNS-11 remain pending until a human records go; the closeout does not implement a responder or complete Phase 3 (D-12)."
  artifacts:
    - path: "scripts/spike_mdns_candidates.py"
      provides: "Case-bounded zeroconf closeout runner, immutable-evidence reconciliation and fail-closed validator"
      contains: "run-zeroconf-closeout"
    - path: "scripts/mdns_spike_inputs/zeroconf_recovery.py"
      provides: "Spike-only handling of errors surfaced by supported zeroconf lifecycle operations"
      contains: "RecoveryPrototype"
    - path: "scripts/mdns_spike_tests/test_candidates.py"
      provides: "Behavioural tests for candidate robustness, platform labelling, configuration fit and evidence integrity"
    - path: "scripts/mdns_spike_tests/test_recovery.py"
      provides: "Behavioural tests for supported-operation failures, cleanup ownership, retained errors and explicit retry"
    - path: ".planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json"
      provides: "Hash-linked current evidence, budget accounting, candidate/version identity and MDNS-10 decision support"
  key_links:
    - from: "scripts/spike_mdns_candidates.py"
      to: ".planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json"
      via: "run-zeroconf-closeout writes and validate-zeroconf-closeout verifies a versioned ledger"
      pattern: "(run|validate)-zeroconf-closeout"
    - from: ".planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json"
      to: ".planning/phases/03-mdns-responder/03-continuation-evidence/manifest.json"
      via: "SHA-256 references preserve retained zeroconf evidence without rewriting it"
      pattern: "continuation-evidence/manifest.json"
    - from: ".planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json"
      to: ".planning/phases/03-mdns-responder/03-01-EVIDENCE.json"
      via: "candidate-labelled reconciliation keeps old direct-responder results historical"
      pattern: "03-01-EVIDENCE.json"
    - from: "scripts/mdns_spike_inputs/zeroconf_recovery.py"
      to: "scripts/mdns_spike_tests/test_recovery.py"
      via: "public-operation boundary faults exercise lifecycle state and fleet policy"
      pattern: "(register|update|unregister|interfaces|close)"
---

<objective>
Close only the remaining evidence cells for the zeroconf implementation-selection spike, then obtain the blocking MDNS-10 go/no-go decision under the amended D-08 contract.

Purpose: Convert the retained platform, packaging, discovery and recovery observations into a case-complete, candidate-labelled decision record without reopening silent listener-health work or beginning responder implementation.
Output: A validated `03-ZEROCONF-CLOSEOUT.json` and one human MDNS-10 route; only go permits a later plan for MDNS-01–09 and MDNS-11.

Decision coverage for this closeout:
- D-01: Treat existing factory ownership and server enablement as candidate-fit constraints; production wiring remains pending after go.
- D-02: Exercise candidate support for opt-in server operation, default device advertisement and concrete-address fallback without adding those APIs now.
- D-03: Prove invalid-address, wrong-family and Thread-opt-out cases can be rejected before registration in the spike seam.
- D-04: Keep advertisement settings immutable within each candidate case; mutation is represented as remove/recreate evidence.
- D-05: Preserve the read-only disabled/stopped/running/failed plus error model in surfaced-operation evidence.
- D-06: Permit explicit retry only after owned cleanup and keep WiFi LIFX work uninterrupted.
- D-07: Exercise failed-mDNS Thread admission before any simulated membership change.
- D-08: Handle errors surfaced by supported operations, accept silent listener loss, and define running as lifecycle state only.
- D-09: Reconcile candidates in the locked order without restarting generic fallback exploration.
- D-10: Prefer zeroconf only when the public-API adapter and all required evidence pass; private mechanisms remain excluded.
- D-11: Retain direct-extension/adaptation findings as historical fallback evidence, never as zeroconf proof.
- D-12: Keep MDNS-01–09/11 pending until a validator-supported human go decision.
- D-13: Reuse 1/10/100 complete-fleet results only after verifying the zeroconf manifest identity.
- D-14: Require a fresh 7,200-second active-work decision because every earlier allowance is exhausted; record CI queue time separately.
- D-15: Permit no automatic extra time and require concrete evidence plus exact acquisition steps for any provisional result.
- D-16: Exclude the optional separate-machine experiment from this closeout.
- D-17: Do not define or provision a VM.
- D-18: Do not claim cross-machine discovery/control evidence or use it as a substitute for the required local/CI gates.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-mdns-responder/03-CONTEXT.md
@.planning/phases/03-mdns-responder/03-SPEC.md
@.planning/phases/03-mdns-responder/03-RESEARCH.md
@.planning/phases/03-mdns-responder/03-PATTERNS.md
@.planning/phases/03-mdns-responder/COVERAGE.md

# Historical inputs, not execution dependencies
@.planning/phases/03-mdns-responder/03-01-SUMMARY.md
@.planning/phases/03-mdns-responder/03-01-EVIDENCE.json
@.planning/phases/03-mdns-responder/03-CONTINUATION.md
@.planning/phases/03-mdns-responder/03-RECOVERY-PROTOTYPE.md
@.planning/phases/03-mdns-responder/03-LISTENER-HEALTH-INVESTIGATION.md
@.planning/phases/03-mdns-responder/03-continuation-evidence/manifest.json
@.planning/phases/03-mdns-responder/03-recovery-evidence/local-macos.json
@.planning/phases/03-mdns-responder/03-recovery-evidence/listener-health-local-macos.json

# Existing spike implementation and CI path
@scripts/spike_mdns_candidates.py
@scripts/mdns_spike_inputs/zeroconf_recovery.py
@scripts/mdns_spike_tests/test_candidates.py
@scripts/mdns_spike_tests/test_recovery.py
@.github/workflows/ci.yml
</context>

<historical_dependency_note>
Plan 03-01 is intentionally absent from `depends_on`: its halted summary and immutable evidence are source material, while this plan is the runnable continuation. Do not resume or rewrite 03-01, its summary, or its retained evidence. Reconcile their candidate-labelled findings into the new ledger by path and SHA-256 only.
</historical_dependency_note>

<planning_shape>
The budget checkpoint precedes the tracer because the initial four-hour allowance and later 60-minute allowance are exhausted. It grants execution authority only; the first executable work remains one end-to-end tracer from behavioural cases through exact-head evidence and validation.
</planning_shape>

<interfaces>
`scripts/spike_mdns_candidates.py` must expose `run-zeroconf-closeout` and `validate-zeroconf-closeout`. The runner accepts `--evidence`, `--candidate-version`, `--source-revision` and `--active-budget-seconds`; the validator is stdlib-only apart from the project environment and returns zero for an internally consistent go/no-go/provisional ledger, while rejecting a decision stronger than its evidence.

`03-ZEROCONF-CLOSEOUT.json` records schema version, active budget/elapsed time, CI queue time separately, evaluated and registry-current versions with official provenance, immutable input paths/digests, per-case status/evidence/acquisition step, exact-head platform receipts, old direct-candidate reconciliation, and a decision-support object. Each case status is `demonstrated`, `failed`, `simulated` or `untested`; simulation and historical results cannot satisfy real zeroconf gates.

`RecoveryPrototype` remains spike-only. Extend its public-operation boundary to cover exceptions raised by supported `async_register_service`, returned announcement awaitables, `async_update_service`, `async_unregister_service`, `async_update_interfaces` and `async_close` calls. It retains the first operation error plus cleanup context, closes owned resources when possible, blocks a fresh owner when cleanup is incomplete, and permits only explicit retry. Per D-08, no callback, private listener access, health poll or watchdog is added.
</interfaces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Authorise one case-bounded closeout allowance</name>
  <reversibility rating="reversible">This controls only how much additional spike work may run; holding leaves all retained evidence unchanged.</reversibility>
  <read_first>
    - .planning/phases/03-mdns-responder/03-CONTEXT.md
    - .planning/phases/03-mdns-responder/03-CONTINUATION.md
    - .planning/phases/03-mdns-responder/03-RECOVERY-PROTOTYPE.md
    - .planning/phases/03-mdns-responder/03-LISTENER-HEALTH-INVESTIGATION.md
    - .planning/phases/03-mdns-responder/03-01-SUMMARY.md
  </read_first>
  <action>Record a fresh active-work decision because D-14's initial allowance and the later targeted allowance are exhausted. Recommend one 120-active-minute cap covering only Task 2's named malformed/flood/resource, identified Windows simulation, configuration/interface fit, supported-operation failure, exact-head CI receipt and evidence-validation cases; CI queue time is recorded separately. Existing approval for the harness and public zeroconf API scope remains in force and is not re-asked. This checkpoint grants no responder implementation, dependency-manifest change, fallback prototype, VM, cross-machine work or automatic extension. If the allowance is held, retain MDNS-10 as provisional and stop.</action>
  <acceptance_criteria>
    - The chosen route is recorded before any new probe or harness edit begins.
    - Approval names 7,200 active seconds and the exact Task 2 case families; it does not revive either exhausted allowance or grant more time by implication.
    - Hold produces no spike or evidence mutation and leaves the existing provisional status accurate.
  </acceptance_criteria>
  <verify><automated>uv run --frozen python -c "from pathlib import Path; c=Path('.planning/phases/03-mdns-responder/03-CONTEXT.md').read_text(); s=Path('.planning/phases/03-mdns-responder/03-01-SUMMARY.md').read_text(); assert 'D-08 (amended 2026-09-23)' in c; assert 'status: halted' in s"</automated></verify>
  <fails_when>The automated command cannot establish the amended contract and historical halt, or work begins without an explicit fresh allowance. Approval also fails if it expands beyond the enumerated cases or treats CI queue time as reusable active time.</fails_when>
  <decision>Grant one fresh, bounded MDNS-10 closeout allowance.</decision>
  <options>
    <option id="approve-120"><name>Approve 120 active minutes</name><pros>Covers the finite remaining cases and exact-head evidence acquisition in one bounded run.</pros><cons>Consumes up to two further hours of active work; an incomplete result still remains provisional.</cons></option>
    <option id="hold"><name>Hold provisional</name><pros>Uses no additional time and preserves the evidence exactly as retained.</pros><cons>MDNS-10 remains unresolved and Phase 3 implementation stays gated.</cons></option>
  </options>
  <resume-signal>Select `approve-120` or `hold`.</resume-signal>
  <done>Either 7,200 active seconds are authorised for Task 2 alone or the plan stops with an honest provisional hold.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Carry the remaining zeroconf cases through one validated exact-head ledger</name>
  <reversibility rating="reversible">All changes remain spike tooling, behavioural tests and evidence; production source, public APIs and dependency manifests are untouched.</reversibility>
  <files>scripts/spike_mdns_candidates.py, scripts/mdns_spike_tests/test_candidates.py, scripts/mdns_spike_inputs/zeroconf_recovery.py, scripts/mdns_spike_tests/test_recovery.py, .planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json</files>
  <read_first>
    - .planning/phases/03-mdns-responder/03-CONTEXT.md
    - .planning/phases/03-mdns-responder/03-SPEC.md
    - .planning/phases/03-mdns-responder/03-RESEARCH.md
    - .planning/phases/03-mdns-responder/03-PATTERNS.md
    - .planning/phases/03-mdns-responder/COVERAGE.md
    - .planning/phases/03-mdns-responder/03-01-EVIDENCE.json
    - .planning/phases/03-mdns-responder/03-continuation-evidence/manifest.json
    - .planning/phases/03-mdns-responder/03-recovery-evidence/local-macos.json
    - .planning/phases/03-mdns-responder/03-recovery-evidence/listener-health-local-macos.json
    - scripts/spike_mdns_candidates.py
    - scripts/mdns_spike_inputs/zeroconf_recovery.py
    - scripts/mdns_spike_tests/test_candidates.py
    - scripts/mdns_spike_tests/test_recovery.py
    - .github/workflows/ci.yml
  </read_first>
  <behavior>
    - "Malformed/resource case: a real public zeroconf instance receives a fixed corpus of empty, short-header, truncated-question, compression-loop and large UDP-safe datagrams, remains able to answer a fresh valid query, and releases its owned transports/tasks/threads after close; no response semantics are invented for malformed input."
    - "Flood case: 256 uniquely identified PTR/direct-query operations finish within a test deadline, retain query/source correlation metrics, leave no growing pending-work inventory, and are followed by successful pristine-client discovery."
    - "Windows case: the spike's injected-platform seam records public constructor/interface choices as identified simulation, never as real Windows socket or multicast evidence."
    - "Configuration/interface case: executable candidate-fit rows cover explicit IPv4 interface selection, per-device A/AAAA data, override/fallback representation, wildcard omission, blank/malformed/wrong-family/link-local rejection, equivalent IPv6 text and opt-out classification; rows distinguish native public API support from small-adapter responsibility and do not claim production validation."
    - "D-08 operation case: each supported lifecycle operation can surface an exception into failed status with retained operation/error identity, owned cleanup and explicit retry; WiFi-only remains live, Thread/mixed applies the shutdown policy, and silent transport closure remains outside the guarantee."
    - "Evidence case: old direct-responder robustness/configuration results remain labelled historical and do not fill zeroconf cells; retained zeroconf manifests are referenced by verified hashes and never rewritten."
    - "Version case: official read-only provenance is resolved first. If the registry-current version differs from the evaluated pin, the ledger supports go only after the same closeout cases are measured in an isolated environment for that exact version; no production upgrade or manifest edit occurs, and unavailable comparison evidence yields provisional."
  </behavior>
  <action>Start the 7,200-second active clock only after Task 1 approval and extend the existing spike harness rather than creating a production adapter. Write failing behavioural tests first, then add one `run-zeroconf-closeout` path that reuses the existing public-API candidate setup and current PR-only CI job; the existing workflow already runs the full spike test directory and exact-head `run-zeroconf-platform`, so fold the new platform fields into that payload and receipt validation without editing CI. Exercise only the fixed cases in `<behavior>`. For malformed and flood inputs, measure continued valid discovery, bounded completion and before/after resource inventories instead of imposing unapproved reply semantics or transferring the direct encoder's limits. For configuration/interface fit, build an executable D-01–D-04 matrix around public `AsyncZeroconf`/`ServiceInfo` inputs and spike-only validation seams; no factory/server implementation is authorised. For D-05–D-08, inject faults only at supported-operation call boundaries and retain the amended distinction between a reported error and silent listener loss. Resolve official candidate provenance before probes; keep 0.151.3 as the evaluated historical pin when unchanged, and if a newer release exists, record and execute an isolated compatibility branch rather than silently replacing the pin. Reconcile D-09/D-10 candidate order and the historical direct-candidate ledger by identity and digest, never by copying its candidate-specific pass cells. Preserve all prior evidence files byte-for-byte. Stop at the active cap, render every unfinished cell as `untested` with an exact acquisition step, and keep the decision provisional. D-12 keeps MDNS-01–09/11 pending; D-13's completed 1/10/100 benchmarks are reused only from verified zeroconf manifests; D-16–D-18 exclude VM and cross-machine work.</action>
  <acceptance_criteria>
    - The focused tests fail before the harness change for every new behaviour family and pass afterward; they probe observable results and cleanup, not implementation text or private zeroconf internals.
    - `03-ZEROCONF-CLOSEOUT.json` identifies exact candidate version(s), source/input/runner/adapter hashes, local and exact-head CI environments, active elapsed time, separate CI queue time, case result/evidence/acquisition step and immutable historical inputs.
    - Candidate-specific robustness, configuration/interface, Windows-simulation and supported-operation rows are all demonstrated, failed, simulated or untested; no old direct-responder row is credited to zeroconf.
    - `running` is described only as lifecycle startup with no handled failure, per D-08; the accepted listener-loss result remains a limitation rather than a failing gate.
    - Existing Ubuntu/macOS, pristine-client, complete-fleet, packaging, removal/restoration and cleanup evidence is reused only after path/hash/version checks; changed or missing inputs force a fresh result or provisional.
    - Registry version drift cannot produce automatic selection or production dependency changes. A go on a changed latest version requires isolated measured parity and official provenance for that exact version.
    - `pyproject.toml`, `uv.lock`, production source, 03-01 evidence/summary and retained continuation/recovery evidence have no diff; no VM, cross-machine test, dependency addition or responder implementation occurs.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run --frozen pytest scripts/mdns_spike_tests/test_candidates.py scripts/mdns_spike_tests/test_recovery.py -q --no-cov &amp;&amp; UV_CACHE_DIR=/tmp/lifx-review-uv uv run --frozen python scripts/spike_mdns_candidates.py run-zeroconf-closeout --candidate-version 0.151.3 --source-revision 48b7efbff59656499373b13ef17e3008d125feb5 --active-budget-seconds 7200 --evidence .planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json &amp;&amp; UV_CACHE_DIR=/tmp/lifx-review-uv uv run --frozen python scripts/spike_mdns_candidates.py validate-zeroconf-closeout .planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json</automated>
  </verify>
  <fails_when>The command exits nonzero for a defective harness, malformed/corrupt ledger, input hash mismatch, historical-candidate result transfer, unlabelled simulation, private API use, false health claim, unsupported go, production/dependency mutation, work beyond the approved case set, active-budget overrun or missing required evidence without a provisional acquisition step. A valid failed/untested case produces a validated provisional or evidence-supported no-go ledger rather than a false pass.</fails_when>
  <done>Every still-required zeroconf closeout cell has candidate-specific evidence or an honest bounded limitation, and the validator can support only the MDNS-10 routes warranted by that exact ledger.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Record the MDNS-10 go, no-go or provisional decision</name>
  <reversibility rating="costly">A go selects the foundation inherited by all later Phase 3 implementation plans, although this plan itself changes no production contract.</reversibility>
  <read_first>
    - .planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json
    - .planning/phases/03-mdns-responder/03-CONTEXT.md
    - .planning/phases/03-mdns-responder/03-SPEC.md
    - .planning/phases/03-mdns-responder/03-01-EVIDENCE.json
    - .planning/phases/03-mdns-responder/03-CONTINUATION.md
    - .planning/phases/03-mdns-responder/03-RECOVERY-PROTOTYPE.md
    - .planning/phases/03-mdns-responder/03-LISTENER-HEALTH-INVESTIGATION.md
  </read_first>
  <action>Run the closeout validator immediately before presenting the decision. Offer go with zeroconf only when the exact evaluated/current-version branch, immutable retained zeroconf evidence and every required remaining case support it through public APIs and the small spike adapter. Treat D-08's accepted silent-listener limitation as an explicit contract limit, not an untested gate; state that `running` is lifecycle status. Offer no-go only when the ledger decisively rejects every D-09 candidate route already evaluated; a zeroconf failure with incomplete fallback evidence is provisional. Offer provisional for any missing, failed-but-not-decisive, version-drift, environment, exact-head or integrity gap, naming the acquisition step. Do not offer another generic extension, infer implementation authority from old direct-candidate passes or mark Phase 3 complete. After the human selection, store only the selected status/rationale/evidence digest in the new closeout ledger and rerun the validator. Per D-12, go returns to planning for MDNS-01–09/11; no-go/provisional keeps them pending.</action>
  <acceptance_criteria>
    - The decision cites the exact closeout ledger digest, candidate version, matching platform receipts and retained input hashes.
    - Go is unavailable when any required candidate-specific cell is failed/untested, a latest-version drift branch lacks measured compatibility, or exact-head evidence does not match.
    - No-go is unavailable unless the evidence decisively rejects all required D-09 routes; other incomplete outcomes are provisional with concrete acquisition steps.
    - The chosen result is recorded without rewriting any historical ledger and explicitly states that MDNS-01–09/11 remain pending until a later implementation plan executes after go.
  </acceptance_criteria>
  <verify><automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run --frozen python scripts/spike_mdns_candidates.py validate-zeroconf-closeout .planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json</automated></verify>
  <fails_when>The validator rejects the selected route, evidence identity is ambiguous, a direct-candidate result is used as zeroconf proof, silent listener health is claimed, a further allowance is implied, or the checkpoint describes spike completion as Phase 3 implementation/completion.</fails_when>
  <decision>Choose the MDNS-10 route supported by the completed candidate ledger.</decision>
  <options>
    <option id="go"><name>Go with zeroconf</name><pros>All required selection gates are demonstrated for the exact supported public-API path.</pros><cons>Later Phase 3 plans inherit zeroconf and the documented silent-listener limitation.</cons></option>
    <option id="no-go"><name>No-go</name><pros>Stops responder implementation when all evaluated foundations are decisively unsupported.</pros><cons>Phase 3 remains blocked until its roadmap is revised or a new evidenced route is approved.</cons></option>
    <option id="provisional"><name>Provisional hold</name><pros>Preserves exact uncertainty and acquisition steps without overstating the evidence.</pros><cons>MDNS-01–09/11 remain unplanned and implementation stays gated.</cons></option>
  </options>
  <resume-signal>Select `go`, `no-go` or `provisional` when offered by the validated ledger.</resume-signal>
  <done>The user records one validator-supported MDNS-10 result; only go unlocks detailed planning of the remaining Phase 3 requirements.</done>
</task>

</tasks>

<api_coverage>
The external library surface is enumerated in `COVERAGE.md`. All supported lifecycle operations that can affect responder ownership are exercised. Discovery/browser consumer APIs and private engine/listener internals are explicit opt-outs with reasons; no unenumerated API capability is treated as integrated.
</api_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted UDP → zeroconf parser/listener | Malformed, truncated and high-rate datagrams enter a third-party parser and event-loop work path. |
| Public zeroconf operations → spike lifecycle adapter | Reported registration/update/unregistration/interface/close failures must preserve truthful state, cleanup and retry ownership. |
| Candidate configuration → host interfaces/multicast | Interface and address selection can expose discovery data or interfere with the host daemon. |
| Historical/CI artefacts → MDNS-10 decision | Stale, mismatched or cross-candidate evidence can produce an unsupported go. |

## STRIDE Threat Register — ASVS L1, block high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-20 | Denial of Service | zeroconf datagram ingestion | high | mitigate | Fixed malformed/truncated corpus and 256-query pressure case must preserve a fresh valid query path, bounded completion and unchanged pending-task/thread inventory. |
| T-03-21 | Spoofing / Information Disclosure | interface and legacy-unicast routing | high | mitigate | Exact-head receipts retain explicit IPv4 interface, source/destination/query correlation and exact public TXT/address records. |
| T-03-22 | Tampering | candidate/version and historical evidence | high | mitigate | Validator checks official provenance, exact version/revision, SHA-256 inputs and candidate labels; direct-responder passes cannot fill zeroconf rows. |
| T-03-23 | Denial of Service | lifecycle operation failures | high | mitigate | Public-operation boundary tests require retained error identity, owned cleanup, blocked replacement after cleanup failure and explicit retry only. |
| T-03-24 | Denial of Service | silent listener loss | medium | accept | D-08 explicitly accepts nondetection for this test-oriented emulator; status text and the decision must avoid a network-health guarantee. |
| T-03-SC | Tampering | isolated zeroconf distribution | high | mitigate | Resolve official PyPI/project provenance, pin exact isolated versions, measure drift before selection and leave project manifests/lockfile unchanged. |
</threat_model>

<deferred_coverage_ledger>
| Requirement | Closeout use | Status after this plan |
|---|---|---|
| MDNS-01 | Explicit-interface/daemon evidence is an MDNS-10 candidate-fit input | Pending implementation after go (D-01–D-04, D-12) |
| MDNS-02 | Retained legacy-unicast evidence is hash-verified | Pending implementation after go (D-12) |
| MDNS-03 | Retained complete-fleet/packet-independent evidence is hash-verified | Pending implementation after go (D-12, D-13) |
| MDNS-04 | Retained exact TXT evidence is hash-verified | Pending implementation after go (D-12) |
| MDNS-05 | Retained A/AAAA family evidence is hash-verified | Pending implementation after go (D-12) |
| MDNS-06 | Configuration/address/interface candidate-fit rows close without production code | Pending implementation after go (D-01–D-04, D-12) |
| MDNS-07 | Retained direct-query evidence is hash-verified | Pending implementation after go (D-12) |
| MDNS-08 | Retained removal/restoration evidence is hash-verified | Pending implementation after go (D-12) |
| MDNS-09 | Supported-operation failure fit closes under amended D-08 | Pending implementation after go (D-05–D-08, D-12) |
| MDNS-11 | Ubuntu/macOS real evidence and identified Windows simulation are selection inputs | Pending implementation after go (D-12) |
</deferred_coverage_ledger>

<verification>
- The focused behavioural suite and `run-zeroconf-closeout` command complete within the approved active cap; the validator accepts the resulting go/no-go/provisional ledger.
- `git diff --exit-code -- pyproject.toml uv.lock packages/lifx-emulator-core/src packages/lifx-emulator/src .planning/phases/03-mdns-responder/03-01-EVIDENCE.json .planning/phases/03-mdns-responder/03-01-SUMMARY.md .planning/phases/03-mdns-responder/03-continuation-evidence .planning/phases/03-mdns-responder/03-recovery-evidence` passes.
- Exact-head Ubuntu/macOS evidence is matched by commit, version, input digest and receipt hash; Windows is labelled simulation.
- The final checkpoint exposes only routes accepted by the validator, and the pending-requirements ledger remains intact.
</verification>

<success_criteria>
- One fresh allowance bounds the final spike work and cannot expand automatically.
- Every remaining zeroconf selection case is demonstrated, failed, simulated or untested with reproducible evidence and an acquisition step.
- D-08 is represented exactly: supported-operation failures are handled; silent listener loss is accepted; `running` is lifecycle status.
- A human records go/no-go/provisional for MDNS-10 from exact evidence.
- No responder implementation, production dependency, VM/cross-machine environment or Phase 3 completion claim is created.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature/requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Evidence-backed responder selection before `lifx-async` discovery implementation | 03-02 | COVERED | This plan closes selection only; the production goal remains gated. |
| REQ | MDNS-10 | Explicit evidenced go/no-go before responder construction | 03-02 | COVERED | Tasks 2–3. |
| REQ | MDNS-01–09, MDNS-11 | Production responder requirements | later post-go plans | EXCLUDED | D-12 is a dependency gate; this plan retains them individually above and does not claim implementation coverage. |
| RESEARCH | — | Zeroconf malformed/flood/resource availability evidence | 03-02 | COVERED | Task 2 fixed behavioural corpus and resource inventory. |
| RESEARCH | — | Identified Windows simulation | 03-02 | COVERED | Task 2 labels simulation distinctly from real platform evidence. |
| RESEARCH | — | Configuration/interface candidate fit | 03-02 | COVERED | Task 2 executable fit matrix, no production integration. |
| RESEARCH | — | Supported-operation lifecycle failures | 03-02 | COVERED | Task 2 under amended D-08. |
| CONTEXT | D-01–D-04 | Configuration ownership, defaults, early validation and immutability | 03-02 | COVERED | Candidate-fit only; implementation remains pending. |
| CONTEXT | D-05–D-07 | Status/error, explicit retry and Thread admission | 03-02 | COVERED | Supported-operation fit only. |
| CONTEXT | D-08 | Narrowed observable runtime-failure policy | 03-02 | COVERED | Silent listener loss accepted and status semantics pinned. |
| CONTEXT | D-09–D-11 | Candidate order, zeroconf preference and fallback identity | 03-02 | COVERED | Historical candidate results reconciled without transfer. |
| CONTEXT | D-12 | Evidence gaps hold implementation planning | 03-02 | COVERED | Deferred ledger and final checkpoint enforce the hold. |
| CONTEXT | D-13 | Complete-fleet sizes 1/10/100 | 03-02 | COVERED | Reused only from verified zeroconf manifests. |
| CONTEXT | D-14–D-15 | Exhausted budget and evidence-justified further work | 03-02 | COVERED | Task 1 grants one new fixed cap; no automatic extension. |
| CONTEXT | D-16–D-18 | Optional VM/cross-machine work | — | EXCLUDED | Explicitly outside this closeout; no extension or environment is authorised. |

<output>
Create `.planning/phases/03-mdns-responder/03-02-SUMMARY.md` after the decision. A go returns to `$gsd-plan-phase 3` to plan MDNS-01–09/11; no-go or provisional holds the phase with exact evidence and acquisition steps.
</output>
