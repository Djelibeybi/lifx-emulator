---
phase: 03-mdns-responder
plan: 07
type: execute
wave: 5
depends_on:
  - 03-06
files_modified:
  - scripts/prepare_mdns_oracle.py
  - packages/lifx-emulator-core/tests/test_mdns_integration.py
  - packages/lifx-emulator-core/tests/test_mdns_platform.py
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - MDNS-01
  - MDNS-02
  - MDNS-03
  - MDNS-04
  - MDNS-05
  - MDNS-06
  - MDNS-07
  - MDNS-08
  - MDNS-09
  - MDNS-11
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Production integration evidence proves complete eligible-fleet discovery for 1, 10 and 100 mixed devices through pristine `lifx-async` at pinned oracle revision 48b7efbff59656499373b13ef17e3008d125feb5, while raw-wire assertions independently prove source/ID/TTL/cache-flush/record correctness."
    - "MDNS-03 edges (adjacency, boundary, concurrency, empty, ordering, precision) pass for zero/one/many complete associated record sets, shared addresses, committed port-zero service ports, overlapping membership changes and unrestricted packet grouping."
    - "Real loopback multicast runs on Ubuntu and macOS beside the host daemon; Windows socket/interface branches are explicitly labelled simulations and never reported as a real Windows run."
    - "MDNS-11 evidence distinguishes local, hosted CI and simulated-platform results; a skipped or unavailable multicast/client test cannot satisfy the dedicated integration job."
    - "D-01–D-08 are implemented by 03-03–03-06; D-09–D-15 and MDNS-10 remain historically fulfilled by the approved 03-02 closeout rather than repeated; optional D-16–D-18 VM/cross-machine work remains out of Phase 3 implementation scope."
    - "The complete repository regression, Ruff and Pyright gates pass after production network evidence, with Phase 2 transport/WebSocket semantics unchanged."
  artifacts:
    - path: "scripts/prepare_mdns_oracle.py"
      provides: "Reproducible isolated checkout and exact revision/tree/cleanliness verification for the lifx-async oracle"
    - path: "packages/lifx-emulator-core/tests/test_mdns_integration.py"
      provides: "Raw multicast plus pristine lifx-async complete-fleet discovery evidence"
    - path: "packages/lifx-emulator-core/tests/test_mdns_platform.py"
      provides: "Explicit Windows simulation and cross-platform socket configuration assertions"
    - path: ".github/workflows/ci.yml"
      provides: "Dedicated Ubuntu/macOS production mDNS integration job with host-daemon prerequisites"
      contains: "mdns-production-integration"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "packages/lifx-emulator-core/tests/test_mdns_integration.py"
      via: "dedicated job sets the required-integration flag, checks out the pinned oracle and fails on skipped tests"
      pattern: "MDNS_INTEGRATION_REQUIRED"
    - from: "scripts/prepare_mdns_oracle.py"
      to: "packages/lifx-emulator-core/tests/test_mdns_integration.py"
      via: "isolated exact-revision checkout exported through LIFX_ASYNC_PATH/PYTHONPATH and installed with uv --with"
      pattern: "LIFX_ASYNC_PATH"
  prohibitions: []
---

<objective>
Prove the finished core responder on real Ubuntu/macOS network stacks, pristine client discovery and identified Windows simulations, then run the full repository quality gates.

Purpose: Convert the production implementation into MDNS-11 evidence without confusing spike receipts, skips or simulations with current implementation proof.
Output: Production integration/platform tests and a dedicated two-OS CI job.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mdns-responder/03-SPEC.md
@.planning/phases/03-mdns-responder/03-CONTEXT.md
@.planning/phases/03-mdns-responder/03-06-SUMMARY.md
@scripts/mdns_spike_inputs/active.json
@.github/workflows/ci.yml
@packages/lifx-emulator-core/tests/test_ipv6_transport.py
@packages/lifx-emulator-core/tests/test_mdns_responder.py

<interfaces>
Final core surface under test:
- `EmulatedLifxServer(..., mdns_enabled=True)` with committed IPv4/IPv6 endpoints.
- `server.mdns_status`, `server.mdns_error`, `await server.wait_for_mdns_updates()` and `await server.retry_mdns()`.
- Factory `mdns_enabled` / `mdns_address` and immutable WiFi/Thread connectivity.
- Pinned pristine oracle revision from `scripts/mdns_spike_inputs/active.json` key `.oracle.revision`.
</interfaces>
</context>

## Artifacts this phase produces

- `packages/lifx-emulator-core/tests/test_mdns_integration.py` with production raw-wire and lifx-async oracle cases.
- `scripts/prepare_mdns_oracle.py` isolated exact-revision oracle acquisition helper.
- `packages/lifx-emulator-core/tests/test_mdns_platform.py` with Windows simulation-labelled cases.
- `.github/workflows/ci.yml` `mdns-production-integration` job and path filter.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove complete production discovery through raw wire and pristine lifx-async</name>
  <files>scripts/prepare_mdns_oracle.py, packages/lifx-emulator-core/tests/test_mdns_integration.py</files>
  <read_first>
    - .planning/phases/03-mdns-responder/03-SPEC.md
    - .planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json
    - scripts/mdns_spike_inputs/active.json
    - scripts/spike_mdns_candidates.py
    - packages/lifx-emulator-core/tests/test_mdns_responder.py
    - packages/lifx-emulator-core/tests/test_mdns_lifecycle.py
    - packages/lifx-emulator-core/tests/test_ipv6_transport.py
  </read_first>
  <behavior>
    - Raw ephemeral-port queries prove reply destination, query ID, clear cache-flush, TTL at most 10 and complete PTR/SRV/TXT/address associations across datagrams.
    - Zero/one/mixed 10/mixed 100 fleets return exactly every eligible serial; WiFi opt-out is absent, Thread is AAAA-only and WiFi is A-only.
    - Shared addresses retain distinct instances, port-zero startup publishes the committed port, and firmware/serial TXT text remains exact.
    - Direct A/AAAA, add/barrier, remove, absent interval and re-add/barrier queries observe only completed immutable states.
    - Pristine lifx-async discovers the entire eligible fleet and can query power over the advertised IPv4/IPv6 endpoint.
  </behavior>
  <action>Create `scripts/prepare_mdns_oracle.py` as a standard-library-only acquisition helper run with `uv run`. It reads `constraints.lifx_async_install`, `.oracle.revision` and `.oracle.tree` from `scripts/mdns_spike_inputs/active.json`, requires the install revision to equal the oracle revision, initialises a caller-supplied fresh temporary directory, fetches that exact commit from the recorded GitHub URL, checks it out detached, and fails unless `git rev-parse HEAD` equals `48b7efbff59656499373b13ef17e3008d125feb5`, `git rev-parse HEAD^{tree}` equals the recorded tree, and `git status --porcelain` is empty. It must never read, reset or mutate `../lifx-async`, and it must fail rather than reuse a non-empty destination. Create production integration tests that instantiate the shipped server/adapter and reuse a local raw DNS fixture from the production test tree, never spike responder or spike candidate modules. Require real multicast only when `MDNS_INTEGRATION_REQUIRED=1`; in that mode missing daemon/interface/oracle prerequisites must fail with a concrete diagnostic rather than skip. Outside the dedicated job, environment-based skips are permitted for ordinary repository runs but must be labelled and never counted as integration evidence. Load the client only from `LIFX_ASYNC_PATH` rooted in the isolated checkout, assert its repository HEAD/tree/clean status again before discovery, and use `PYTHONPATH=<checkout>/src` plus `uv run --with <checkout>` so oracle dependencies are resolved by uv without editing either repository. Exercise 1/10/100 deterministic mixed fleets and compare sets, not packet count/order. Keep performance as recorded evidence rather than a pass threshold. Include live membership completion and endpoint control; do not provision a VM or run cross-machine tests.</action>
  <verify>
    <automated>ORACLE_DIR=$(mktemp -d /tmp/lifx-emulator-mdns-oracle.XXXXXX) &amp;&amp; UV_CACHE_DIR=/tmp/lifx-review-uv uv run scripts/prepare_mdns_oracle.py --destination "$ORACLE_DIR" &amp;&amp; MDNS_INTEGRATION_REQUIRED=1 LIFX_ASYNC_PATH="$ORACLE_DIR/src" PYTHONPATH="$ORACLE_DIR/src" UV_CACHE_DIR=/tmp/lifx-review-uv uv run --with "$ORACLE_DIR" pytest packages/lifx-emulator-core/tests/test_mdns_integration.py -q --no-cov -rs</automated>
    <fails_when>acquisition exits non-zero, exact HEAD/tree/clean checks fail, dependency setup bypasses uv, pytest exits non-zero, any test is skipped, zero tests are collected, or any eligible serial is missing/extra</fails_when>
  </verify>
  <acceptance_criteria>
    - Required-mode tests contain no skip outcome and distinguish raw-wire facts from client-oracle discovery.
    - Exact eligible serial sets pass for 1, 10 and 100 devices independently of datagram grouping/order.
    - Direct queries, shared addresses, exact TXT, port-zero SRV and live membership completion all pass.
    - The isolated pristine pinned client discovers and controls advertised endpoints with exact HEAD/tree and clean-worktree assertions, uv-only dependency setup, and no client patch or spike responder import.
  </acceptance_criteria>
  <done>The finished responder has current raw-wire and client-oracle production evidence for the complete contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate real Ubuntu/macOS integration and explicit Windows simulations in CI</name>
  <files>packages/lifx-emulator-core/tests/test_mdns_platform.py, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/03-mdns-responder/03-SPEC.md
    - .github/workflows/ci.yml
    - packages/lifx-emulator-core/tests/test_mdns_integration.py
    - packages/lifx-emulator-core/tests/test_server.py
    - scripts/mdns_spike_inputs/active.json
  </read_first>
  <behavior>
    - Simulated Windows tests patch the detected platform/socket capability seam and assert public explicit-interface configuration and cleanup; names/output include `simulated_windows`.
    - Dedicated CI runs required-mode production integration on ubuntu-latest and macos-latest beside avahi-daemon/mDNSResponder.
    - CI checks out the pinned pristine sibling revision, configures only disposable loopback/ULA routes already proven by the closeout, and fails on skips.
    - Existing standard Python 3.10–3.14 regression matrix and quality jobs remain intact.
  </behavior>
  <action>Add deterministic Windows simulation tests for the production adapter's public zeroconf interface/IP-version and reuse capability seam; never mutate process-wide `sys.platform`, claim a Windows run, or inherit the direct spike responder's socket-option evidence. Extend CI path filtering for `lifx_emulator/mdns.py`, server/factory/device-manager mDNS files, `scripts/prepare_mdns_oracle.py` and production mDNS tests. Add `mdns-production-integration` with an Ubuntu/macOS matrix: install/start avahi on Linux; assert mDNSResponder and configure the established disposable loopback multicast route/ULA alias on macOS; create a fresh runner-temp oracle directory through `uv run scripts/prepare_mdns_oracle.py`, then run the required integration command with that checkout's `src` in `LIFX_ASYNC_PATH`/`PYTHONPATH` and dependencies supplied by `uv run --with <checkout>`. Fail before pytest unless exact detached HEAD/tree and clean-worktree checks pass, and fail on any test skip. Run `test_mdns_platform.py` in the normal suite as simulation only. Preserve the historical spike jobs and do not use their artefacts as production pass evidence.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest packages/lifx-emulator-core/tests/test_mdns_platform.py -q --no-cov</automated>
    <fails_when>non-zero exit, zero tests are collected, a test is skipped, or simulation labels/platform assertions are absent</fails_when>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run ruff check packages/lifx-emulator-core/src packages/lifx-emulator-core/tests &amp;&amp; UV_CACHE_DIR=/tmp/lifx-review-uv uv run pyright packages/lifx-emulator-core</automated>
    <fails_when>non-zero exit or Ruff/Pyright reports any diagnostic</fails_when>
  </verify>
  <acceptance_criteria>
    - Windows cases are mechanically labelled simulation and exercise the production adapter seam.
    - CI contains a two-OS `mdns-production-integration` job whose required-mode command cannot pass with skips.
    - The job checks out and verifies the exact oracle revision before running client tests.
    - Existing quality/test/release jobs and historical spike evidence jobs remain present.
  </acceptance_criteria>
  <done>CI distinguishes real Linux/macOS production evidence from Windows simulation and rejects unavailable/skipped integration as a pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI host network → production multicast tests | Hosted daemons/routes determine whether real network evidence is possible. |
| sibling checkout → oracle result | External client source is executed as the acceptance oracle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07-01 | Spoofing | client oracle checkout | high | mitigate | Fetch detached pinned revision 48b7efbff59656499373b13ef17e3008d125feb5 and assert HEAD before importing/running it. |
| T-03-07-02 | Repudiation | skipped/simulated evidence | high | mitigate | Required-mode job fails on missing prerequisites/skips and Windows test names/output explicitly record simulation. |
| T-03-07-03 | Information Disclosure | CI logs/artifacts | medium | mitigate | Assert protocol metadata only; do not record host interface inventories, secrets, physical addresses or cross-machine captures. |
| T-03-07-04 | Denial of Service | 100-device integration | low | accept | Deterministic bounded test fleet runs in a dedicated path-filtered job; no performance threshold or unbounded generator is introduced. |
</threat_model>

<source_audit>

| Source | ID | Feature/requirement | Plan coverage | Status | Notes |
|--------|----|---------------------|---------------|--------|-------|
| GOAL | — | lifx-async legacy-unicast discovery with Thread AAAA-only and WiFi A-only | 03-03–03-07 | COVERED | Production tracer, configuration, lifecycle and current oracle evidence. |
| REQ | MDNS-01–MDNS-07 | reception, wire replies, records, metadata, families, addresses, follow-up | 03-03, 03-04, 03-07 | COVERED | Unit plus real two-OS integration. |
| REQ | MDNS-08–MDNS-09 | live membership, failure and ownership | 03-05, 03-06, 03-07 | COVERED | Completion barrier and cleanup are explicit. |
| REQ | MDNS-10 | evidence-led selection | 03-02 | COVERED | Historical fulfilled prerequisite; not repeated. |
| REQ | MDNS-11 | datagram, multicast, client and Windows simulation evidence | 03-03–03-07 | COVERED | Production evidence stays distinct from spike receipts. |
| RESEARCH | — | Use zeroconf public API, bounded ownership, explicit interface and no hand-rolled parser | 03-03–03-06 | COVERED | Closeout supersedes the pre-go provisional wording. |
| RESEARCH | — | Real Ubuntu/macOS plus identified Windows simulation | 03-07 | COVERED | No Windows CI claim. |
| CONTEXT | D-01–D-08 | final public configuration/lifecycle/recovery decisions | 03-03–03-06 | COVERED | Every implementation task cites the governing IDs. |
| CONTEXT | D-09–D-15 | candidate order/evidence/budget decision | 03-02 | COVERED | Historical fulfilled; implementation does not reopen it. |
| CONTEXT | D-16–D-18 | optional VM/cross-machine extension | — | EXCLUDED | Explicitly optional/out of scope; no VM or cross-machine work is planned. |

</source_audit>

<verification>
  <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest</automated>
  <fails_when>non-zero exit or any test outside an explicitly labelled optional local-network skip fails/errors</fails_when>
  <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run ruff check . &amp;&amp; UV_CACHE_DIR=/tmp/lifx-review-uv uv run pyright</automated>
  <fails_when>non-zero exit or any lint/type diagnostic is reported</fails_when>
</verification>

<success_criteria>
- All MDNS-01–09/11 behaviours have focused automated coverage.
- Dedicated Ubuntu/macOS real-network CI cannot pass with skipped integration.
- Windows evidence is simulation-labelled and production-seam specific.
- Full regression, Ruff and Pyright gates pass.
- MDNS-10 remains the approved historical prerequisite and optional VM work remains excluded.
</success_criteria>

<output>
Create `.planning/phases/03-mdns-responder/03-07-SUMMARY.md` when done.
</output>
