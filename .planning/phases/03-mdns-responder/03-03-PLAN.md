---
phase: 03-mdns-responder
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - packages/lifx-emulator-core/pyproject.toml
  - uv.lock
  - packages/lifx-emulator-core/src/lifx_emulator/mdns.py
  - packages/lifx-emulator-core/src/lifx_emulator/server.py
  - packages/lifx-emulator-core/tests/test_mdns_responder.py
autonomous: true
requirements:
  - MDNS-01
  - MDNS-02
  - MDNS-03
  - MDNS-04
  - MDNS-05
  - MDNS-11
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Core construction remains mDNS-opt-in: mdns_enabled defaults to false and opens no mDNS owner or socket; enabling it registers an eligible concrete-address WiFi device through zeroconf 0.151.3 (D-01, D-02, D-10)."
    - "An ephemeral-port PTR query receives a legacy-unicast reply at its source with its own query ID, positive TTL at most 10 seconds and clear cache-flush bits; repeated query IDs remain independent."
    - "MDNS-01 edges (concurrency, idempotency, unclassified) hold for opt-in startup, daemon coexistence and repeated lifecycle ownership; MDNS-02 edges (adjacency, boundary, concurrency, empty, idempotency, ordering, precision, unclassified) hold for source/ID affinity, bounded TTL and zero-device silence."
    - "MDNS-03 default-address edges (adjacency, boundary, concurrency, empty, ordering, precision) produce complete per-device PTR/SRV/TXT/A sets for zero, one and multiple WiFi devices without packet-order or packet-capacity assumptions."
    - "MDNS-04 edges (adjacency, empty, encoding, idempotency, ordering, precision, unclassified) preserve all 12 serial digits, integer firmware text such as 4.200 and stable exact-key TXT content."
    - "The executor checks the live PyPI latest release before dependency mutation; any version other than the approved 0.151.3 returns to the implementation-selection gate instead of silently changing the selected dependency."
  artifacts:
    - path: "packages/lifx-emulator-core/src/lifx_emulator/mdns.py"
      provides: "Public-zeroconf adapter and complete default WiFi ServiceInfo construction"
      exports: ["MdnsResponder"]
    - path: "packages/lifx-emulator-core/src/lifx_emulator/server.py"
      provides: "Opt-in server construction and one owned responder lifecycle"
      contains: "mdns_enabled"
    - path: "packages/lifx-emulator-core/tests/test_mdns_responder.py"
      provides: "Raw datagram and real loopback proof for the tracer path"
  key_links:
    - from: "packages/lifx-emulator-core/src/lifx_emulator/server.py"
      to: "packages/lifx-emulator-core/src/lifx_emulator/mdns.py"
      via: "server starts MdnsResponder only after the committed LIFX port is known and closes it during server stop"
      pattern: "MdnsResponder"
    - from: "packages/lifx-emulator-core/src/lifx_emulator/mdns.py"
      to: "zeroconf.asyncio.AsyncZeroconf"
      via: "public async register, unregister and close operations"
      pattern: "AsyncZeroconf"
  prohibitions: []
---

<objective>
Ship the production tracer for the approved zeroconf responder: one explicitly enabled core server advertises a real WiFi device and answers the legacy-unicast discovery path end to end (D-01, D-02, D-09, D-10).

Purpose: Prove dependency, record construction, committed-port wiring, socket coexistence and owned cleanup before expanding configuration and failure policy.
Output: Locked zeroconf dependency, core adapter, opt-in server seam and executable raw-wire tests.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mdns-responder/03-SPEC.md
@.planning/phases/03-mdns-responder/03-CONTEXT.md
@.planning/phases/03-mdns-responder/03-02-SUMMARY.md
@packages/lifx-emulator-core/src/lifx_emulator/server.py
@packages/lifx-emulator-core/src/lifx_emulator/devices/states.py
@packages/lifx-emulator-core/tests/test_server.py

<interfaces>
Existing interfaces to preserve:
- `EmulatedLifxServer.__init__(..., *, ipv6_bind_address="::1", max_pending_packets=1024)`; append `mdns_enabled: bool = False` as a keyword-only argument.
- `EmulatedLifxServer.start()` publishes `_effective_port`, `_ipv4_endpoint` and `_ipv6_endpoint` under `_lifecycle_lock`; mDNS uses the committed port, including port zero.
- `EmulatedLifxServer.stop()` delegates to `_stop_locked()` while holding the lifecycle lock; responder cleanup must be callable from the locked helper.
- `DeviceState` exposes `serial`, `product`, `version_major`, `version_minor`, `port` and `connectivity` through delegated state.
</interfaces>
</context>

## Artifacts this phase produces

- `lifx_emulator.mdns.MdnsResponder` with async `start()`, `stop()` and public-zeroconf service ownership.
- `EmulatedLifxServer(..., mdns_enabled: bool = False)`.
- `packages/lifx-emulator-core/tests/test_mdns_responder.py`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one WiFi device from server start to a legacy-unicast discovery reply</name>
  <files>packages/lifx-emulator-core/pyproject.toml, uv.lock, packages/lifx-emulator-core/src/lifx_emulator/mdns.py, packages/lifx-emulator-core/src/lifx_emulator/server.py, packages/lifx-emulator-core/tests/test_mdns_responder.py</files>
  <read_first>
    - .planning/phases/03-mdns-responder/03-SPEC.md
    - .planning/phases/03-mdns-responder/03-CONTEXT.md
    - .planning/phases/03-mdns-responder/03-ZEROCONF-CLOSEOUT.json
    - packages/lifx-emulator-core/pyproject.toml
    - packages/lifx-emulator-core/src/lifx_emulator/server.py
    - packages/lifx-emulator-core/src/lifx_emulator/devices/states.py
    - packages/lifx-emulator-core/tests/test_server.py
  </read_first>
  <behavior>
    - Disabled case: default server construction creates no mDNS owner and existing LIFX start/stop behaviour is unchanged.
    - Happy path: an enabled loopback server with one default WiFi device answers an ephemeral-port `_lifx._udp.local.` PTR query and the returned records identify that device at the committed LIFX port.
    - Wire contract: reply destination equals the query source, query ID is echoed, record TTLs are positive and at most 10, and cache-flush bits are clear.
    - Two-stage completion: startup remains pending until both `async_register_service` and its returned announcement awaitable complete; stop/error cleanup remains pending until both `async_unregister_service` and its returned goodbye awaitable complete, then awaits `async_close` once.
    - Error case: failures from either the outer public-operation coroutine or its returned awaitable propagate through startup/cleanup and still leave no work or sockets retained.
  </behavior>
  <action>Before editing dependency files, query the live PyPI JSON metadata for `zeroconf`. The approved 0.151.3 release is current at planning time; if the execution-time latest differs, stop with a blocking decision checkpoint that reports the new version and returns to the D-09/D-10 selection gate. If it remains 0.151.3, use `uv add --package lifx-emulator-core "zeroconf==0.151.3"` so `packages/lifx-emulator-core/pyproject.toml` and `uv.lock` are updated together at the approved exact version. Implement `MdnsResponder` solely through public `AsyncZeroconf`, `ServiceInfo`, `async_register_service`, `async_update_service`, `async_unregister_service` and `async_close` APIs. Treat register, update and unregister as two-stage operations everywhere: await the public method to obtain its returned announcement/goodbye awaitable, then await that returned awaitable before publishing completion; await `async_close` exactly once because it has no second layer. The tracer exercises register and unregister now, while retaining the same helper contract for 03-06 update/reconciliation. Use `_lifx._udp.local.` service identity, `<serial>._lifx._udp.local.` instance names, `<serial>.local.` hostnames, the committed LIFX port, exact `id`/`p`/`fw`/`tm` TXT keys, A-only WiFi addresses, and TTL values no greater than 10. Add keyword-only `mdns_enabled=False` to `EmulatedLifxServer` per D-01/D-02; start the responder only after the endpoint pair and effective port are committed, and close it from the existing locked shutdown path without calling public `stop()` while `_lifecycle_lock` is already held. Write the failing raw-query test first. Add controlled public-operation doubles whose returned awaitables can be held and failed independently from the outer coroutine; prove server start and stop/error cleanup wait for both stages, propagate either-stage errors, and await close once.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest packages/lifx-emulator-core/tests/test_mdns_responder.py -q --no-cov -k "tracer or disabled or legacy_unicast or cleanup"</automated>
    <fails_when>non-zero exit, no selected tests, any selected test is skipped, or the summary contains a failed/error result</fails_when>
  </verify>
  <acceptance_criteria>
    - `packages/lifx-emulator-core/pyproject.toml` declares zeroconf and `uv.lock` resolves version 0.151.3.
    - Default `EmulatedLifxServer` construction leaves mDNS disabled and the established server regression tests pass unchanged.
    - The enabled tracer receives a real loopback query from a non-5353 source and returns the complete WiFi service record set to that source with the query ID, TTL and cache-flush properties asserted.
    - Tests hold and fail the returned register announcement and unregister goodbye awaitables independently, proving startup/cleanup do not finish early and propagate either-layer errors.
    - A failed or stopped tracer has no registered service and no live AsyncZeroconf owner; `async_close` is awaited once.
  </acceptance_criteria>
  <done>One production WiFi discovery path works end to end through the final dependency and server lifecycle seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete deterministic zero, one and multi-device default record behaviour</name>
  <files>packages/lifx-emulator-core/src/lifx_emulator/mdns.py, packages/lifx-emulator-core/tests/test_mdns_responder.py</files>
  <read_first>
    - .planning/phases/03-mdns-responder/03-SPEC.md
    - packages/lifx-emulator-core/src/lifx_emulator/mdns.py
    - packages/lifx-emulator-core/tests/test_mdns_responder.py
    - packages/lifx-emulator-core/src/lifx_emulator/devices/states.py
  </read_first>
  <behavior>
    - Empty fleet: PTR discovery produces no device advertisement.
    - Multiple devices: every eligible default WiFi serial has one associated PTR, SRV, TXT and A record regardless of response packet grouping or order.
    - Exact metadata: trailing-zero serials remain 12 lowercase hexadecimal characters and firmware 4.200 renders as `4.200` on repeated queries.
    - Direct query: a matching A query for the advertised hostname returns the same address; an AAAA query does not invent a WiFi AAAA record.
  </behavior>
  <action>Expand the adapter's immutable ServiceInfo snapshot construction at registration/update time to cover zero, one and multiple default WiFi devices without relying on one packet per device or one packet for the fleet. Snapshot device inputs before creating each complete ServiceInfo so an update never publishes half of a device's record set; zeroconf remains solely responsible for parsing queries and emitting replies through its public registered-service path. Keep service identities serial-specific when addresses are shared, retain exact integer firmware formatting, and let zeroconf group packets as needed. Add a production-test-tree raw DNS query/response fixture in `test_mdns_responder.py` rather than importing spike harness modules; compare record associations as sets across all returned datagrams, repeat queries with distinct IDs, and verify direct A behaviour without imposing a packet count, packet order or fleet-size ceiling.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest packages/lifx-emulator-core/tests/test_mdns_responder.py -q --no-cov</automated>
    <fails_when>non-zero exit, any test is skipped, or the summary reports zero collected tests</fails_when>
  </verify>
  <acceptance_criteria>
    - Record-set assertions pass for zero, one and multiple devices independently of datagram count and order.
    - Every discovered instance has exactly the four TXT keys and one A record, with SRV priority/weight zero and port equal to the committed endpoint.
    - Repeated queries return stable TXT content and independently correct query IDs.
    - Direct A succeeds and opposite-family lookup produces no address for the WiFi hostname.
  </acceptance_criteria>
  <done>The tracer's record core covers the deterministic default-device contract without a packet-capacity limit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| multicast client → zeroconf parser | Untrusted LAN datagrams reach the selected dependency and adapter. |
| device state → DNS-SD records | Emulator identity and endpoint data become LAN-visible records. |
| PyPI/lockfile → packaged core | Third-party code enters the published core dependency graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-01 | Denial of Service | `MdnsResponder` query path | high | mitigate | Use zeroconf's public parser, perform no per-query task allocation in emulator code, snapshot bounded device state once per registration/reconcile operation and cover malformed/empty input in focused tests. |
| T-03-03-02 | Spoofing | legacy-unicast reply routing | medium | mitigate | Let zeroconf reply only to the observed query source for the legacy path and assert source, destination and query-ID affinity on raw datagrams. |
| T-03-03-03 | Information Disclosure | TXT and address records | low | accept | The emulator intentionally publishes serial/product/firmware/connectivity and selected endpoint only; no secrets or persistence paths enter ServiceInfo. |
| T-03-03-SC | Tampering | `uv add` / PyPI distribution | high | mitigate | Require live latest-version equality with the human-approved 0.151.3 selection, mutate through uv only and verify the locked distribution before tests. |
</threat_model>

<verification>
  <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest packages/lifx-emulator-core/tests/test_mdns_responder.py packages/lifx-emulator-core/tests/test_server.py -q --no-cov</automated>
  <fails_when>non-zero exit, a selected mDNS test is skipped, or pytest reports no collected tests</fails_when>
</verification>

<success_criteria>
- Zeroconf 0.151.3 is locked through uv after the execution-time latest-version gate.
- Default core behaviour remains unchanged with mDNS disabled.
- One enabled server answers the actual legacy-unicast path and cleans up owned resources.
- Default WiFi record sets are complete and packet-grouping independent.
</success_criteria>

<output>
Create `.planning/phases/03-mdns-responder/03-03-SUMMARY.md` when done.
</output>
