---
phase: 03-mdns-responder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/spike_mdns_candidates.py
  - packages/lifx-emulator-core/tests/test_mdns_spike_datagrams.py
  - packages/lifx-emulator-core/tests/test_mdns_spike_socket_failures.py
  - .github/workflows/ci.yml
  - docs/evidence/phase-03-mdns-spike-provenance.json
  - docs/evidence/phase-03-mdns-spike.md
autonomous: false
requirements:
  - MDNS-10
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "MDNS-10 ends with criterion-by-criterion demonstrated, failed, simulated, untested or not-run-with-reason evidence and an explicit go, no-go or provisional result; research alone is never proof."
    - "Each candidate run distinguishes harness completion from candidate compliance: a validated rejection or provisional verdict advances successfully, while infrastructure/tool failure and unsupported go evidence fail validation."
    - "Candidate order and preference are fixed: current zeroconf first (D-09, D-10), then direct-versus-adapted lifx-async reuse (D-11), then a new responder only after decisive earlier failures."
    - "Missing platform or PyApp evidence forces a provisional result and holds MDNS-01–09/11 planning (D-12)."
    - "Benchmarks run one WiFi and one Thread device separately, then deterministic 10- and 100-device mixed fleets, measuring complete discovery time, CPU and peak RSS without creating fleet limits (D-13)."
    - "The initial budget is four active hours across all candidates, with CI queue time separate (D-14); extension requires D-15 evidence and never starts automatically."
    - "VM definition/provisioning/testing is absent from the initial run; D-16, D-17 and D-18 remain conditional extension constraints."
    - "Future API/configuration constraints D-01, D-02, D-03 and D-04, and lifecycle/recovery constraints D-05, D-06, D-07 and D-08, are evaluated for candidate fit without production implementation."
  artifacts:
    - path: scripts/spike_mdns_candidates.py
      provides: "Bounded candidate runner, structured outcomes, isolated dependency injection, raw-wire assertions, lifx-async benchmarks and evidence validation"
    - path: packages/lifx-emulator-core/tests/test_mdns_spike_datagrams.py
      provides: "Executable legacy-unicast, record-boundary and discovery-oracle evidence"
    - path: packages/lifx-emulator-core/tests/test_mdns_spike_socket_failures.py
      provides: "Lifecycle, malformed-input, Windows-simulation and cleanup evidence"
    - path: .github/workflows/ci.yml
      provides: "Non-publishing Ubuntu, macOS and Intel macOS PyApp spike evidence job"
    - path: docs/evidence/phase-03-mdns-spike-provenance.json
      provides: "Execution-time latest zeroconf resolution plus immutable candidate/oracle revisions and content digests"
    - path: docs/evidence/phase-03-mdns-spike.md
      provides: "Auditable decision ledger, time accounting and deferred coverage ledger"
  key_links:
    - from: scripts/spike_mdns_candidates.py
      to: docs/evidence/phase-03-mdns-spike-provenance.json
      via: "resolve-provenance output consumed by every uv-isolated candidate runner invocation"
      pattern: "version|tag|commit|sha256|environment_digest"
    - from: scripts/spike_mdns_candidates.py
      to: "lifx-async@48b7efbff59656499373b13ef17e3008d125feb5"
      via: "independent pristine oracle plus disposable direct/adapted candidate snapshots"
      pattern: "git\\+https://github.com/.+@[0-9a-f]{40}"
    - from: .github/workflows/ci.yml
      to: docs/evidence/phase-03-mdns-spike.md
      via: "downloaded non-publishing run artefacts, hashes and run URL"
      pattern: "mdns-spike-evidence"
---

<objective>
Produce the bounded MDNS-10 evidence needed to select, reject or defer an mDNS responder foundation.

Purpose: Prevent production responder construction before raw protocol, host-daemon, platform, no-thread, performance and Intel macOS PyApp feasibility are proved.
Output: Reproducible spike tooling, focused tests, non-publishing CI artefacts, an evidence ledger and one human decision gate.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-mdns-responder/03-CONTEXT.md
@.planning/phases/03-mdns-responder/03-SPEC.md
@.planning/phases/03-mdns-responder/03-RESEARCH.md
@.planning/phases/03-mdns-responder/03-PATTERNS.md
@packages/lifx-emulator-core/src/lifx_emulator/server.py
@packages/lifx-emulator-core/src/lifx_emulator/devices/manager.py
@packages/lifx-emulator-core/tests/test_ipv6_transport.py
@packages/lifx-emulator-core/tests/test_server.py
@.github/workflows/ci.yml
@.github/workflows/release-binaries.yml
@../lifx-async/src/lifx/network/discovery/mdns/discovery.py
@../lifx-async/src/lifx/network/discovery/mdns/dns.py
@../lifx-async/src/lifx/network/discovery/mdns/transport.py
</context>

<interfaces>
- Outcome precedence: the four-active-hour cap always stops work. An unavailable required environment, unverifiable provenance or incomplete probe at that cap is recorded as provisional with acquisition steps. These observed limitations are not `tool_error`; that status means a defective harness or unusable/corrupt evidence. Diagnostic fixes consume the same budget. Candidate rejection remains useful evidence, and no-go requires decisive rejection rather than missing proof.
- `EmulatedLifxServer.ipv4_endpoint` / `ipv6_endpoint` expose the committed same-port pair; the spike may import and run the stock server but must not add responder production APIs.
- `lifx.network.discovery.mdns.discovery._LifxRecordCache.add_packet(records, source_ip)` accumulates per-device packets; use public `discover_mdns()` as the end-to-end oracle.
- PyApp 0.26.0 supports a local candidate wheel through `PYAPP_PROJECT_PATH`; the registry release does not contain this candidate and is not packaging proof.
</interfaces>

<artifacts_this_phase_produces>
- `CandidateSpec`, `CandidateOutcome`, `EvidenceResult`, `resolve_provenance()`, `run_wire_probe()`, `run_discovery_benchmark()` and `validate_evidence()` in `scripts/spike_mdns_candidates.py`.
- `docs/evidence/phase-03-mdns-spike-provenance.json` with resolved official release/tag/commit, oracle revision, snapshot digests and the exact isolated-runner dependency set.
- `test_single_wifi_legacy_unicast_tracer()` and parameterised packet-contract cases in `test_mdns_spike_datagrams.py`.
- `test_windows_socket_branch_is_simulation()` plus malformed-input, repeat-close and no-owned-work cases in `test_mdns_spike_socket_failures.py`.
- CI job `mdns-spike-evidence` and evidence sections `Environment`, `Candidate ledger`, `Benchmarks`, `Platform and packaging`, `Decision`, and `Deferred coverage`.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resolve and exercise one zeroconf WiFi path through the evidence harness</name>
  <reversibility rating="reversible">Spike-only tooling and exact isolated pins can be removed without changing the production package graph or public API.</reversibility>
  <files>scripts/spike_mdns_candidates.py, packages/lifx-emulator-core/tests/test_mdns_spike_datagrams.py, docs/evidence/phase-03-mdns-spike-provenance.json, docs/evidence/phase-03-mdns-spike.md</files>
  <read_first>
    - .planning/phases/03-mdns-responder/03-CONTEXT.md
    - .planning/phases/03-mdns-responder/03-SPEC.md
    - .planning/phases/03-mdns-responder/03-RESEARCH.md
    - .planning/phases/03-mdns-responder/03-PATTERNS.md
    - packages/lifx-emulator-core/tests/test_ipv6_transport.py
    - ../lifx-async/src/lifx/network/discovery/mdns/discovery.py
    - ../lifx-async/src/lifx/network/discovery/mdns/dns.py
    - ../lifx-async/src/lifx/network/discovery/mdns/transport.py
  </read_first>
  <behavior>
    - The runner resolves the latest non-yanked zeroconf release from official metadata at execution, resolves its official tag to a 40-character immutable commit, records release/tag/commit/archive digest, and uses that provenance file for the whole candidate run; research version 0.151.3 is a reference rather than an execution pin.
    - One stock WiFi device is attempted through zeroconf public APIs on an explicit IPv4 interface, receives a legacy-unicast PTR query from an ephemeral source, and is checked by a separate pristine lifx-async oracle pinned to `48b7efbff59656499373b13ef17e3008d125feb5`.
    - A completed probe records `candidate_status` as `meets_gate`, `rejected` or `provisional` and `execution_status` as `completed`; candidate rejection or provisional evidence is a successful harness result, while infrastructure/tool errors use `execution_status=tool_error` and exit nonzero.
    - When a reply exists, the capture independently checks destination, non-zero echoed ID and questions, clear cache-flush bits, positive TTLs at most 10, and one self-contained PTR/SRV/TXT/A device record set; failure of any check is retained as rejection evidence rather than erased by a failing assertion.
    - TXT is exactly `id`, `p`, `fw`, `tm`; the service port is the stock server's committed port; no production responder/module/dependency is added.
  </behavior>
  <action>Start the D-14 four-active-hour ledger before implementation and record CI queue separately. Build the production-quality tracer and a structured outcome state machine in the owned artefacts. Add `resolve-provenance --output docs/evidence/phase-03-mdns-spike-provenance.json`: query official PyPI metadata, identify the latest non-yanked zeroconf release at execution, confirm its official python-zeroconf repository/tag, resolve that tag to an immutable commit and record the source digest. Treat `0.151.3`/`b7aea22b51d82f2139c9143292e86a361ea6796f` only as the research reference; never silently substitute it when the current release differs. Successfully observed disagreement between official metadata/tag/source becomes an evidence-valid provisional outcome that can never support go; network, parser, subprocess or harness errors are tool errors and must exit nonzero. Implement `run-candidate --candidate zeroconf --provenance ... --oracle-revision 48b7efbff59656499373b13ef17e3008d125feb5` so it performs the full probe inside one uv-isolated environment using the immutable requirements recorded in provenance; all candidate tests and the pristine oracle run through this entrypoint. Follow the sibling `MdnsTransport` explicit socket construction, async context management and cleanup-on-error pattern, but require the caller's concrete IPv4 interface and reject its automatic route/interface selection. Use no private zeroconf internals, patch, fork or production integration. Capture real datagrams and turn each wire assertion into retained demonstrated/failed evidence independently of client success. Permit a completed rejected/provisional verdict to return zero after schema validation; reserve nonzero for invalid evidence, mutable/unresolved inputs or tool/infrastructure failure. D-01 through D-08 are future-fit questions only; D-09 and D-10 control this tracer.</action>
  <acceptance_criteria>
    - The focused test starts red, then proves the harness can carry one complete WiFi attempt through candidate API, raw capture and pinned oracle to an evidence-valid `meets_gate`, `rejected` or `provisional` status.
    - If provenance cannot be established or the active-work cap arrives first, record the unrun probe and cause explicitly, close as provisional and proceed to the decision checkpoint; do not require a fabricated packet capture or start a candidate with unresolved provenance.
    - Evidence contains the execution-time latest official release resolution, exact revisions/digests, commands, isolated-environment digest, active elapsed time and packet assertions; zero collected tests or missing result evidence is a tool failure.
    - A decisive zeroconf failure validates as `rejected` and makes the D-09 fallback reachable; missing required proof validates as `provisional`; neither status is reported as candidate compliance.
    - `pyproject.toml`, `uv.lock` and production source remain unchanged.
  </acceptance_criteria>
  <verify><automated>uv run --frozen --isolated python scripts/spike_mdns_candidates.py resolve-provenance --output docs/evidence/phase-03-mdns-spike-provenance.json &amp;&amp; uv run --frozen --isolated python scripts/spike_mdns_candidates.py run-candidate --candidate zeroconf --provenance docs/evidence/phase-03-mdns-spike-provenance.json --oracle-revision 48b7efbff59656499373b13ef17e3008d125feb5 --evidence docs/evidence/phase-03-mdns-spike.md</automated></verify>
  <fails_when>The command exits nonzero for absent result evidence, running a candidate on unresolved provenance, unsupported transitions, production/manifest/lock changes or a defective harness. It exits zero for a validated meets_gate/rejected/provisional result. Zero tests or absent captures are invalid unless the ledger explicitly records that provenance, environment availability or the active-work cap prevented the probe; such a record is provisional and cannot support go.</fails_when>
  <done>A real zeroconf attempt or an explicit preflight/time-cap hold leaves a validated outcome from which D-09 can continue or the decision checkpoint can record provisional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand only as evidence requires and close the four-hour ledger</name>
  <reversibility rating="reversible">Evidence-only tests and CI wiring do not select or integrate a production responder.</reversibility>
  <files>scripts/spike_mdns_candidates.py, packages/lifx-emulator-core/tests/test_mdns_spike_datagrams.py, packages/lifx-emulator-core/tests/test_mdns_spike_socket_failures.py, .github/workflows/ci.yml, docs/evidence/phase-03-mdns-spike-provenance.json, docs/evidence/phase-03-mdns-spike.md</files>
  <read_first>
    - docs/evidence/phase-03-mdns-spike.md
    - .planning/phases/03-mdns-responder/03-SPEC.md
    - .planning/phases/03-mdns-responder/03-RESEARCH.md
    - packages/lifx-emulator-core/src/lifx_emulator/server.py
    - packages/lifx-emulator-core/src/lifx_emulator/devices/manager.py
    - packages/lifx-emulator-core/tests/test_server.py
    - .github/workflows/ci.yml
    - .github/workflows/release-binaries.yml
    - ../lifx-async/src/lifx/network/discovery/mdns/transport.py
  </read_first>
  <behavior>
    - `run-sequence` consumes Task 1's structured zeroconf outcome: `meets_gate` continues the remaining zeroconf gates, `rejected` advances to both D-11 reuse forms, `provisional` stops with a hold, and `tool_error` exits nonzero without being recast as candidate evidence.
    - D-11 direct extension and adaptation each run from disposable immutable snapshots of sibling revision `48b7efbff59656499373b13ef17e3008d125feb5`, preserve the sibling checkout, use separately digested overlays, retain a pristine independently pinned lifx-async oracle, and receive the same protocol/platform/benchmark gates.
    - A new-responder probe is reachable only when both `lifx-direct` and `lifx-adapted` have validated `rejected` outcomes; provisional or tool-error states never authorise it.
    - The candidate ledger covers raw PTR plus direct A/AAAA queries, one complete datagram per device, exact legacy ID/question/cache-flush/TTL behaviour, WiFi A-only and Thread AAAA-only records, exact TXT, explicit interface selection and repeated open/close cleanup.
    - One WiFi and one Thread benchmark run separately; 10 uses 5+5 and 100 uses 50+50, recording complete discovery timing, CPU, peak RSS, datagram count/size, thread inventory and pending owned work.
    - Ubuntu and macOS prove actual multicast beside an identified running host daemon; Windows evidence is explicitly simulated. Intel macOS builds PyApp from a local candidate wheel, proves its metadata contains the candidate, uses a fresh install directory, and proves first-run import/version without publishing a release.
  </behavior>
  <action>Implement `run-sequence --provenance ... --source-revision 48b7efbff59656499373b13ef17e3008d125feb5 --evidence ...` as the only expansion entrypoint; it delegates to `run-candidate --candidate zeroconf|lifx-direct|lifx-adapted|new-responder` and creates one reproducible uv-isolated environment per candidate containing the immutable candidate input and a separate pristine oracle pinned by the provenance file. Continue zeroconf only while its validated status remains `meets_gate`; on `rejected`, stop it and run both D-11 forms. For `lifx-direct`, create a temporary root, run `git clone --no-local ../lifx-async <temporary>/lifx-direct`, detach at the exact revision and apply the bounded extension overlay only there. For `lifx-adapted`, export the same revision's relevant mDNS files with `git archive` from the disposable clone into `<temporary>/lifx-adapted` and adapt only that copy. Never write to the sibling checkout or its Git metadata. Record source commit/tree, exported-file manifest, base snapshot digest, overlay/patch digest, final candidate digest, exact command and isolated-environment digest for each form. Run the oracle from the pristine pinned install, outside both candidate import roots. Apply identical raw-wire, discovery, benchmark, socket, lifecycle, platform and packaging gates to the two forms; compare coupling, maintenance and compatibility only after their evidence is validated. Reach the bounded new-responder runner only after both reuse outcomes are `rejected`; keep every prototype in disposable directories and the spike harness with no production integration. Add bounded malformed/truncated/flood cases, explicit destination/interface checks, repeated lifecycle cleanup, before/during/after thread inventories and zero-pending-task proof. When a decisive hard-gate failure ends a candidate before benchmarks, write each absent benchmark as `not-run` with the decisive criterion and reason; the validator accepts that honest terminal record but rejects absent metrics for any candidate claimed eligible/go. Add a least-privilege `mdns-spike-evidence` CI job that invokes the same provenance-driven harness; it may upload ordinary artefacts but must not invoke release events, registries or release upload actions. Its Intel leg builds a job-local candidate wheel and sets `PYAPP_PROJECT_PATH`, because the published app lacks the candidate. At four active hours stop even mid-candidate and record go/no-go/provisional plus every missing check and exact acquisition command; the cap takes precedence over completing a probe. Per D-12, D-14 and D-15, do not consume extension time. Per D-16, D-17 and D-18, do not define/provision a VM or run cross-machine work; record an extension case only when concrete cited evidence warrants one.</action>
  <acceptance_criteria>
    - Every MDNS-10 criterion and all 46 SPEC edge rows have an evidence status; skips, empty files, missing artefacts and unavailable platforms are untested and force provisional where required, while post-rejection benchmarks are explicitly not-run-with-reason.
    - The sibling checkout, project manifests, lockfile and production source remain unchanged; direct/adapted prototypes are reproducible from recorded exact snapshots and overlay digests.
    - CI evidence records OS/daemon identity, run URL, candidate-wheel hash/METADATA, PyApp build/install/first-run output, and clearly labels Windows simulation.
    - A decisive candidate failure prevents further work on that candidate; fallback order and the four-hour active cap are preserved, and both D-11 forms must reject before a new responder is assessed.
    - Evidence validation exits zero for internally consistent go, no-go or provisional records, including honest candidate rejection; it exits nonzero for tool/infrastructure errors, unsupported go, invalid ordering or missing required evidence without a permitted not-run reason.
    - Permitted not-run reasons include a decisive prior rejection, a required environment being unavailable, unverifiable official provenance, and exhaustion of the active-work cap. Each names the missing check and acquisition step. Absent CI artefacts or benchmarks under these conditions require provisional, except checks made irrelevant by a decisive rejection. A corrupt ledger or defective harness remains an error to diagnose within the remaining budget.
  </acceptance_criteria>
  <verify><automated>uv run --frozen --isolated python scripts/spike_mdns_candidates.py run-sequence --provenance docs/evidence/phase-03-mdns-spike-provenance.json --source-revision 48b7efbff59656499373b13ef17e3008d125feb5 --evidence docs/evidence/phase-03-mdns-spike.md &amp;&amp; uv run --frozen --isolated python scripts/spike_mdns_candidates.py validate-evidence --provenance docs/evidence/phase-03-mdns-spike-provenance.json docs/evidence/phase-03-mdns-spike.md</automated></verify>
  <fails_when>The command exits nonzero for missing ledger, dependency leakage, snapshot/digest mismatch, sibling mutation, invalid ordering, false pass/go claims, unlabelled simulation, missing metrics/artefacts without a permitted not-run reason, extension/VM work, publishing/production integration or a defective harness. A valid no-go/provisional ledger exits zero, including explicitly recorded environment/provenance/time-cap limitations.</fails_when>
  <done>The initial spike closes within four active hours with reproducible complete evidence or an honest provisional/no-go record.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Decide go, no-go, provisional hold or evidenced extension</name>
  <reversibility rating="costly">The selected foundation constrains the remaining Phase 3 plans, although no production contract changes in this spike.</reversibility>
  <read_first>
    - docs/evidence/phase-03-mdns-spike.md
    - .planning/phases/03-mdns-responder/03-CONTEXT.md
    - .planning/phases/03-mdns-responder/03-SPEC.md
  </read_first>
  <action>Run the stdlib-only evidence validator before presenting options, then assess the exact revisions/snapshot digests, raw captures, benchmark tables or valid post-rejection not-run reasons, CI artefact hashes, daemon proof, cleanup/no-thread results and Intel PyApp first-run output produced by Tasks 1–2. The validator must accept internally consistent no-go/provisional evidence, reject any go with a failed/untested required gate or unverifiable zeroconf provenance, reject invalid D-09/D-11 ordering, and return nonzero for malformed evidence or tool/infrastructure errors. Offer go only when every required gate is demonstrated; offer no-go when evidence decisively rejects the evaluated foundations; require provisional hold when platform, packaging or official release provenance remains unverifiable. Offer at most one further four-active-hour extension only if the ledger contains D-15 documentation/source links with exact lines and a concrete worthwhile depth/performance/completeness result; otherwise omit it. Do not ask the user to accept unproven evidence, and do not plan MDNS-01–09/11 until a valid go.</action>
  <acceptance_criteria>
    - The checkpoint names the exact evidence files/run URLs and explains why each available route is valid.
    - Go is unavailable with any required untested/failed gate; provisional explicitly holds remaining planning; no-go preserves findings.
    - Extension is a single optional route, never automatic, and VM work remains separately conditional after required evidence.
  </acceptance_criteria>
  <verify><automated>uv run --frozen --isolated python scripts/spike_mdns_candidates.py validate-evidence --provenance docs/evidence/phase-03-mdns-spike-provenance.json docs/evidence/phase-03-mdns-spike.md</automated></verify>
  <fails_when>The validator command must exit nonzero for malformed/incomplete evidence, tool/infrastructure errors, invalid candidate ordering or unsupported go; an evidence-valid no-go/provisional decision exits zero. The checkpoint also fails if it recommends from research/API shape, hides missing proof, repeats approval requests or treats provisional as implementation authority.</fails_when>
  <decision>Authorise the Phase 3 path supported by the MDNS-10 evidence.</decision>
  <options>
    <option id="go"><name>Go with evidenced candidate</name><pros>All required gates are demonstrated for the named revision.</pros><cons>Commits later Phase 3 planning to this foundation.</cons></option>
    <option id="no-go"><name>No-go</name><pros>Stops unsupported responder construction.</pros><cons>Phase 3 remains blocked pending a new approach.</cons></option>
    <option id="provisional"><name>Provisional hold</name><pros>Preserves honest uncertainty and exact acquisition steps.</pros><cons>Remaining Phase 3 planning stays paused.</cons></option>
    <option id="extend"><name>Evidenced extension</name><pros>Uses at most four further active hours on a cited promising lead.</pros><cons>Valid only when the D-15 case exists; it does not pre-authorise VM work.</cons></option>
  </options>
  <resume-signal>Select go, no-go, provisional, or extend when that option is present in the evidence-backed checkpoint.</resume-signal>
  <done>The user records one evidence-valid route; only go unlocks detailed planning of the pending requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Untrusted UDP → candidate parser/reply path | Malformed, spoofed or high-rate DNS datagrams cross into candidate work. |
| Candidate → explicit host interface/multicast | Binding/reply choices can expose traffic or amplify replies beyond the query source. |
| Package/CI inputs → local wheel/PyApp | Exact source identity and non-publishing artefacts must survive build and first-run. |

## STRIDE Threat Register — ASVS L1, block high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Spoofing/Tampering | DNS query/reply correlation | high | mitigate | Raw tests assert source destination, echoed ID/questions, record ownership, cache-flush and TTL per datagram. |
| T-03-02 | Denial of Service | Parser and candidate work | high | mitigate | Bounded malformed/truncated/flood cases, packet/record limits and zero pending work after close. |
| T-03-03 | Information Disclosure | Interface/TXT selection | high | mitigate | Explicit IPv4 interface only; assert TXT contains exactly public `id/p/fw/tm` and replies target the legacy-unicast source. |
| T-03-04 | Denial of Service | Lifecycle/thread ownership | medium | mitigate | Repeat open/close with socket, task and thread inventories before/during/after. |
| T-03-SC | Tampering | Candidate package and PyApp payload | high | mitigate | Official provenance, immutable commits, uv isolation, local-wheel hash/METADATA proof and non-publishing CI artefacts. |
</threat_model>

<deferred_coverage_ledger>
| Requirement | Retained evaluation input | Status after this plan |
|---|---|---|
| MDNS-01 | AC-01/02/11 opt-in, eligibility, daemon coexistence, repeat lifecycle | Pending; candidate-fit evidence only (D-01–D-04) |
| MDNS-02 | AC-03 legacy destination, ID/questions, cache-flush and TTL | Pending; raw spike gate |
| MDNS-03 | AC-04/06/11 one complete per-device packet, empty/many/shared-address/order edges | Pending; decisive candidate gate |
| MDNS-04 | AC-05 exact/stable serial/product/firmware/connectivity TXT edges | Pending; raw spike gate |
| MDNS-05 | AC-04/06 matching-family-only records | Pending; mixed-family spike gate |
| MDNS-06 | AC-07 explicit/fallback/wildcard/invalid/equivalent-address edges | Pending; future API-fit constraint (D-01–D-04) |
| MDNS-07 | AC-08 direct family query and removed/opted-out absence | Pending; raw spike gate |
| MDNS-08 | AC-09/11 add/remove/re-add/listener/concurrency edges | Pending; future integration-fit constraint |
| MDNS-09 | AC-10/11 status/retry/fleet failure/cleanup edges | Pending; future lifecycle-fit constraint (D-05–D-08) |
| MDNS-11 | AC-13 datagram, actual Ubuntu/macOS multicast, identified Windows simulation | Pending; spike evidence input |

All 46 supplied SPEC edge rows remain evaluation inputs through their mapped ACs. The SPEC found zero bespoke prohibitions; none are invented. No SPEC-less fallback applies.
</deferred_coverage_ledger>

<verification>
- The provenance-driven isolated runner completes the focused spike checks and the stdlib-only evidence validator accepts a non-empty, internally consistent go/no-go/provisional record.
- `git diff -- pyproject.toml uv.lock packages/lifx-emulator-core/src packages/lifx-emulator/src` is empty.
- The sibling `../lifx-async` checkout revision and worktree remain unchanged; direct/adapted experiments exist only as disposable exact-revision snapshots with recorded digests.
- The decision is go/no-go/provisional; extension appears only with a D-15 evidence case.
</verification>

<success_criteria>
- MDNS-10 has reproducible raw, consumer, performance, lifecycle, platform and packaging evidence for the furthest viable candidate in the locked order.
- The initial active work is capped at four hours; CI queue is separate; no VM or production responder work occurs.
- Missing required evidence yields provisional and keeps MDNS-01–09/11 pending.
- A human records the evidence-valid route before any remaining Phase 3 detailed plan is created.
</success_criteria>

## Multi-Source Coverage Audit
| Source | Item | Status |
|---|---|---|
| GOAL | Evidence-backed mDNS responder selection before implementation | COVERED by Tasks 1–3; production goal remains gated |
| REQ | MDNS-10 | COVERED by Tasks 1–3 |
| REQ | MDNS-01–09, MDNS-11 | PENDING by D-12 dependency gate; retained individually above, neither covered nor moved |
| RESEARCH | Raw protocol, oracle, benchmarks, daemon/platform/no-thread/PyApp evidence | COVERED by Tasks 1–2 |
| CONTEXT | D-01–D-08 | COVERED as future-fit constraints only; no pre-go implementation |
| CONTEXT | D-09–D-18 | COVERED by candidate order, evidence gates, budgets and terminal checkpoint |

<output>
Create `.planning/phases/03-mdns-responder/03-01-SUMMARY.md` when done. A go result returns to `$gsd-plan-phase 3` for MDNS-01–09/11 planning; no-go/provisional holds the phase.
</output>
