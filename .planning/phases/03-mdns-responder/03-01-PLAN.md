---
phase: 03-mdns-responder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/spike_mdns_candidates.py
  - scripts/mdns_spike_tests/test_candidates.py
  - scripts/mdns_spike_inputs/active.json
  - .github/workflows/ci.yml
  - .planning/phases/03-mdns-responder/03-01-EVIDENCE.json
  - .planning/phases/03-mdns-responder/03-01-EVIDENCE.md
autonomous: false
requirements:
  - MDNS-10
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "MDNS-10 ends with criterion-by-criterion demonstrated, failed, simulated, untested or not-run-with-reason evidence and an explicit go, no-go or provisional result; research alone is never proof."
    - "Each candidate run distinguishes harness completion from candidate compliance: a validated rejection or provisional verdict advances successfully, while infrastructure/tool failure and unsupported go evidence fail validation."
    - "Candidate order and preference are fixed: current zeroconf first (D-09, D-10), then direct-versus-adapted lifx-async reuse (D-11), then a new responder only after decisive earlier failures."
    - "Raw ::1 AAAA fidelity is proved separately from oracle discovery on a reachable selector-compatible ULA/GUA environment; an unavailable oracle environment is provisional rather than candidate rejection, and scoped link-local is diagnostic only because MDNS-06 rejects advertised link-local addresses (D-12)."
    - "Benchmarks run one WiFi and one Thread device separately, then deterministic 10- and 100-device mixed fleets, measuring complete discovery time, CPU and peak RSS without creating fleet limits (D-13)."
    - "The initial budget is four active hours across all candidates, with CI queue time separate (D-14); extension requires D-15 evidence and never starts automatically."
    - "VM definition/provisioning/testing is absent from the initial run; D-16, D-17 and D-18 remain conditional extension constraints."
    - "Future API/configuration constraints D-01, D-02, D-03 and D-04, and lifecycle/recovery constraints D-05, D-06, D-07 and D-08, are evaluated for candidate fit without production implementation."
  artifacts:
    - path: scripts/spike_mdns_candidates.py
      provides: "Bounded candidate runner, structured outcomes, isolated dependency injection, raw-wire assertions, lifx-async benchmarks and evidence validation"
    - path: scripts/mdns_spike_tests/test_candidates.py
      provides: "Explicitly invoked spike tests outside default pytest testpaths; no candidate or network probe is collected by ordinary CI"
    - path: scripts/mdns_spike_inputs/active.json
      provides: "Frozen immutable input specification for the one candidate currently eligible to run"
    - path: .github/workflows/ci.yml
      provides: "Non-publishing Ubuntu, macOS and Intel macOS PyApp spike evidence job"
    - path: .planning/phases/03-mdns-responder/03-01-EVIDENCE.json
      provides: "Simple machine-validated criterion ledger, immutable candidate/oracle inputs, time accounting and CI artefact hashes"
    - path: .planning/phases/03-mdns-responder/03-01-EVIDENCE.md
      provides: "Human-readable rendering of the JSON ledger and decision evidence"
  key_links:
    - from: scripts/spike_mdns_candidates.py
      to: .planning/phases/03-mdns-responder/03-01-EVIDENCE.json
      via: "one JSON ledger consumed by every candidate runner and validator invocation"
      pattern: "version|tag|commit|sha256|input_spec_digest|realised_environment_digest"
    - from: scripts/spike_mdns_candidates.py
      to: "lifx-async@48b7efbff59656499373b13ef17e3008d125feb5"
      via: "independent pristine oracle plus disposable direct/adapted candidate snapshots"
      pattern: "48b7efbff59656499373b13ef17e3008d125feb5"
    - from: .github/workflows/ci.yml
      to: .planning/phases/03-mdns-responder/03-01-EVIDENCE.json
      via: "PR-head non-publishing artefacts, hashes and run URL merged into the JSON ledger"
      pattern: "mdns-spike-evidence"
---

<objective>
Produce the bounded MDNS-10 evidence needed to select, reject or defer an mDNS responder foundation.

Purpose: Prevent production responder construction before raw protocol, host-daemon, platform, no-thread, performance and Intel macOS PyApp feasibility are proved.
Output: Reproducible spike tooling, focused tests, non-publishing CI artefacts, an evidence ledger and one human decision gate.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-mdns-responder/03-CONTEXT.md
@.planning/phases/03-mdns-responder/03-SPEC.md
@.planning/phases/03-mdns-responder/03-RESEARCH.md
@.planning/phases/03-mdns-responder/03-PATTERNS.md
@packages/lifx-emulator-core/src/lifx_emulator/server.py
@packages/lifx-emulator-core/src/lifx_emulator/devices/manager.py
@packages/lifx-emulator-core/tests/test_ipv6_transport.py
@packages/lifx-emulator-core/tests/test_server.py
@.github/workflows/ci.yml
@.github/workflows/release-binaries.yml
@../lifx-async/src/lifx/network/discovery/mdns/discovery.py
@../lifx-async/src/lifx/network/discovery/mdns/dns.py
@../lifx-async/src/lifx/network/discovery/mdns/transport.py
</context>

<interfaces>
- Outcome precedence: the four-active-hour cap always stops work. An unavailable required environment, unverifiable provenance or incomplete probe at that cap is recorded as provisional with acquisition steps. These observed limitations are not `tool_error`; that status means a defective harness or unusable/corrupt evidence. Diagnostic fixes consume the same budget. Candidate rejection remains useful evidence, and no-go requires decisive rejection rather than missing proof.
- `EmulatedLifxServer.ipv4_endpoint` / `ipv6_endpoint` expose the committed same-port pair; the spike may import and run the stock server but must not add responder production APIs.
- `lifx.network.discovery.mdns.discovery._LifxRecordCache.add_packet(records, source_ip)` accumulates per-device packets; public `lifx.api.discover_mdns()` at `../lifx-async/src/lifx/api.py:1266` is the end-to-end oracle.
- The oracle selector at `../lifx-async/src/lifx/network/discovery/mdns/discovery.py:215-243` does not rank `::1`. Preserve `::1` as a raw AAAA fidelity case, then run the oracle on a reachable ULA or GUA. A scoped link-local can diagnose selector behaviour but cannot satisfy the locked advertised-address contract.
- PyApp 0.26.0 supports a local candidate wheel through `PYAPP_PROJECT_PATH`; the registry release does not contain this candidate and is not packaging proof.
</interfaces>

<artifacts_this_phase_produces>
- `CandidateOutcome`, `CriterionResult`, `resolve_inputs()`, `run_wire_probe()`, `run_discovery_benchmark()`, `run_sequence()` and `validate_evidence()` in `scripts/spike_mdns_candidates.py`.
- `.planning/phases/03-mdns-responder/03-01-EVIDENCE.json` as the single machine-readable ledger; the Markdown evidence file is rendered from it and never parsed for validation.
- `test_single_wifi_legacy_unicast_tracer()` plus parameterised packet, environment-classification, resume and lifecycle cases in `scripts/mdns_spike_tests/test_candidates.py`.
- CI job `mdns-spike-evidence` and evidence sections `Environment`, `Candidate ledger`, `Benchmarks`, `Platform and packaging`, `Decision`, and `Deferred coverage`.
</artifacts_this_phase_produces>

<symbol_list>
- `CandidateOutcome(candidate, execution_status, candidate_status, criteria, elapsed_active_seconds)` keeps harness execution separate from candidate suitability.
- `CriterionResult(status, evidence, acquisition_step)` permits only demonstrated, failed, simulated, untested and not-run-with-reason.
- `input_spec_digest` covers immutable source/version/overlay/dependency constraints shared by local and CI runs; `realised_environment_digest` is recorded separately for each OS/architecture/Python tuple.
- `run_sequence(..., resume=True)` reuses validated Task 1 results and resumes at the first unmet progressive gate.
- `validate_evidence()` validates JSON structure, ordering, time accounting and decision support; `render_evidence()` produces Markdown from JSON.
</symbol_list>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resolve and exercise one zeroconf WiFi path through the evidence harness</name>
  <reversibility rating="reversible">Spike-only tooling and exact isolated pins can be removed without changing the production package graph or public API.</reversibility>
  <files>scripts/spike_mdns_candidates.py, scripts/mdns_spike_tests/test_candidates.py, scripts/mdns_spike_inputs/active.json, .github/workflows/ci.yml, .planning/phases/03-mdns-responder/03-01-EVIDENCE.json, .planning/phases/03-mdns-responder/03-01-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/03-mdns-responder/03-CONTEXT.md
    - .planning/phases/03-mdns-responder/03-SPEC.md
    - .planning/phases/03-mdns-responder/03-RESEARCH.md
    - .planning/phases/03-mdns-responder/03-PATTERNS.md
    - packages/lifx-emulator-core/tests/test_ipv6_transport.py
    - ../lifx-async/src/lifx/network/discovery/mdns/discovery.py
    - ../lifx-async/src/lifx/network/discovery/mdns/dns.py
    - ../lifx-async/src/lifx/network/discovery/mdns/transport.py
  </read_first>
  <behavior>
    - Decision coverage: D-01:, D-02:, D-03:, D-04:, D-05:, D-06:, D-07: and D-08: are candidate-fit criteria only; D-09: and D-10: control the tracer.
    - The D-14 four-active-hour ledger starts before scaffolding, provenance checks or environment probes; only CI queue time is excluded.
    - The runner resolves the latest non-yanked zeroconf release from official metadata at execution and records its immutable version/source digest; research version 0.151.3 is a reference rather than an execution pin.
    - One stock WiFi device is attempted through zeroconf public APIs on an explicit IPv4 interface, receives a legacy-unicast PTR query from an ephemeral source, and is checked by a separate pristine lifx-async oracle pinned to `48b7efbff59656499373b13ef17e3008d125feb5`.
    - A completed probe records `candidate_status` as `meets_gate`, `rejected` or `provisional` and `execution_status` as `completed`; candidate rejection or provisional evidence is a successful harness result, while infrastructure/tool errors use `execution_status=tool_error` and exit nonzero.
    - When a reply exists, the capture independently checks destination, non-zero echoed ID and questions, clear cache-flush bits, positive TTLs at most 10, and one self-contained PTR/SRV/TXT/A device record set; failure of any check is retained as rejection evidence rather than erased by a failing assertion.
    - TXT is exactly `id`, `p`, `fw`, `tm`; the service port is the stock server's committed port; no production responder/module/dependency is added.
  </behavior>
  <action>Addresses review concerns L49-L56@5ebe463 and L67@5ebe463. Start D-14 timing before the first file or environment check and keep all scaffolding inside the four active hours. Implement a small stdlib top-level CLI whose source of truth is `.planning/phases/03-mdns-responder/03-01-EVIDENCE.json`; render Markdown from JSON and never parse Markdown. Before Task 1 completes, commit only the currently eligible zeroconf generator/materialiser and `scripts/mdns_spike_inputs/active.json`, frozen to the exact zeroconf release/tag/commit/archive digest, oracle commit/tree, stock emulator core tree, generator revision, immutable overlay/dependency constraints and shared `input_spec_digest`; do not pre-build fallback generators. Run the top-level script in the workspace with `uv run --frozen python`; spawn candidate workers with `uv run --isolated --no-project --with ./packages/lifx-emulator-core --with zeroconf==RESOLVED --with 'lifx-async @ git+https://github.com/Djelibeybi/lifx-async.git@48b7efbff59656499373b13ef17e3008d125feb5' python scripts/spike_mdns_candidates.py candidate-worker ...`. Record the shared input specification once and record each realised environment separately by OS, architecture and Python version; local and CI `realised_environment_digest` values are not expected to match. Verify the exact oracle SHA through the GitHub commits API and a detached exact-SHA fetch; an acquisition/environment failure is provisional with an acquisition step, never candidate rejection, and does not authorise pushing or re-pinning. Put every spike test under `scripts/mdns_spike_tests/`; ordinary `uv run --frozen pytest` must collect none of them, while explicit-path pytest runs them without direct candidate imports. Build the first zeroconf WiFi legacy-unicast path and progressive gates in cheapest-decisive order: packet boundary, destination/ID/questions/cache-flush/TTL, exact TXT/family records, daemon coexistence, oracle discovery, benchmarks, packaging. Add a PR-only `mdns-spike-evidence` job to the existing CI workflow. Extend `changes` with a cumulative `mdns_spike` output covering `.github/workflows/ci.yml`, `scripts/spike_mdns_candidates.py`, `scripts/mdns_spike_tests/**` and `scripts/mdns_spike_inputs/**`. Add a per-event expensive-job guard: for `synchronize`, fetch the exact `github.event.before` and `github.event.after` SHAs and run only when `git diff --name-only BEFORE AFTER` changes one of those four input path groups; for `opened`, `reopened` and `ready_for_review`, use the full-PR `mdns_spike` result. If either synchronize SHA is absent, fetch fails or the diff cannot be established, conservatively run the expensive job and record why. Thus a receipt-only commit can leave the cumulative filter true without rerunning expensive evidence. Because `dorny/paths-filter` reads pull-request files, the `changes` job declares `contents: read` and `pull-requests: read`; the evidence job separately remains `contents: read`. Use concurrency keyed by PR number with cancellation, retain ordinary artefacts for seven days, and include no release job/action/registry/publishing step. Do not add workflow_dispatch because the revised unmerged workflow cannot provide the first dispatch path. D-01–D-08 remain future-fit questions; D-09/D-10 control the tracer.</action>
  <acceptance_criteria>
    - The focused test starts red, then proves the harness can carry one complete WiFi attempt through candidate API, raw capture and pinned oracle to an evidence-valid `meets_gate`, `rejected` or `provisional` status.
    - If provenance cannot be established or the active-work cap arrives first, record the unrun probe and cause explicitly, close as provisional and proceed to the decision checkpoint; do not require a fabricated packet capture or start a candidate with unresolved provenance.
    - Evidence contains the execution-time latest official release resolution, exact revisions/digests, commands, isolated-environment digest, active elapsed time and packet assertions; the explicit spike test command collects tests, while the repository-default collection excludes the spike directory.
    - A decisive zeroconf failure validates as `rejected` and makes the D-09 fallback reachable; missing required proof validates as `provisional`; neither status is reported as candidate compliance.
    - `pyproject.toml`, `uv.lock` and production source remain unchanged; the currently eligible zeroconf generator and `scripts/mdns_spike_inputs/active.json` are complete and committed before Task 2 pushes that candidate's PR head.
  </acceptance_criteria>
  <verify><automated>uv run --frozen pytest scripts/mdns_spike_tests/test_candidates.py -q --no-cov -k "tracer or collection or resume or classification" &amp;&amp; uv run --frozen python scripts/spike_mdns_candidates.py run-candidate --candidate zeroconf --oracle-revision 48b7efbff59656499373b13ef17e3008d125feb5 --evidence .planning/phases/03-mdns-responder/03-01-EVIDENCE.json --stop-after legacy-wire &amp;&amp; uv run --frozen python scripts/spike_mdns_candidates.py validate-evidence .planning/phases/03-mdns-responder/03-01-EVIDENCE.json</automated></verify>
  <fails_when>The command exits nonzero for absent result evidence, running a candidate on unresolved provenance, unsupported transitions, production/manifest/lock changes or a defective harness. It exits zero for a validated meets_gate/rejected/provisional result. Zero tests or absent captures are invalid unless the ledger explicitly records that provenance, environment availability or the active-work cap prevented the probe; such a record is provisional and cannot support go.</fails_when>
  <done>A real zeroconf attempt or an explicit preflight/time-cap hold leaves a validated outcome from which D-09 can continue or the decision checkpoint can record provisional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand only as evidence requires and close the four-hour ledger</name>
  <reversibility rating="reversible">Evidence-only tests and CI wiring do not select or integrate a production responder.</reversibility>
  <precondition>Task 1 is committed on the Phase 3 branch so the existing default-branch pull_request workflow can acquire the revised CI job from the PR head.</precondition>
  <files>scripts/spike_mdns_candidates.py, scripts/mdns_spike_tests/test_candidates.py, scripts/mdns_spike_inputs/active.json, .planning/phases/03-mdns-responder/03-01-EVIDENCE.json, .planning/phases/03-mdns-responder/03-01-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/03-mdns-responder/03-01-EVIDENCE.json
    - .planning/phases/03-mdns-responder/03-01-EVIDENCE.md
    - .planning/phases/03-mdns-responder/03-SPEC.md
    - .planning/phases/03-mdns-responder/03-RESEARCH.md
    - packages/lifx-emulator-core/src/lifx_emulator/server.py
    - packages/lifx-emulator-core/src/lifx_emulator/devices/manager.py
    - packages/lifx-emulator-core/tests/test_server.py
    - .github/workflows/ci.yml
    - .github/workflows/release-binaries.yml
    - ../lifx-async/src/lifx/network/discovery/mdns/transport.py
  </read_first>
  <behavior>
    - Decision coverage: D-11: controls direct/adapted reuse; D-12: holds later requirements; D-13: fixes benchmark populations; D-14: caps active work; D-15: gates extension; D-16:, D-17: and D-18: exclude initial VM/cross-machine work.
    - `run-sequence --resume` validates and consumes Task 1's structured zeroconf outcome, resumes at its first unmet gate and never repeats a completed probe.
    - D-11 direct extension and adaptation each run from disposable immutable snapshots of sibling revision `48b7efbff59656499373b13ef17e3008d125feb5`, preserve the sibling checkout, use separately digested overlays, retain a pristine independently pinned lifx-async oracle, and receive the same protocol/platform/benchmark gates.
    - A new-responder probe is reachable only when both `lifx-direct` and `lifx-adapted` have validated `rejected` outcomes; provisional or tool-error states never authorise it.
    - The candidate ledger covers raw PTR plus direct A/AAAA queries, one complete datagram per device, exact legacy ID/question/cache-flush/TTL behaviour, WiFi A-only and Thread AAAA-only records, exact TXT, explicit interface selection and repeated open/close cleanup.
    - One WiFi and one Thread benchmark run separately; 10 uses 5+5 and 100 uses 50+50, recording complete discovery timing, CPU, peak RSS, datagram count/size, thread inventory and pending owned work.
    - Raw Thread AAAA fidelity uses `::1`; oracle discovery uses a reachable ULA/GUA environment selected by `_pick_address`. A scoped link-local may diagnose selector behaviour but cannot support go under MDNS-06. Missing reachable oracle setup is provisional.
    - Ubuntu and macOS prove actual multicast beside an identified running host daemon; Windows evidence is explicitly simulated. Intel macOS builds PyApp 0.26.0 from a temporary candidate snapshot wheel, proves wheel METADATA contains the candidate, uses a fresh `PYAPP_INSTALL_DIR_LIFX_EMULATOR`, and proves first-run app plus candidate import/version without publishing.
  </behavior>
  <action>Addresses review concerns L49-L56@5ebe463 and L62-L67@5ebe463. First validate that the committed HEAD contains the currently eligible candidate generator and `scripts/mdns_spike_inputs/active.json`, record that exact SHA and shared `input_spec_digest`, then push that SHA and open or update a draft PR against `main`. Execute `run-sequence --resume` locally against those immutable inputs while CI runs; do not recreate Task 1's completed zeroconf probe. Record PR URL/head SHA, locate the matching CI run, require its `headSha` to equal the recorded candidate HEAD, wait for completion and download only its evidence artefact. Merge it only when the receipt repeats the exact head SHA and shared `input_spec_digest`, base, overlay and final candidate digests; record each local/CI `realised_environment_digest` under its own OS/architecture/Python key rather than comparing cross-platform values. An older run, shared-input mismatch or missing platform identity is invalid evidence. After a decisive candidate failure, Task 2 may spend remaining D-14 time authoring only the next candidate permitted by D-09/D-11: update the harness and `active.json`, commit and push those inputs, then collect a new exact-head CI run under the same rules. Evaluate direct and adapted lifx-async reuse after zeroconf rejection; do not author a new-responder candidate until both D-11 forms have validated `rejected` outcomes. Receipt/render-only commits do not run the expensive job because the synchronize guard sees no harness/test/workflow/frozen-input delta; missing diff evidence conservatively runs. Separate raw `::1` AAAA fidelity from oracle discovery on a concrete reachable ULA/GUA; record scoped link-local only as selector diagnostic and make an unavailable acceptable address environment provisional rather than rejected. Absence of an existing responder is `needs-overlay`, not rejection. Generate each eligible overlay into a disposable exact-SHA snapshot, record base tree/overlay/final digests, and use the same committed immutable input specification locally and in CI; do not mutate `../lifx-async`. For the Intel packaging leg, build three temporary artefacts from the active frozen input: an exact stock `lifx-emulator-core` wheel from the recorded core tree, the evaluated candidate wheel/source artefact, and a copied `lifx-emulator` app wheel whose temporary METADATA uses direct local-file references to those exact two artefacts instead of `lifx-emulator-core>=2.4.0`; keep all paths and metadata edits under `$RUNNER_TEMP`. Record all hashes and METADATA, then build PyApp v0.26.0 with relative `PYAPP_PROJECT_PATH`, `PYAPP_PYTHON_VERSION=3.12`, `PYAPP_EXEC_SPEC=lifx_emulator_app.__main__:main`, `PYAPP_UV_ENABLED=true`, `PYAPP_UV_VERSION=0.9.9`, `PYAPP_EXPOSE_PYTHON=true` and `CARGO_BUILD_TARGET=x86_64-apple-darwin`. Use `dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87` to install `stable` and record `rustc -Vv`; the action SHA pins the installer, not the compiler. With a nonexistent `PYAPP_INSTALL_DIR_LIFX_EMULATOR`, run the binary once, then use `self python` to record installed app/core/candidate identities, METADATA/direct-url evidence and hashes. Label this as packaging installability of the exact stock core plus evaluated candidate, never production responder integration or proof of a published manifest. Keep all work inside the same D-14 active cap, stop at the limit, and render missing checks plus acquisition commands. D-12 keeps MDNS-01–09/11 pending; D-15 alone permits the later checkpoint to offer one extension; D-16–D-18 prohibit VM and cross-machine work now.</action>
  <acceptance_criteria>
    - Every MDNS-10 criterion and all 46 SPEC edge rows have an evidence status; skips, empty files, missing artefacts and unavailable platforms are untested and force provisional where required, while post-rejection benchmarks are explicitly not-run-with-reason.
    - The sibling checkout, project manifests, lockfile and production source remain unchanged; direct/adapted prototypes are reproducible from recorded exact snapshots and overlay digests.
    - CI evidence records the exact matching candidate HEAD, shared `input_spec_digest`, base/overlay/final digests, OS/daemon identity, run URL, per-platform `realised_environment_digest`, app/core/candidate hashes and METADATA, `rustc -Vv`, PyApp first-run distribution identities and identified Windows simulation; no cross-platform environment-digest equality is required.
    - A decisive candidate failure prevents further work on that candidate; fallback order and the four-hour active cap are preserved, and both D-11 forms must reject before a new responder is assessed.
    - Evidence validation exits zero for internally consistent go, no-go or provisional records, including honest candidate rejection; it exits nonzero for tool/infrastructure errors, unsupported go, invalid ordering or missing required evidence without a permitted not-run reason.
    - Permitted not-run reasons include a decisive prior rejection, a required environment being unavailable, unverifiable official provenance, and exhaustion of the active-work cap. Each names the missing check and acquisition step. Absent CI artefacts or benchmarks under these conditions require provisional, except checks made irrelevant by a decisive rejection. A corrupt ledger or defective harness remains an error to diagnose within the remaining budget.
  </acceptance_criteria>
  <verify><automated>uv run --frozen pytest scripts/mdns_spike_tests/test_candidates.py -q --no-cov &amp;&amp; uv run --frozen python scripts/spike_mdns_candidates.py run-sequence --resume --source-revision 48b7efbff59656499373b13ef17e3008d125feb5 --evidence .planning/phases/03-mdns-responder/03-01-EVIDENCE.json &amp;&amp; uv run --frozen python scripts/spike_mdns_candidates.py validate-evidence .planning/phases/03-mdns-responder/03-01-EVIDENCE.json</automated></verify>
  <fails_when>The command exits nonzero for missing ledger, dependency leakage, snapshot/digest mismatch, sibling mutation, invalid ordering, false pass/go claims, unlabelled simulation, missing metrics/artefacts without a permitted not-run reason, extension/VM work, publishing/production integration or a defective harness. A valid no-go/provisional ledger exits zero, including explicitly recorded environment/provenance/time-cap limitations.</fails_when>
  <done>The initial spike closes within four active hours with reproducible complete evidence or an honest provisional/no-go record.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Decide go, no-go, provisional hold or evidenced extension</name>
  <reversibility rating="costly">The selected foundation constrains the remaining Phase 3 plans, although no production contract changes in this spike.</reversibility>
  <read_first>
    - .planning/phases/03-mdns-responder/03-01-EVIDENCE.json
    - .planning/phases/03-mdns-responder/03-01-EVIDENCE.md
    - .planning/phases/03-mdns-responder/03-CONTEXT.md
    - .planning/phases/03-mdns-responder/03-SPEC.md
  </read_first>
  <action>Run the stdlib-only evidence validator before presenting options, then assess the exact revisions/snapshot digests, raw captures, benchmark tables or valid post-rejection not-run reasons, CI artefact hashes, daemon proof, cleanup/no-thread results and Intel PyApp first-run output produced by Tasks 1–2. The validator must accept internally consistent no-go/provisional evidence, reject any go with a failed/untested required gate or unverifiable zeroconf provenance, reject invalid D-09/D-11 ordering, and return nonzero for malformed evidence or tool/infrastructure errors. Offer go only when every required gate is demonstrated; offer no-go when evidence decisively rejects the evaluated foundations; require provisional hold when platform, packaging or official release provenance remains unverifiable. Offer at most one further four-active-hour extension only if the ledger contains D-15 documentation/source links with exact lines and a concrete worthwhile depth/performance/completeness result; otherwise omit it. Do not ask the user to accept unproven evidence, and do not plan MDNS-01–09/11 until a valid go.</action>
  <acceptance_criteria>
    - The checkpoint names the exact evidence files/run URLs and explains why each available route is valid.
    - Go is unavailable with any required untested/failed gate; provisional explicitly holds remaining planning; no-go preserves findings.
    - Extension is a single optional route, never automatic, and VM work remains separately conditional after required evidence.
  </acceptance_criteria>
  <verify><automated>uv run --frozen python scripts/spike_mdns_candidates.py validate-evidence .planning/phases/03-mdns-responder/03-01-EVIDENCE.json</automated></verify>
  <fails_when>The validator command must exit nonzero for malformed/incomplete evidence, tool/infrastructure errors, invalid candidate ordering or unsupported go; an evidence-valid no-go/provisional decision exits zero. The checkpoint also fails if it recommends from research/API shape, hides missing proof, repeats approval requests or treats provisional as implementation authority.</fails_when>
  <decision>Authorise the Phase 3 path supported by the MDNS-10 evidence.</decision>
  <options>
    <option id="go"><name>Go with evidenced candidate</name><pros>All required gates are demonstrated for the named revision.</pros><cons>Commits later Phase 3 planning to this foundation.</cons></option>
    <option id="no-go"><name>No-go</name><pros>Stops unsupported responder construction.</pros><cons>Phase 3 remains blocked pending a new approach.</cons></option>
    <option id="provisional"><name>Provisional hold</name><pros>Preserves honest uncertainty and exact acquisition steps.</pros><cons>Remaining Phase 3 planning stays paused.</cons></option>
    <option id="extend"><name>Evidenced extension</name><pros>Uses at most four further active hours on a cited promising lead.</pros><cons>Valid only when the D-15 case exists; it does not pre-authorise VM work.</cons></option>
  </options>
  <resume-signal>Select go, no-go, provisional, or extend when that option is present in the evidence-backed checkpoint.</resume-signal>
  <done>The user records one evidence-valid route; only go unlocks detailed planning of the pending requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Untrusted UDP → candidate parser/reply path | Malformed, spoofed or high-rate DNS datagrams cross into candidate work. |
| Candidate → explicit host interface/multicast | Binding/reply choices can expose traffic or amplify replies beyond the query source. |
| Package/CI inputs → local wheel/PyApp | Exact source identity and non-publishing artefacts must survive build and first-run. |

## STRIDE Threat Register — ASVS L1, block high
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Spoofing/Tampering | DNS query/reply correlation | high | mitigate | Raw tests assert source destination, echoed ID/questions, record ownership, cache-flush and TTL per datagram. |
| T-03-02 | Denial of Service | Parser and candidate work | high | mitigate | Bounded malformed/truncated/flood cases, packet/record limits and zero pending work after close. |
| T-03-03 | Information Disclosure | Interface/TXT selection | high | mitigate | Explicit IPv4 interface only; assert TXT contains exactly public `id/p/fw/tm` and replies target the legacy-unicast source. |
| T-03-04 | Denial of Service | Lifecycle/thread ownership | medium | mitigate | Repeat open/close with socket, task and thread inventories before/during/after. |
| T-03-SC | Tampering | Candidate package and PyApp payload | high | mitigate | Official provenance, immutable commits, uv isolation, local-wheel hash/METADATA proof and non-publishing CI artefacts. |
</threat_model>

<deferred_coverage_ledger>
| Requirement | Retained evaluation input | Status after this plan |
|---|---|---|
| MDNS-01 | AC-01/02/11 opt-in, eligibility, daemon coexistence, repeat lifecycle | Pending; candidate-fit evidence only (D-01–D-04) |
| MDNS-02 | AC-03 legacy destination, ID/questions, cache-flush and TTL | Pending; raw spike gate |
| MDNS-03 | AC-04/06/11 one complete per-device packet, empty/many/shared-address/order edges | Pending; decisive candidate gate |
| MDNS-04 | AC-05 exact/stable serial/product/firmware/connectivity TXT edges | Pending; raw spike gate |
| MDNS-05 | AC-04/06 matching-family-only records | Pending; mixed-family spike gate |
| MDNS-06 | AC-07 explicit/fallback/wildcard/invalid/equivalent-address edges | Pending; future API-fit constraint (D-01–D-04) |
| MDNS-07 | AC-08 direct family query and removed/opted-out absence | Pending; raw spike gate |
| MDNS-08 | AC-09/11 add/remove/re-add/listener/concurrency edges | Pending; future integration-fit constraint |
| MDNS-09 | AC-10/11 status/retry/fleet failure/cleanup edges | Pending; future lifecycle-fit constraint (D-05–D-08) |
| MDNS-11 | AC-13 datagram, actual Ubuntu/macOS multicast, identified Windows simulation | Pending; spike evidence input |

All 46 supplied SPEC edge rows remain evaluation inputs through their mapped ACs. The SPEC supplies no bespoke prohibition rows; the fallback recall confirms there are no additional phase-specific prohibitions to invent, while canonical protocol, security and repository-safety constraints remain enforced elsewhere in this plan.
</deferred_coverage_ledger>

<verification>
- The provenance-driven isolated runner completes the focused spike checks and the stdlib-only evidence validator accepts a non-empty, internally consistent go/no-go/provisional record.
- `git diff -- pyproject.toml uv.lock packages/lifx-emulator-core/src packages/lifx-emulator/src` is empty.
- The sibling `../lifx-async` checkout revision and worktree remain unchanged; direct/adapted experiments exist only as disposable exact-revision snapshots with recorded digests.
- The decision is go/no-go/provisional; extension appears only with a D-15 evidence case.
</verification>

<success_criteria>
- MDNS-10 has reproducible raw, consumer, performance, lifecycle, platform and packaging evidence for the furthest viable candidate in the locked order.
- The initial active work is capped at four hours; CI queue is separate; no VM or production responder work occurs.
- Missing required evidence yields provisional and keeps MDNS-01–09/11 pending.
- A human records the evidence-valid route before any remaining Phase 3 detailed plan is created.
</success_criteria>

## Review Dispositions Ledger

### Round 1 — 5ebe463

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Scaffolding and fallback authoring can consume the D-14 cap | HIGH | Plan 03-01 Tasks 1–2 use one simple JSON ledger, cheapest-decisive progressive gates, bounded D-11 delta/overlay work and start the clock before all scaffolding. |
| Spike tests would pollute default pytest collection | HIGH | Plan 03-01 Tasks 1–2 place all spike tests under `scripts/mdns_spike_tests/` and invoke them only by explicit path. |
| `::1` cannot be selected by the pinned oracle | HIGH | Plan 03-01 Tasks 1–2 separate raw loopback AAAA fidelity from oracle discovery on reachable ULA/GUA and classify unavailable oracle setup as provisional. |
| CI evidence lacked a first executable trigger path | HIGH | Plan 03-01 Tasks 1–2 add a filtered PR-only job to the existing default-branch CI workflow, then push/open a draft PR, wait for the matching head-SHA run and download hashed artefacts. |
| Candidate snapshot, overlay transfer and PyApp first-run were underspecified | MEDIUM | Plan 03-01 Task 2 uses committed overlay generators/digests, temporary package metadata, full pinned PyApp/Rust build inputs, a fresh install directory and candidate import/version proof. |
| Task 2 could repeat Task 1's zeroconf probe | MEDIUM | Plan 03-01 Tasks 1–2 define and test `run-sequence --resume`, which starts at the first unmet gate. |
| TDD tracer did not run pytest | MEDIUM | Plan 03-01 Task 1 now runs the explicit spike test path before the tracer command. |
| Loopback injection and LAN/oracle checks were blended | MEDIUM | Plan 03-01 Tasks 1–2 record raw loopback, daemon/LAN and oracle environments as separate criteria. |
| Evidence Markdown required a brittle validator and lived under published docs | MEDIUM | Plan 03-01 uses JSON as the validator input and renders Markdown under the phase directory. |
| Public `discover_mdns()` citation was indirect | LOW | Plan 03-01 interfaces point to `../lifx-async/src/lifx/api.py:1266`. |
| PyApp toolchain prerequisites were incomplete | LOW | Plan 03-01 Task 2 names the pinned Rust action pattern, PyApp v0.26.0 checkout, build variables, wheel location and fresh first-run proof. |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| Move preflight/scaffolding outside the four-hour clock | Rejected per D-14: all active work, including scaffolding and preflight, remains inside the cap. |
| Replace PyApp build/first-run with a plain import | Rejected because D-12 and MDNS-10 require PyApp packaging evidence; the plan retains the full build and labels its exact scope. |
| Reject lifx-async reuse because it lacks a responder | Rejected per D-11: absence is recorded as a delta and bounded overlays are evaluated before rejection. |
| Push or re-pin oracle commit `48b7efbff59656499373b13ef17e3008d125feb5` | Rejected: the exact commit resolves through the GitHub API; Tasks 1–2 verify/acquire it without changing the pin. |
| Treat `uv run --isolated` as omitting the project or conflicting with `--frozen` | Rejected as factually unsupported; the plan instead makes workspace top-level and `--isolated --no-project --with ...` child environments explicit. |
| Add Ruff C90/PLR exemptions for the harness | Rejected because those rule families are not selected; explicit tests and existing Ruff checks remain sufficient for this spike. |

## Internal Checker Dispositions

### Iteration 1 — ab22a12314883f94010b8ac022ed4e6caddcbe582e9e95cdaebd7175cc02ed3

| Finding | Disposition |
|---------|-------------|
| `dorny/paths-filter` lacks pull-request permission | Addressed in Task 1: the change detector receives `contents: read` plus `pull-requests: read`; the evidence job remains `contents: read`. |
| CI could run an older Task 1 head while Task 2 authors overlays | Addressed in Tasks 1–2: the currently eligible generator and frozen manifest are committed before its push; artefacts merge only for the exact head SHA and matching shared input specification plus base/overlay/final digests, while realised environments remain platform-specific. |
| Temporary app wheel could resolve released core from PyPI | Addressed in Task 2: the temporary app dependency graph uses direct local references to the exact stock core wheel and evaluated candidate, then verifies installed distribution identities and provenance without production edits. |
| Rust action pin was described as a compiler pin | Addressed in Task 2: the pinned action installs `stable`, and the evidence records `rustc -Vv` as the actual compiler identity. |

### Iteration 2 — targeted checker follow-up

| Finding | Disposition |
|---------|-------------|
| Task 1 required fallback generators before their candidates became eligible | Addressed in Tasks 1–2: Task 1 freezes only the zeroconf generator and `scripts/mdns_spike_inputs/active.json`; after a decisive failure, Task 2 may author, commit and push only the next D-09/D-11-permitted candidate inputs within the same cap. |
| Cumulative pull-request path filtering would rerun on receipt-only commits | Addressed in Task 1: opened/reopened/ready-for-review events use the full-PR filter, while synchronize events fetch and diff the exact before/after SHAs; the expensive job runs only for workflow, harness, test or frozen-input changes and runs conservatively when the delta cannot be established. |
| Cross-platform environment-digest equality was impossible | Addressed throughout Tasks 1–2: `input_spec_digest` is the shared immutable comparison key, while each OS/architecture/Python environment records its own `realised_environment_digest`. |

## Multi-Source Coverage Audit
| Source | Item | Status |
|---|---|---|
| GOAL | Evidence-backed mDNS responder selection before implementation | COVERED by Tasks 1–3; production goal remains gated |
| REQ | MDNS-10 | COVERED by Tasks 1–3 |
| REQ | MDNS-01–09, MDNS-11 | PENDING by D-12 dependency gate; retained individually above, neither covered nor moved |
| RESEARCH | Raw protocol, oracle, benchmarks, daemon/platform/no-thread/PyApp evidence | COVERED by Tasks 1–2 |
| CONTEXT | D-01–D-08 | COVERED as future-fit constraints only; no pre-go implementation |
| CONTEXT | D-09–D-18 | COVERED by candidate order, evidence gates, budgets and terminal checkpoint |

<output>
Create `.planning/phases/03-mdns-responder/03-01-SUMMARY.md` when done. A go result returns to `$gsd-plan-phase 3` for MDNS-01–09/11 planning; no-go/provisional holds the phase.
</output>
