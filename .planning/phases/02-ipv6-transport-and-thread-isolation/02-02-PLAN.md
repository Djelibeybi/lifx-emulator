---
phase: 02-ipv6-transport-and-thread-isolation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/lifx-emulator/src/lifx_emulator_app/api/services/event_bridge.py
  - packages/lifx-emulator/src/lifx_emulator_app/api/app.py
  - packages/lifx-emulator/tests/test_websocket.py
autonomous: true
requirements: [HYG-01]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every synchronous device, activity, and state-change callback schedules its WebSocket coroutine through an owner-local tracker using an exact colon-separated operation label."
    - "Forced garbage collection loses no broadcast and each completion removes its task exactly once (SPEC Edge Coverage: HYG-01 idempotency/repetition and concurrency/effect ordering)."
    - "Direct observer/wiring construction and non-lifespan `TestClient(app)` construction remain admission-compatible; entering lifespan provides the bounded shutdown guarantee."
    - "FastAPI teardown stops `StatsBroadcaster`, drains bridge work for at most five seconds, cancels/awaits the remainder, and leaves no tracked task."
    - "Existing WebSocket payloads, synchronous inner-observer order, and public positional arguments remain compatible."
  artifacts:
    - path: "packages/lifx-emulator/src/lifx_emulator_app/api/services/event_bridge.py"
      provides: "Backward-compatible keyword-only tracker injection for every fire-and-forget WebSocket bridge callback"
      contains: "task_tracker"
    - path: "packages/lifx-emulator/src/lifx_emulator_app/api/app.py"
      provides: "One application-owned tracker with lifespan reopen/drain and test diagnostics"
      contains: "background_task_tracker"
    - path: "packages/lifx-emulator/tests/test_websocket.py"
      provides: "Direct-construction, label, retention, failure, non-lifespan admission, and lifespan-drain coverage"
      contains: "BackgroundTaskTracker"
  key_links:
    - from: "packages/lifx-emulator/src/lifx_emulator_app/api/app.py"
      to: "packages/lifx-emulator-core/src/lifx_emulator/background_tasks.py"
      via: "create_api_app owns one open tracker, injects it into all adapters, and the lifespan drains it"
      pattern: "BackgroundTaskTracker"
    - from: "packages/lifx-emulator/src/lifx_emulator_app/api/services/event_bridge.py"
      to: "packages/lifx-emulator/src/lifx_emulator_app/api/services/websocket_manager.py"
      via: "Each callback passes the unchanged WebSocket coroutine and exact operation label to its tracker"
      pattern: '_task_tracker\.schedule'
  prohibitions:
    - statement: "No bespoke Phase 2 prohibition exists beyond AC-01 through AC-15; the SPEC records the supplied prohibition row as None."
      status: resolved-none
      verification: ".planning/phases/02-ipv6-transport-and-thread-isolation/02-SPEC.md Prohibitions table"
---

<objective>
Complete HYG-01 in the standalone application by routing every event-bridge broadcast through owner-local `BackgroundTaskTracker` instances, implementing D-13 through D-16 without breaking direct construction or WebSocket message contracts.

Purpose: The Plan 01 tracker becomes the single scheduling policy for synchronous-to-async bridge callbacks. The application factory owns the shared production instance and FastAPI lifespan supplies its deterministic shutdown boundary.

Output: compatible tracker-injected adapters, stable labels, application lifespan ownership/drain, and warning-free forced-GC/failure/direct-construction tests.

<acceptance_criteria>
- All event-bridge fire-and-forget broadcasts use a supplied or owner-local fallback tracker; there is no module-global collection or bare scheduling helper.
- Existing positional arguments remain valid; `task_tracker` is keyword-only and optional on every importable entry point.
- Production factory wiring injects one exact tracker; direct/non-lifespan construction accepts work rather than silently closing it.
- Lifespan teardown leaves `app.state.background_task_tracker.pending_count == 0` after the periodic broadcaster stops.
</acceptance_criteria>
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-SPEC.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-CONTEXT.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-RESEARCH.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-01-SUMMARY.md
@CLAUDE.md
@.claude/CLAUDE.md

<interfaces>
Plan 01 provides `BackgroundTaskTracker(owner, accepting=True)`, labelled `schedule`, idempotent admission control, `pending_count`, and reusable async `shutdown(timeout=5.0)`.
Preserved entry points: `wire_device_events(device_manager, ws_manager)`, `WebSocketActivityObserver(ws_manager, inner_observer=None)`, and `WebSocketStateChangeObserver(ws_manager)`.
New optional suffix on each entry point: `*, task_tracker: BackgroundTaskTracker | None = None`; the existing positional arguments and order do not change.
Exact operation labels: `device-added:<serial>`, `device-removed:<serial>`, `activity:rx:<pkt_type>`, `activity:tx:<pkt_type>`, and `device-updated:<serial>:<pkt_type>`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one device-added callback through compatible tracked broadcast</name>
  <reversibility rating="reversible">D-13 through D-15 replace an internal scheduler behind preserved callback and payload contracts.</reversibility>
  <files>packages/lifx-emulator/src/lifx_emulator_app/api/services/event_bridge.py, packages/lifx-emulator/tests/test_websocket.py</files>
  <read_first>
    - packages/lifx-emulator/src/lifx_emulator_app/api/services/event_bridge.py (`_schedule_async`, three importable adapter entry points, callback order, and `StatsBroadcaster`)
    - packages/lifx-emulator-core/src/lifx_emulator/background_tasks.py (Plan 01 tracker contract)
    - packages/lifx-emulator/src/lifx_emulator_app/api/services/websocket_manager.py (broadcast method arguments and payloads)
    - packages/lifx-emulator/tests/test_websocket.py (direct observer construction and warning-producing scheduler patches)
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-REVIEWS.md (Round 1 constructor compatibility, admission, labels, and StatsBroadcaster findings)
  </read_first>
  <behavior>
    - A device-added callback schedules its unchanged payload once through the injected tracker with `device-added:<serial>` and survives forced collection.
    - Existing two-position-argument `WebSocketActivityObserver(ws_manager, inner_observer)` construction remains valid.
    - Omitting `task_tracker` creates one accepting owner-local fallback captured by that adapter, with a DEBUG diagnostic that it has no external lifespan owner.
    - Every five fire-and-forget operation families uses the exact label contract from `<interfaces>`.
    - Activity delegates synchronously before scheduling and broadcast failures are observed once without an un-awaited coroutine.
  </behavior>
  <action>
Remove `_schedule_async` and import `BackgroundTaskTracker` at module scope. Add the backward-compatible keyword-only optional tracker parameter to `wire_device_events`, `WebSocketActivityObserver`, and `WebSocketStateChangeObserver`. A missing tracker creates a distinct accepting fallback owned by the closure/observer and logs at DEBUG that normal task completion, rather than application lifespan, is its drain boundary; never use a module-global collection. Route all five callback-created coroutines through `schedule()` with the exact labels in `<interfaces>`. Preserve inner-observer order, callback composition, and every broadcast argument. Keep `StatsBroadcaster` on its existing awaited `_task` lifecycle because it is not fire-and-forget HYG-01 work. Addresses review concern: required/positional injection would break public call sites or create invalid Python, labels were underspecified, and StatsBroadcaster's intentional exclusion needed to be explicit.

Replace scheduler patching with an injected recording tracker that closes unawaited captures. Add the real-tracker device-added tracer, then expand assertions across device removed, RX, TX, and updated callbacks, including failure logging and exact payload preservation.
  </action>
  <verify>
    <automated>uv run pytest packages/lifx-emulator/tests/test_websocket.py -q --no-cov -k "event_bridge or activity_observer or state_change"</automated>
    <fails_when>non-zero exit, zero tests selected, a legacy constructor raises, any exact label or payload differs, forced collection loses a broadcast, or a coroutine/task exception warning appears</fails_when>
  </verify>
  <acceptance_criteria>
    - All legacy positional construction sites remain valid and the new tracker argument is keyword-only with a `None` fallback.
    - No `_schedule_async` definition/call or module-global task collection remains.
    - Tests cover all five exact operation labels and unchanged broadcast payloads.
    - `StatsBroadcaster` remains explicitly managed by its own awaited start/stop task and is not routed through the short-lived tracker.
  </acceptance_criteria>
  <done>Every bridge callback has compatible owner-local task retention, stable diagnostics, and unchanged WebSocket data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Own and drain the production bridge tracker in FastAPI lifespan</name>
  <reversibility rating="reversible">D-16 adds bounded teardown to the existing application lifespan without changing public HTTP/WebSocket routes.</reversibility>
  <files>packages/lifx-emulator/src/lifx_emulator_app/api/app.py, packages/lifx-emulator/tests/test_websocket.py</files>
  <read_first>
    - packages/lifx-emulator/src/lifx_emulator_app/api/app.py (`create_api_app`, nested lifespan, manager/broadcaster construction, adapter wiring, and app.state)
    - packages/lifx-emulator/src/lifx_emulator_app/api/services/event_bridge.py (Task 1 keyword-only injection and exact labels)
    - packages/lifx-emulator-core/src/lifx_emulator/background_tasks.py (open/reopen/shutdown semantics)
    - packages/lifx-emulator/tests/test_websocket.py (`client` returns non-context-managed TestClient; lifespan cases require `with TestClient(app)`)
    - packages/lifx-emulator/tests/test_api.py (non-context-managed TestClient compatibility surface)
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-CONTEXT.md (D-13 and D-16)
  </read_first>
  <behavior>
    - `create_api_app` creates one accepting bridge tracker and injects that exact object into all three adapter entry points.
    - Constructing `TestClient(app)` without entering its context never closes admission; a callback scheduled on a running loop is retained and completes.
    - Lifespan startup idempotently calls `start_accepting()` so a second lifespan can reuse a previously drained application tracker.
    - Teardown always stops `StatsBroadcaster` before tracker shutdown, including when the lifespan body raises.
    - Normal and exceptional teardown leave `pending_count == 0`.
  </behavior>
  <action>
Instantiate `BackgroundTaskTracker("websocket-event-bridge", accepting=True)` alongside `WebSocketManager` and `StatsBroadcaster`, expose it as `app.state.background_task_tracker`, and pass the exact object with keyword arguments to all Task 1 adapters. In lifespan, call `start_accepting()` before starting the broadcaster; use `try/finally` to stop the broadcaster and then `shutdown(timeout=5.0)`. This open-at-construction/reopen-at-lifespan model prevents direct or non-context-managed test clients from silently refusing broadcasts while preserving a deterministic drain whenever ASGI lifespan is entered. Addresses review concern: closed-by-default admission conflicted with the repository's direct protocol and non-lifespan TestClient patterns.

Add one non-context-managed construction/admission regression and context-managed normal, exceptional, and re-entered lifespan cases. Coordinate pending broadcasts with events, not arbitrary sleeps. Assert tracker identity across adapters, stop-before-drain ordering, cancellation/await of the timed-out remainder, and zero pending tasks.
  </action>
  <verify>
    <automated>uv run pytest packages/lifx-emulator/tests/test_websocket.py packages/lifx-emulator/tests/test_api.py -q --no-cov</automated>
    <fails_when>non-zero exit, zero tests collected, non-lifespan construction closes admission, injected tracker identities differ, teardown order is wrong, pending work survives teardown, or existing API/WebSocket behaviour regresses</fails_when>
  </verify>
  <acceptance_criteria>
    - One production tracker exists per app and is open immediately, exposed in app state, and shared by all adapters.
    - Non-context-managed `TestClient` construction does not disable bridge callbacks; context-managed lifespan provides and proves bounded shutdown.
    - Normal, exceptional, and repeated lifespan cycles finish with no tracked work after `StatsBroadcaster.stop()`.
  </acceptance_criteria>
  <done>The application supports existing construction patterns while lifespan deterministically drains every production event-bridge task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UDP/device callbacks → WebSocket bridge | Packet-driven synchronous events can fan out asynchronous broadcasts. |
| WebSocket coroutine → FastAPI lifespan | Application teardown must account for every production broadcast. |

## STRIDE Threat Register (ASVS 4.0.3 Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Denial of service | event bridge callback fan-out | high | mitigate | Strong owner-local tracking, forced-GC flood tests, and bounded production lifespan drain/cancel. |
| T-02-06 | Repudiation | failed WebSocket broadcast | medium | mitigate | Exact operation labels and one retrieved/logged outcome per task. |
| T-02-07 | Tampering | tracker ownership | medium | mitigate | Keyword-only injection of one app-owned instance; fallback instances remain owner-local and no global collection exists. |
| T-02-08 | Denial of service | FastAPI shutdown | high | mitigate | Stop the periodic producer first, then five-second bridge grace followed by cancel/await and empty-state assertion. |
| T-02-SC | Tampering | package installation | low | accept | No package-manager install occurs in this plan. |
</threat_model>

<verification>
  <automated>uv run pytest packages/lifx-emulator/tests/test_websocket.py packages/lifx-emulator/tests/test_api.py -q --no-cov</automated>
  <fails_when>non-zero exit, zero tests collected, any task/coroutine warning, changed payload, constructor incompatibility, or non-empty production tracker after lifespan teardown</fails_when>
</verification>

<success_criteria>
- Every event-bridge fire-and-forget broadcast is strongly retained with exact operation context.
- Existing adapter and TestClient construction patterns accept work; production lifespan owns deterministic shutdown.
- Forced GC loses no broadcast, failures/cancellations are consumed once, and payloads remain unchanged.
</success_criteria>

## Artifacts this plan produces

- Backward-compatible tracker injection and exact label formatting in `event_bridge.py`.
- One production tracker and bounded lifespan lifecycle in `app.py`.
- Direct-construction, non-lifespan, retention, failure, and lifespan tests in `test_websocket.py`; no core transport or future API/schema symbols are claimed here.

## Review Dispositions Ledger

### Round 1 — 60efff5

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Closed admission breaks non-lifespan `TestClient` and direct construction. | HIGH | Task 2 creates the app tracker open, reopens idempotently on lifespan entry, and tests the repository's non-context-managed construction pattern. |
| Required tracker parameters break importable observer/wiring signatures and could be invalid after a defaulted argument. | MEDIUM | Task 1 preserves all positional parameters and adds only keyword-only `task_tracker=None` suffixes with owner-local fallbacks. |
| Operation labels were underspecified. | LOW | The interfaces block fixes exact colon-separated labels for all five event families and Task 1 asserts them. |
| `StatsBroadcaster` appeared unmanaged by the new tracker. | LOW | Task 1 explicitly retains its existing awaited `_task`; Task 2 stops it before draining fire-and-forget bridge work. |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| A tracker created for an app whose ASGI lifespan is never entered has no deterministic teardown callback. | Such construction has no application lifecycle boundary to invoke; admission remains compatible and completed tasks self-remove, while bounded drain is guaranteed and tested whenever lifespan is entered. |

<output>
Create `.planning/phases/02-ipv6-transport-and-thread-isolation/02-02-SUMMARY.md` when done.
</output>
