---
phase: 02-ipv6-transport-and-thread-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lifx-emulator-core/src/lifx_emulator/background_tasks.py
  - packages/lifx-emulator-core/src/lifx_emulator/server.py
  - packages/lifx-emulator-core/src/lifx_emulator/devices/device.py
  - packages/lifx-emulator-core/tests/test_background_tasks.py
  - packages/lifx-emulator-core/tests/test_server.py
  - packages/lifx-emulator-core/tests/test_device.py
autonomous: true
requirements: [HYG-01, HYG-02]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Completion callbacks retrieve outcomes and remove each task exactly once; an explicitly reopened tracker can schedule a new lifecycle after a completed shutdown (SPEC Edge Coverage: HYG-01 idempotency/repetition)."
    - "Forced garbage collection cannot lose server or persistence work, and every owner-local collection drains to empty after completion or bounded shutdown (SPEC Edge Coverage: HYG-01 concurrency/effect ordering)."
    - "Tagged and all-zero targets render as `broadcast`, while a packet shorter than a complete header emits no activity event (SPEC Edge Coverage: HYG-02 empty/degenerate)."
    - "Valid untagged targets render the first six target bytes as exactly 12 lowercase hexadecimal characters, including serials ending in `0`, `00`, and `0000` (SPEC Edge Coverage: HYG-02 encoding/representation)."
    - "Short, header-unparsable, and payload-unparsable datagram counters have explicit regression assertions before family-routing reorders valid-packet effects."
    - "The existing IPv4 request/reply path remains usable after packet scheduling moves behind the tracker, including direct protocol construction without `server.start()`."
  artifacts:
    - path: "packages/lifx-emulator-core/src/lifx_emulator/background_tasks.py"
      provides: "Reusable owner-local BackgroundTaskTracker with labelled scheduling, outcome consumption, reopenable admission, pending diagnostics, and bounded shutdown"
      contains: "class BackgroundTaskTracker"
    - path: "packages/lifx-emulator-core/src/lifx_emulator/server.py"
      provides: "Tracked IPv4 packet scheduling and canonical exact target rendering"
      contains: "_format_target"
    - path: "packages/lifx-emulator-core/src/lifx_emulator/devices/device.py"
      provides: "Exception-observing owner-local tracking for persistence work"
      contains: "BackgroundTaskTracker"
    - path: "packages/lifx-emulator-core/tests/test_background_tasks.py"
      provides: "Deterministic task retention, completion, failure, cancellation, scheduling-failure, reopen, grace, and timeout coverage"
      contains: "TestBackgroundTaskTracker"
  key_links:
    - from: "packages/lifx-emulator-core/src/lifx_emulator/server.py"
      to: "packages/lifx-emulator-core/src/lifx_emulator/background_tasks.py"
      via: "LifxProtocol.datagram_received schedules each packet coroutine through the server-owned tracker"
      pattern: '_background_tasks\.schedule'
    - from: "packages/lifx-emulator-core/src/lifx_emulator/devices/device.py"
      to: "packages/lifx-emulator-core/src/lifx_emulator/background_tasks.py"
      via: "_save_state schedules labelled persistence work through a device-owned tracker"
      pattern: "BackgroundTaskTracker"
    - from: "packages/lifx-emulator-core/src/lifx_emulator/server.py"
      to: "packages/lifx-emulator-core/src/lifx_emulator/devices/observers.py"
      via: "_format_target supplies PacketEvent.target only after complete-header parsing"
      pattern: "target=target_str"
  prohibitions:
    - statement: "No bespoke Phase 2 prohibition exists beyond AC-01 through AC-15; the SPEC records the supplied prohibition row as None."
      status: resolved-none
      verification: ".planning/phases/02-ipv6-transport-and-thread-isolation/02-SPEC.md Prohibitions table"
---

<objective>
Establish the production task-lifetime and exact-target pattern on the existing IPv4 path before a second protocol instance is introduced, implementing HYG-01/HYG-02 and D-13 through D-16.

Purpose: The phase tracer proves that a retained datagram travels from the existing protocol callback through normal response/activity behaviour while forced garbage collection runs. Two bounded expansions then migrate persistence work and fix all operator-visible target rendering while pinning malformed-datagram statistics.

Output: `BackgroundTaskTracker`, tracked server/device work, exact activity and unknown-packet targets, and regression tests for retention, failures, cancellation, scheduling failure, reopen, bounded shutdown, malformed input, broadcast rendering, and trailing-zero serials.

<acceptance_criteria>
- HYG-01: every fire-and-forget packet or persistence task created in the touched core paths is retained until completion, its result is retrieved, expected cancellation is debug-logged, and every terminal path empties its owner-local collection.
- HYG-02: `PacketEvent.target`, normal RX logging, and unknown-packet logging use exact first-six-byte serial rendering; tagged/all-zero targets use `broadcast`; short packets emit no event.
- Short and malformed packet statistics are explicit, stable inputs to Plan 03's resolve-before-effects change.
- The stock server still owns only its existing IPv4 protocol after this plan; no AF_INET6 socket or public endpoint property is introduced.
</acceptance_criteria>
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-SPEC.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-CONTEXT.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-RESEARCH.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-PATTERNS.md
@.planning/phases/01-thread-device-identity/01-04-SUMMARY.md
@CLAUDE.md
@.claude/CLAUDE.md

<interfaces>
Existing `EmulatedLifxServer.LifxProtocol.datagram_received(data, addr)` schedules `server.handle_packet(data, addr)` without retaining the created task.
Existing `EmulatedLifxDevice._save_state()` creates a task and `_track_save_task()` retains it without retrieving its outcome.
Existing `PacketEvent.target` is `str | None`; `ActivityLogger.on_packet_received()` copies it unchanged into recent activity.
New tracker contract: `BackgroundTaskTracker(owner: str, *, accepting: bool = True)`, `schedule(coroutine, operation) -> asyncio.Task | None`, `start_accepting()`, `stop_accepting()`, `shutdown(timeout: float = 5.0)`, and read-only `pending_count`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one retained IPv4 datagram from protocol callback to reply</name>
  <reversibility rating="reversible">D-13 through D-16 are internal lifecycle policy behind unchanged packet and activity interfaces.</reversibility>
  <files>packages/lifx-emulator-core/src/lifx_emulator/background_tasks.py, packages/lifx-emulator-core/src/lifx_emulator/server.py, packages/lifx-emulator-core/tests/test_background_tasks.py, packages/lifx-emulator-core/tests/test_server.py</files>
  <read_first>
    - packages/lifx-emulator-core/src/lifx_emulator/server.py (`LifxProtocol.datagram_received` is the tracer entry; preserve `handle_packet` and the single IPv4 transport contract in this task)
    - packages/lifx-emulator-core/src/lifx_emulator/devices/device.py (`background_save_tasks` and `_track_save_task` are the strong-reference analogue)
    - packages/lifx-emulator-core/tests/test_server.py (`TestProtocolClass.test_protocol_datagram_received` directly constructs a protocol without calling `start()`)
    - packages/lifx-emulator-core/tests/test_background_tasks.py (new file; follow the core async pytest style)
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-CONTEXT.md (D-13, D-14, D-15, D-16)
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-REVIEWS.md (Round 1 tracker-admission, cancellation, creation-failure, and sizing concerns)
  </read_first>
  <behavior>
    - A scheduled coroutine is in the owner-local strong set before `schedule()` returns and is removed exactly once after success.
    - `_on_done` checks `task.cancelled()` before retrieving an exception, debug-logs expected cancellation, retrieves every non-cancelled outcome, logs failures once at ERROR with owner and operation, and discards in `finally` per D-15.
    - No running loop or closed admission closes the supplied coroutine and logs the refusal without an un-awaited-coroutine warning per D-14.
    - If task creation itself raises, the supplied coroutine is closed before the original failure is logged and re-raised.
    - The tracker accepts work at construction; `shutdown()` closes admission and empties it; `start_accepting()` may reopen an empty tracker for a later owner lifecycle.
    - A valid IPv4 datagram submitted through a directly constructed `LifxProtocol` remains alive across `gc.collect()`, completes once, and replies once.
  </behavior>
  <action>
Create `BackgroundTaskTracker` with the exact interface above. Implement D-13 with per-instance state only. Implement D-14 by closing coroutine objects on no-loop, closed-admission, and task-creation failure paths. Implement D-15 with cancellation inspection before `task.exception()`, labelled outcome logging, and removal in `finally`. Implement D-16 with `asyncio.wait()` for the grace period followed by explicit cancellation and `gather(return_exceptions=True)` for the remainder. Make a completed shutdown reusable only through an explicit `start_accepting()` call when no tasks remain; both admission methods are idempotent.

Create one accepting server-owned tracker in `EmulatedLifxServer.__init__` and route `LifxProtocol.datagram_received` through it with a stable `packet:<peer-host>:<peer-port>` operation label. Preserve direct `LifxProtocol(server)` construction and do not require `start()` to open initial admission. Leave `start()`/`stop()` transport lifecycle unchanged in this task because Plan 04 owns the single atomic D-09 through D-12/D-16 lifecycle rewrite. Addresses review concern: direct protocol tests must schedule work, cancellation must not raise from the done callback, task-creation failures must close coroutines, tracker reuse must be explicit, and the original oversized hygiene task must be split.

Write the tracer test before implementation: block `handle_packet` on an event, call `datagram_received`, force `gc.collect()`, prove the task remains pending, release it, then assert one handler invocation/reply and an empty tracker. Add focused tracker tests for success and no-loop refusal here; the complete terminal matrix is Task 2.
  </action>
  <verify>
    <automated>uv run pytest packages/lifx-emulator-core/tests/test_background_tasks.py packages/lifx-emulator-core/tests/test_server.py -q --no-cov -k "background_task or protocol_datagram_received or retained"</automated>
    <fails_when>non-zero exit, zero tests selected, direct protocol construction drops the datagram, forced collection loses work, task creation leaks a coroutine, or the tracker remains non-empty after completion</fails_when>
  </verify>
  <acceptance_criteria>
    - The tracer blocks a real scheduled handler, forces collection, and proves exactly one completion with `pending_count == 0`.
    - `BackgroundTaskTracker` defaults to accepting, owns no module-global collection, and can reopen only after a completed empty shutdown.
    - Cancelled tasks never call `task.exception()`; non-cancelled outcomes are retrieved before final removal.
    - A forced `create_task` failure closes the coroutine and preserves the original exception.
  </acceptance_criteria>
  <done>The production IPv4 callback path retains packet work across garbage collection and the reusable tracker has a precise, warning-free lifecycle contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the tracker matrix and migrate device persistence</name>
  <reversibility rating="reversible">The device adopts the shared internal tracker while its persistence coroutine, data, and timing remain unchanged.</reversibility>
  <files>packages/lifx-emulator-core/src/lifx_emulator/devices/device.py, packages/lifx-emulator-core/tests/test_background_tasks.py, packages/lifx-emulator-core/tests/test_device.py</files>
  <read_first>
    - packages/lifx-emulator-core/src/lifx_emulator/background_tasks.py (Task 1 contract to exercise, not duplicate)
    - packages/lifx-emulator-core/src/lifx_emulator/devices/device.py (`__init__`, `_save_state`, and `_track_save_task`; preserve state and response behaviour)
    - packages/lifx-emulator-core/tests/test_device.py (device fixture and persistence test style)
    - packages/lifx-emulator-core/tests/test_async_storage.py (existing persistence save/load behaviour is the regression oracle)
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-RESEARCH.md (Resolved Decision 2 and Pattern 4)
  </read_first>
  <behavior>
    - Success, raised exception, expected cancellation, no loop, closed admission, task-creation failure, grace completion, timeout cancellation, and reopen all finish with `pending_count == 0`.
    - Device state saves remain fire-and-forget and strongly retained while pending.
    - A successful save executes exactly once; a failed save is consumed and logged once with the serial-bearing operation label.
    - Invoking `_save_state` without a running loop closes the persistence coroutine and emits no RuntimeWarning.
  </behavior>
  <action>
Expand `test_background_tasks.py` to cover every terminal path, coordinating with events rather than sleeps and explicitly asserting the cancelled-task and task-creation-failure cases from Round 1. Test a short timeout without changing the production default of five seconds and prove `start_accepting()` after an empty shutdown permits another exactly-once task.

Replace `EmulatedLifxDevice.background_save_tasks` and `_track_save_task()` with one device-owned `BackgroundTaskTracker`. Pass the persistence coroutine directly to `schedule()` with operation label `save-device:<serial>` so D-14 owns all refusal/creation-failure closure paths. Preserve the no-storage early return, serialised fields, debounce semantics, and Phase 1 Thread identity. Addresses review concern: the existing save-task done callback retains work but never retrieves failures.
  </action>
  <verify>
    <automated>uv run pytest packages/lifx-emulator-core/tests/test_background_tasks.py packages/lifx-emulator-core/tests/test_device.py packages/lifx-emulator-core/tests/test_async_storage.py packages/lifx-emulator-core/tests/test_thread_identity.py -q --no-cov</automated>
    <fails_when>non-zero exit, zero tests collected, a terminal path leaves pending work, persistence executes more or less than once, a failure is unconsumed, or Phase 1 identity/persistence behaviour regresses</fails_when>
  </verify>
  <acceptance_criteria>
    - The tracker matrix covers success, failure, cancellation, both refusal causes, task-creation failure, grace completion, timeout cancellation, and reopen.
    - `device.py` contains one owner-local tracker and no `_track_save_task` implementation.
    - Successful and failing persistence operations each execute once and finish with no retained work or un-awaited-coroutine warning.
  </acceptance_criteria>
  <done>Server and device fire-and-forget work share one exception-observing policy without sharing task collections or changing persistence behaviour.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix exact targets and pin malformed-datagram statistics</name>
  <reversibility rating="reversible">HYG-02 corrects a defective operator representation without changing packet bytes or public data types.</reversibility>
  <files>packages/lifx-emulator-core/src/lifx_emulator/server.py, packages/lifx-emulator-core/tests/test_server.py</files>
  <read_first>
    - packages/lifx-emulator-core/src/lifx_emulator/server.py (unknown-packet warning, normal RX debug log, `PacketEvent.target`, short-header guard, header unpack, and payload unpack paths)
    - packages/lifx-emulator-core/src/lifx_emulator/devices/manager.py (`header.target[:6].hex()` is the canonical exact-serial rule)
    - packages/lifx-emulator-core/src/lifx_emulator/devices/observers.py (`PacketEvent` and `ActivityLogger` are the observable HYG-02 surface)
    - packages/lifx-emulator-core/tests/test_server.py (`test_handle_packet_too_short`, unknown packet tests, and server statistics/activity classes)
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-SPEC.md (AC-13, AC-14 and HYG-02 edge rows)
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-REVIEWS.md (Round 1 short/unparsable statistics and unknown-log findings)
  </read_first>
  <behavior>
    - Specific targets ending in `0`, `00`, and `0000` render as exactly 12 lowercase hex characters in normal RX logs, unknown-packet warnings, and `PacketEvent.target`.
    - Tagged and untagged all-zero targets render as `broadcast` in all three surfaces.
    - A short datagram increments `packets_received` and `error_count` once, changes no by-type counter, and emits no activity event.
    - A complete but header-unparsable datagram increments `packets_received` and `error_count` once, changes no by-type counter, and emits no activity event.
    - An eligible known packet with an unparsable payload increments `packets_received` and its by-type counter once, preserves the existing `error_count` value, and emits no activity event.
  </behavior>
  <action>
Add `_format_target(header: LifxHeader) -> str`, using the explicit tagged-or-all-zero broadcast predicate and otherwise `header.target[:6].hex()`. Use it for the unknown-packet warning, normal RX debug log, and `PacketEvent.target`; preserve packet byte handling. Addresses review concern: the operator-visible unknown-packet target intentionally changes from padded 16-hex text to the same exact 12-hex/broadcast contract as routing and activity.

Turn `test_handle_packet_too_short` into an assertion-bearing regression and add header-unpack and payload-unpack cases that pin the counter semantics listed in `<behavior>`. These semantics preserve the current malformed-input contract while giving Plan 03 an explicit rule: valid transport-ineligible Thread packets must remain at zero effects, but structurally malformed ingress is still counted and errored as stated. Use actual caplog and `ActivityLogger` observations rather than only unit-testing the helper. Addresses review concern: short and unparsable packet statistics were previously unstated and the existing short-packet test asserted nothing.
  </action>
  <verify>
    <automated>uv run pytest packages/lifx-emulator-core/tests/test_server.py -q --no-cov -k "too_short or unparsable or unknown or target or activity"</automated>
    <fails_when>non-zero exit, zero tests selected, a counter differs from the pinned semantics, unknown and normal target text diverge, a serial loses trailing zeroes, or malformed input emits activity</fails_when>
  </verify>
  <acceptance_criteria>
    - Named tests assert exact target values `d073d5000010`, `d073d5000100`, and `d073d5000000`, plus `broadcast` for tagged and all-zero targets.
    - Unknown-packet warnings, normal RX logs, and `PacketEvent.target` use the same formatter and expected text.
    - Short/header-unparsable paths assert one receive plus one error and no type/activity; payload-unparsable asserts one receive/type, unchanged error count, and no activity.
    - No IPv6 socket, family routing parameter, or endpoint property is added in this task.
  </acceptance_criteria>
  <done>Exact target rendering is consistent across operator surfaces and malformed-datagram counters are explicitly protected for the routing reorder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UDP socket → protocol callback | Untrusted datagrams can schedule work at packet rate. |
| Scheduled coroutine → owner lifecycle | Work must remain retained, observable, and bounded through shutdown. |
| Header bytes → activity/log surface | Untrusted target bytes become operator-visible text only after complete-header validation. |

## STRIDE Threat Register (ASVS 4.0.3 Level 1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Denial of service | `LifxProtocol.datagram_received` / `BackgroundTaskTracker` | high | mitigate | Strong owner-local retention, explicit admission, five-second grace, cancel/await remainder, and forced-GC tests. |
| T-02-02 | Tampering | target rendering in `handle_packet` | medium | mitigate | Validate a complete header, render exactly `target[:6].hex()`, and handle tagged/all-zero targets explicitly. |
| T-02-03 | Repudiation | background task failures | medium | mitigate | Retrieve every non-cancelled outcome and log owner plus stable operation label exactly once. |
| T-02-04 | Denial of service | tracker shutdown | high | mitigate | Stop admission, bound the grace period, cancel/await the remainder, assert empty state, and permit explicit safe reopen only after drain. |
| T-02-SC | Tampering | package installation | low | accept | No package-manager install occurs in this plan; only stdlib primitives are added. |
</threat_model>

<verification>
  <automated>uv run pytest packages/lifx-emulator-core/tests/test_background_tasks.py packages/lifx-emulator-core/tests/test_server.py packages/lifx-emulator-core/tests/test_device.py packages/lifx-emulator-core/tests/test_async_storage.py packages/lifx-emulator-core/tests/test_thread_identity.py -q --no-cov</automated>
  <fails_when>non-zero exit, zero tests collected, a RuntimeWarning/unhandled task exception, or a failed retention, target, counter, persistence, or reuse assertion</fails_when>
</verification>

<success_criteria>
- The phase begins with a production tracer on the existing IPv4 path and no second protocol exists before HYG-01/HYG-02 core work lands.
- Server and device task owners retain work, consume outcomes, close refused coroutines, and drain without duplicate execution.
- Activity and unknown-packet targets preserve all 12 serial hex digits and distinguish both broadcast forms.
- Malformed-input counter semantics are explicit before the family-routing effect reorder.
</success_criteria>

## Artifacts this plan produces

- `packages/lifx-emulator-core/src/lifx_emulator/background_tasks.py` with `BackgroundTaskTracker` and its complete lifecycle contract.
- Server-owned packet scheduling and `_format_target()` in `packages/lifx-emulator-core/src/lifx_emulator/server.py`.
- Device-owned persistence scheduling in `packages/lifx-emulator-core/src/lifx_emulator/devices/device.py`.
- `packages/lifx-emulator-core/tests/test_background_tasks.py` plus focused server/device regressions; no future IPv6 endpoint or app-layer symbols are claimed here.

## Review Dispositions Ledger

### Round 1 — 60efff5

### Review Feedback Addressed
| Concern | Severity | How Addressed |
|---------|----------|---------------|
| Plan 02-01 Task 1 was oversized. | MEDIUM | Plan 01 now has three bounded tasks: tracker/server tracer, persistence expansion, and target/statistics regression. |
| Short and unparsable packet statistics were unpinned. | HIGH | Task 3 defines and tests receive, error, by-type, and activity effects for short, header-unparsable, and payload-unparsable input. |
| Unknown-packet target logging changed silently. | MEDIUM | Task 3 explicitly adopts the 12-hex/broadcast formatter for the warning and adds named caplog coverage. |
| `start()`/`stop()` would be authored in both Plan 01 and Plan 04. | LOW | Plan 01 does not edit transport lifecycle; Plan 04 exclusively owns D-12/D-16 server shutdown wiring. |
| Tracker reuse after shutdown was unspecified. | LOW | Task 1 defines explicit reopen of an empty completed tracker and Task 2 tests a second lifecycle. |
| Direct protocol construction would fail if tracker admission started closed. | MEDIUM | The server tracker defaults open and the direct `test_protocol_datagram_received` path is part of the tracer gate. |
| Cancelled task exception retrieval could raise in the done callback. | LOW | Task 1 requires `task.cancelled()` before outcome retrieval and final removal in `finally`. |
| A task-creation failure could leak the supplied coroutine. | LOW | Task 1 closes the coroutine, logs, and re-raises the original creation failure; Task 2 tests it. |

### Review Feedback Deferred
| Concern | Reason |
|---------|--------|
| None | All current Round 1 findings affecting Plan 01 are executable in its tasks and gates. |

<output>
Create `.planning/phases/02-ipv6-transport-and-thread-isolation/02-01-SUMMARY.md` when done.
</output>
