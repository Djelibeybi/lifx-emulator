---
phase: 02-ipv6-transport-and-thread-isolation
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - packages/lifx-emulator-core/src/lifx_emulator/server.py
  - packages/lifx-emulator-core/tests/conftest.py
  - packages/lifx-emulator-core/tests/test_server.py
  - packages/lifx-emulator-core/tests/test_ipv6_transport.py
autonomous: true
requirements:
  - NET-01
  - NET-03
  - NET-04
  - NET-05
  - HYG-01
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Idempotency / repetition — NET-01: a shared ephemeral port, no duplicate start and repeat-safe stop remain true across lifecycle cycles."
    - "Concurrency / effect ordering — NET-01: dual bind is atomic and cleans up a partial failure."
    - "Concurrency / effect ordering — NET-05: effective endpoints are published only for a complete live bind."
    - "A stock server defaults its configurable IPv6 bind address to `::1`, honours an alternate caller-supplied IPv6 address, and enables packet admission only after the IPv4-first same-port pair is live."
    - "After a port-zero bind commits, existing devices and devices added while running advertise the same non-zero effective service port returned by both endpoint properties."
    - "An IPv6-unavailable host receives an explicit atomic startup failure, never an implicit IPv4-only server."
    - "Shutdown stops admission, drains tracked work while transports remain usable, closes both families, observes bounded closure and clears all public endpoint state."
  artifacts:
    - path: packages/lifx-emulator-core/src/lifx_emulator/server.py
      provides: "Backwards-compatible configurable IPv6 binding, atomic dual-family lifecycle, effective service-port propagation, public endpoint properties and bounded endpoint closure"
    - path: packages/lifx-emulator-core/tests/test_server.py
      provides: "Unit-level default/alternate IPv6 address, bind, rollback, effective StateService port, idempotency, retry, shutdown and subclass-compatibility coverage"
    - path: packages/lifx-emulator-core/tests/test_ipv6_transport.py
      provides: "Real IPv4/IPv6 loopback proof of same-port reply affinity and Thread isolation"
    - path: packages/lifx-emulator-core/tests/conftest.py
      provides: "Collision-resistant dual-family test port allocation"
  key_links:
    - from: packages/lifx-emulator-core/src/lifx_emulator/server.py
      to: "asyncio IPv4 and IPv6 datagram endpoints"
      via: "start binds IPv4 first and passes its effective port into the IPv6 bind"
      pattern: "create_datagram_endpoint"
    - from: "EmulatedLifxServer.ipv6_bind_address"
      to: "the explicit AF_INET6 socket bind"
      via: "the backwards-compatible constructor input defaults to `::1` and is used verbatim for each IPv6 bind attempt"
      pattern: "ipv6_bind_address"
    - from: packages/lifx-emulator-core/src/lifx_emulator/server.py
      to: "LifxProtocol task admission"
      via: "both protocols remain non-accepting until the endpoint pair is committed"
      pattern: "start_accepting"
    - from: packages/lifx-emulator-core/src/lifx_emulator/server.py
      to: "ipv4_endpoint and ipv6_endpoint"
      via: "properties expose live socket names only after complete startup and return None after rollback/stop"
      pattern: "ipv[46]_endpoint"
    - from: "the committed effective UDP port"
      to: "existing device state and add_device()"
      via: "startup updates registered devices before admission, and live additions select `_effective_port` instead of the requested port"
      pattern: "_effective_port"
---

<objective>
Deliver an atomic, inspectable and repeat-safe dual-family UDP lifecycle with real loopback proof.

Purpose: The stock emulator must listen on caller-configurable IPv6 alongside IPv4 on one effective port, advertise that committed port to devices, route replies through the receiving endpoint, and shut down without leaked work or sockets.
Output: Configurable dual-family startup/rollback/shutdown, effective device service-port propagation, public effective endpoints, collision-safe test helpers and end-to-end IPv4/IPv6 transport coverage.
</objective>

<execution_context>
@/Users/amiller/.codex/gsd-core/workflows/execute-plan.md
@/Users/amiller/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-CONTEXT.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-RESEARCH.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-PATTERNS.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-SPEC.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-02-SUMMARY.md
@.planning/phases/02-ipv6-transport-and-thread-isolation/02-03-SUMMARY.md
@packages/lifx-emulator-core/src/lifx_emulator/server.py
@packages/lifx-emulator-core/tests/conftest.py
@packages/lifx-emulator-core/tests/test_server.py
</context>

<interfaces>
Preserve and extend `LifxServer` with the locked lifecycle surface:

- `server.transport`: IPv4 transport compatibility alias (D-01)
- append keyword-only `ipv6_bind_address: str = "::1"` after the existing constructor parameters so every established positional call remains unchanged
- `server.ipv4_endpoint -> tuple[str, int] | None` (D-02)
- `server.ipv6_endpoint -> tuple[str, int] | None` (D-02)
- raw IPv6 transport remains private (D-04)
- `server.port` remains the configured/requested value; private `_effective_port` is populated only for a committed live pair and supplies device service-port state while running
- `LifxProtocol(server, family=socket.AF_INET, accepting=True)` remains viable for direct construction; production startup creates both protocols non-accepting and activates them only after commit
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind and publish one atomic dual-family endpoint pair</name>
  <files>packages/lifx-emulator-core/src/lifx_emulator/server.py, packages/lifx-emulator-core/tests/test_server.py, packages/lifx-emulator-core/tests/conftest.py</files>
  <read_first>
    - packages/lifx-emulator-core/src/lifx_emulator/server.py
    - packages/lifx-emulator-core/tests/test_server.py
    - packages/lifx-emulator-core/tests/conftest.py
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-CONTEXT.md
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-RESEARCH.md
  </read_first>
  <behavior>
    - Construction without the new option stores `ipv6_bind_address == "::1"`; an alternate caller-supplied IPv6 address is retained and used for every IPv6 socket bind attempt without changing any existing positional argument.
    - A normal start binds IPv4 first, then IPv6 at the configured IPv6 bind address on the IPv4 socket's effective port, with `IPV6_V6ONLY=1` set before the IPv6 bind.
    - `port=0` publishes a complete IPv4/IPv6 pair with the same non-zero effective port; an IPv6 `EADDRINUSE` race causes a bounded whole-pair retry with a new IPv4 ephemeral port.
    - A successful port-zero commit updates every registered device to the non-zero effective port before admission; `add_device()` while running applies that same port, and GetService/StateService replies for both initial and newly added devices advertise it.
    - No endpoint or packet admission becomes public until both binds succeed.
    - IPv6 construction/bind failure, including an IPv6-disabled host, closes the partial IPv4 endpoint, resets all live state, logs an explicit IPv6 startup diagnostic and re-raises the triggering exception unchanged.
    - Calling `start()` on a complete live pair is a no-op; partial/stale state is not mistaken for a running server.
  </behavior>
  <action>
Implement D-01, D-02, D-04, D-09, D-10 and D-11 as one production-quality startup slice. Append a keyword-only `ipv6_bind_address: str = "::1"` parameter after the existing `EmulatedLifxServer` constructor parameters, store it on the server and preserve every existing positional argument and IPv4 `bind_address` semantic. Bind IPv4 first. Read its actual `sockname` port and construct a separate `AF_INET6` datagram socket, set `IPV6_V6ONLY=1` before bind, then bind it to `self.ipv6_bind_address` and the same effective port. Do not add CLI/YAML wiring, which remains Phase 4 scope. Keep the raw IPv6 transport private; maintain `server.transport` strictly as the committed IPv4 alias and expose live `ipv4_endpoint`/`ipv6_endpoint` properties. Construct production protocols with admission disabled and activate both only after transports, protocols, endpoint state and device service-port state are committed as a complete pair.

For requested `port=0`, handle the cross-family time-of-check/time-of-use race with a named bounded attempt count of five. Retry only when the IPv6 bind fails with `errno.EADDRINUSE`: close and observe closure of the partial pair, discard the provisional port/state, then begin again with a fresh IPv4 port-zero bind. Do not retry fixed ports or any other exception. If all attempts collide, re-raise the final bind exception unchanged. All other IPv6 construction/bind failures—including an IPv6-disabled host or invalid caller-supplied address—must follow D-10: emit one ERROR diagnostic that identifies the IPv6 bind address and cause, close all partial resources, leave both public endpoints `None`, keep admission closed and re-raise the original exception unchanged. There is no IPv4-only fallback.

Keep `server.port` as the requested/configured value for backwards compatibility. Maintain a private `_effective_port` that is `None` until a complete bind pair is ready. At the atomic commit boundary, set `_effective_port` to the IPv4 socket's actual port and synchronously update every device already registered with the manager before opening either protocol's admission. Make `add_device()` choose `_effective_port` while a complete pair is live and otherwise retain the historical requested `server.port`; clear `_effective_port` on rollback and completed stop. Add port-zero regressions that decode `Device.StateService` for an initial device and a device added after startup, asserting both advertise the shared non-zero endpoint port rather than the requested zero. Preserve any explicit per-device advertised-service override contract outside the default UDP service derived from `device.state.port`.

Treat only a complete live pair as “already running” for D-11. A repeated start then logs DEBUG and returns without rebinding; any detected partial legacy/stale state is closed before a fresh atomic attempt. Update both `test_server.py` and the core suite's `conftest.py` helper so tests either request port zero directly or verify candidate availability on both IPv4 and IPv6, avoiding IPv4-only free-port probes.

Add constructor and fake-socket bind-spy regressions proving omission uses `::1` and a distinct configured address such as the documentation-only `2001:db8::42` reaches the IPv6 bind unchanged without requiring the host to own that address. Keep real-loopback endpoint assertions on the safe default and derive their public value from the socket's effective `sockname`.

Addresses review concern: make NET-01's core-library IPv6 address configurable with a `::1` default; propagate the committed port-zero value into existing and live-added device service state; define IPv6-disabled-host behaviour; make port-zero cross-family collision handling bounded and testable; tighten idempotent start to the complete-pair invariant; replace IPv4-only test-port allocation.
  </action>
  <acceptance_criteria>
    - Existing constructor calls remain valid, omission binds IPv6 to `::1`, and a caller-supplied IPv6 address is the address passed into each IPv6 bind attempt.
    - Successful startup creates distinct IPv4 and IPv6 transports on the same effective port and publishes both endpoint properties atomically.
    - The IPv6 socket is configured IPv6-only before bind and the public surface does not expose the raw IPv6 transport.
    - A port-zero commit leaves `server.port == 0` but makes initial and live-added devices return the shared non-zero effective port in decoded StateService replies before traffic admission.
    - Port-zero `EADDRINUSE` races retry the entire pair at most five times; fixed-port and non-collision failures never retry.
    - Any failed/cancelled startup closes partial resources, leaves admission stopped and endpoint properties `None`, and preserves the triggering exception.
    - Repeated start is a no-op only for a complete live pair, and dual-family-aware test helpers do not reserve an IPv4-only candidate.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/lifx-emulator-core/tests/test_server.py -q --no-cov -k "start or bind or endpoint or ipv6_address or port_zero or service_port or add_device or collision or ipv6_unavailable"</automated>
  </verify>
  <fails_when>
    - The constructor shifts an existing positional argument, omits the `::1` default, ignores a supplied IPv6 address, starts as dual-stack, uses a different port, or exposes a public raw transport.
    - A partial bind publishes endpoint state/admission, leaves IPv4 alive after failure, changes the exception, or silently falls back to IPv4-only operation.
    - A successful port-zero start mutates `server.port`, leaves an initial/live-added device at port zero, opens admission before propagation, or returns a StateService port different from the shared effective endpoint port.
    - Port-zero collisions retry without a bound, reuse the failed provisional IPv4 endpoint, or retry unrelated errors.
    - A lone legacy transport makes `start()` return as if the complete pair were live.
  </fails_when>
  <done>A stock start honours the configurable IPv6 address with a `::1` default, atomically publishes an IPv4/IPv6-only same-port pair, propagates a port-zero allocation into live device service replies, boundedly recovers from collisions, and fails cleanly when IPv6 is unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drain work and close both endpoints with bounded lifecycle completion</name>
  <files>packages/lifx-emulator-core/src/lifx_emulator/server.py, packages/lifx-emulator-core/tests/test_server.py</files>
  <read_first>
    - packages/lifx-emulator-core/src/lifx_emulator/server.py
    - packages/lifx-emulator-core/tests/test_server.py
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-01-SUMMARY.md
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-CONTEXT.md
  </read_first>
  <behavior>
    - Stop disables both protocols before draining the server tracker for up to five seconds, so accepted work can still reply while both transports remain usable.
    - After draining/cancelling tracked work, stop closes both transports, waits for both protocol closure signals with a finite bound and clears transport/protocol/endpoint state.
    - A transport whose mock or broken implementation never reports closure cannot hang shutdown; the timeout is diagnosed and cleanup completes.
    - Repeated stop is safe, and a sibling subclass that initialises only the historical IPv4 `transport` can inherit stop without missing-attribute failure.
    - Completed and cancelled task exceptions are retrieved, and tracking is empty when stop returns.
  </behavior>
  <action>
Complete D-12 and D-16 around the tracker from Plan 02-01. Stop admission on every existing protocol first, wait up to five seconds for the server-owned tracker, cancel and await any remainder, and only then close endpoints so in-flight handlers retain usable reply transports. Have each real protocol expose a closure signal completed by `connection_lost`; close both transports and await those signals. Bound each closure wait to one second so a mock or broken transport cannot hang shutdown forever; log a WARNING naming the endpoint on timeout and continue clearing local state. The timeout is exceptional containment, not permission to skip waiting for functioning real transports.

Make teardown defensive for inherited sibling implementations: use safe attribute access for Phase 2's new IPv4/IPv6 protocol/transport fields, close the historical `transport` when that is the only initialised endpoint, avoid closing the same object twice, and always finish with the tracker empty plus `transport`, private endpoint fields and public endpoint properties cleared. Keep repeated `stop()` safe.

Add focused ordering, forced-timeout, repeated-stop and cancellation tests. Define a local legacy-style subclass in `test_server.py` whose overridden `start()` sets only `transport`, then prove inherited `stop()` succeeds and closes it. Do not edit a sibling package to manufacture compatibility.

Addresses review concern: bound `wait_closed`; make inherited teardown safe for sibling subclasses that initialise only historical state; prove cancellation exception retrieval and complete tracker drain through the final lifecycle.
  </action>
  <acceptance_criteria>
    - New datagrams are rejected before the five-second tracked-work drain begins, and transports remain usable until drain/cancellation finishes.
    - Both functioning real endpoint closure signals are awaited; a missing signal times out in one second with a named warning instead of hanging.
    - Stop returns with no tracked tasks and all transport, protocol and endpoint state cleared, including after cancellation or repeated calls.
    - The local legacy-style subclass test closes its sole historical transport without attribute errors or duplicate close calls.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/lifx-emulator-core/tests/test_server.py -q --no-cov -k "stop or shutdown or close or legacy or cancellation"</automated>
  </verify>
  <fails_when>
    - Either transport closes before tracked handlers drain/cancel, or new datagrams remain admissible during shutdown.
    - Shutdown can wait forever for `connection_lost`, returns with tasks/endpoints retained, or swallows cancellation without cleanup.
    - Repeated stop raises, a legacy-style subclass hits a missing new attribute, or the same transport is closed twice.
  </fails_when>
  <done>Shutdown ordering satisfies D-12/D-16, closure observation is finite, task tracking drains completely and inherited legacy teardown remains safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove dual-family reply affinity and Thread isolation on real loopback sockets</name>
  <files>packages/lifx-emulator-core/tests/test_ipv6_transport.py</files>
  <read_first>
    - packages/lifx-emulator-core/tests/test_ipv6_transport.py
    - packages/lifx-emulator-core/tests/conftest.py
    - packages/lifx-emulator-core/tests/test_server.py
    - packages/lifx-emulator-core/src/lifx_emulator/server.py
    - .planning/phases/02-ipv6-transport-and-thread-isolation/02-SPEC.md
  </read_first>
  <behavior>
    - A real stock server started with port zero exposes live IPv4 and IPv6 endpoints sharing one non-zero port.
    - Repeated and interleaved WiFi discovery/control traffic on IPv4 and IPv6 receives replies only on the originating family.
    - A Thread device responds to exact untagged IPv6 unicast and remains externally silent for IPv4, broadcast, zero, tagged and mismatched traffic.
    - Stopping the server makes both endpoints unreachable and resets both public endpoint properties to `None`.
    - A white-box socket assertion proves `IPV6_V6ONLY == 1` without adding a public raw-transport API forbidden by D-04.
  </behavior>
  <action>
Create `test_ipv6_transport.py` with real asyncio UDP clients bound to `127.0.0.1` and `::1`. Start the stock server on port zero, assert the two public endpoints share the same effective port, and exercise repeated plus concurrently interleaved WiFi requests on both families. Assert replies arrive only at the client that sent each request. Exercise the complete Thread negative matrix and exact IPv6 unicast positive path, asserting rejected traffic yields no response and no observable device/event side effects.

Use a narrowly documented white-box test to inspect the internal IPv6 transport socket and assert `IPV6_V6ONLY == 1`; do not turn that object into public API because D-04 locks `ipv6_endpoint` as the supported inspection surface. Skip real IPv6-loopback cases only when the host demonstrably lacks `::1`; the mocked IPv6-unavailable startup test from Task 1 remains mandatory and unskipped. Put every client/server socket and task cleanup in `try/finally`, call server stop, close clients, await their closure signals and assert both endpoint properties are `None`.

Run the full suite exactly once with coverage after the focused tests, followed by lint, format, type checking and pre-commit. Do not add a second no-coverage full-suite invocation.

Addresses review concern: make all test sockets close in `finally`; avoid duplicate full-suite runs; document why the `IPV6_V6ONLY` proof is intentionally white-box under D-04.
  </action>
  <acceptance_criteria>
    - Real loopback traffic proves same-port dual bind and per-datagram reply affinity under repetition and interleaving.
    - Thread's sole response path is exact untagged IPv6 unicast; every rejected variant is silent and side-effect-free.
    - The IPv6 socket is asserted V6-only without introducing a public raw transport property.
    - Every test-owned client/server resource closes in `finally`, and stop clears both public endpoint properties.
    - The full suite is invoked once, with the repository's 80% coverage floor, and all static/pre-commit gates pass.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/lifx-emulator-core/tests/test_ipv6_transport.py -q --no-cov</automated>
  </verify>
  <fails_when>
    - IPv4 and IPv6 endpoints differ in effective port, a reply crosses client families, or Thread reacts to any rejected packet class.
    - IPv6 absence is hidden by an unconditional skip, or V6-only verification expands the public server API.
    - A test path leaks sockets/tasks because cleanup is outside `finally`.
    - Verification prescribes duplicate full-suite pytest runs.
  </fails_when>
  <done>Real sockets prove the phase goal end to end, resources close on every path, and the final quality gate uses one coverage-enabled full-suite run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host network stack → bind lifecycle | OS socket allocation, cancellation and family availability determine whether the server may become live. |
| UDP clients → dual-family listeners | Untrusted datagrams arrive independently on IPv4 and IPv6 endpoints. |
| Shutdown coordinator → tracked handlers/transports | Lifecycle ordering controls whether accepted work can finish and whether resources leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | Spoofing | Thread target handling | high | mitigate | Real loopback tests prove only exact untagged IPv6 unicast reaches and changes a Thread device. |
| T-02-04-02 | Tampering | IPv6 socket configuration | high | mitigate | Set `IPV6_V6ONLY=1` before bind and assert the kernel socket option in a white-box test. |
| T-02-04-03 | Denial of Service | Atomic startup/retry | medium | mitigate | Roll back every partial bind, retry only port-zero IPv6 collisions and cap attempts at five. |
| T-02-04-04 | Denial of Service | Shutdown closure waits | medium | mitigate | Stop admission, bound tracker and endpoint closure waits, cancel remaining work and clear state deterministically. |
| T-02-04-05 | Information Disclosure | Configurable network exposure | medium | mitigate | Keep both defaults loopback-only (`127.0.0.1` and `::1`), require an explicit core-library argument for any alternate IPv6 address, and bind exactly the caller-selected address. |
| T-02-04-SC | Tampering | Package installation | low | accept | This plan installs no dependency; existing uv lock and project tooling remain unchanged. |
</threat_model>

<verification>
- `uv run pytest --cov-fail-under=80 -q`
- `uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run pyright`
- `prek run --all-files`
- Confirm the full pytest suite appears only once in this plan's verification path; task-level commands remain focused subsets.
</verification>

<success_criteria>
- A stock server defaults IPv6 to `::1`, honours a caller-supplied alternate IPv6 bind address and exposes live IPv4/IPv6 endpoints on one effective port, with the IPv6 socket V6-only.
- A port-zero server preserves its requested `server.port` value while initial and live-added devices advertise the committed non-zero effective port through StateService.
- Startup is atomic under bind errors/cancellation and bounded under port-zero cross-family collisions.
- Thread and WiFi behaviour matches NET-03/NET-04 on real sockets with reply-family affinity.
- Stop drains work, observes both endpoint closures within finite bounds and leaves no public or private live endpoint state.
- The repository passes its full coverage, lint, formatting, type and pre-commit gates.
</success_criteria>

## Review Dispositions Ledger

### Round 1 — 60efff5

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| NET-01's configurable core-library IPv6 bind address was missing. | High | Task 1 appends keyword-only `ipv6_bind_address="::1"`, uses it for every IPv6 bind attempt and verifies both the default and an alternate address without adding Phase 4 CLI/YAML wiring. |
| Port-zero endpoint publication did not propagate the effective port into device-facing service state. | High | Task 1 commits `_effective_port` into every registered device before admission, makes live `add_device()` use it, and decodes StateService replies for both initial and newly added devices. |
| IPv6-disabled-host behaviour was unspecified. | High | Task 1 requires an explicit IPv6 bind ERROR, atomic rollback, unchanged exception propagation and no IPv4-only fallback; a mandatory mocked regression covers it. |
| Port-zero IPv4-first binding can collide before the IPv6 bind. | High | Task 1 retries the whole pair only for IPv6 `EADDRINUSE`, with fresh IPv4 ephemeral allocation and a five-attempt bound. |
| Inherited `stop()` could break sibling subclasses that initialise only `transport`. | High | Task 2 uses defensive state access and includes a local legacy-style subclass regression without editing the sibling package. |
| Endpoint closure waiting could hang forever on a mock or broken transport. | High | Task 2 adds a one-second bound per protocol closure signal, named diagnostics and deterministic state clearing. |
| Idempotent `start()` could treat partial state as already running. | Medium | Task 1 defines the no-op invariant as a complete live endpoint pair and cleans stale partial state before restart. |
| Real-socket tests could leak clients on an assertion or cancellation path. | Medium | Task 3 requires all server/client cleanup in `try/finally` and awaits closure before asserting cleared endpoints. |
| The plan prescribed duplicate full-suite test runs. | Medium | Task 3 and overall verification prescribe one coverage-enabled full-suite invocation; task verifies are focused subsets only. |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| Fall back to an IPv4-only server when the host has no IPv6 support. | Rejected: D-10 locks atomic failure and explicitly forbids fallback; default-on IPv6 is part of the phase goal. The plan instead makes the failure diagnostic and rollback deterministic. |
| Prove `IPV6_V6ONLY` through a supported public raw-socket accessor. | Rejected: D-04 explicitly keeps the raw IPv6 transport private. Task 3 uses the narrow white-box assertion for kernel state while public callers verify only `ipv6_endpoint`. |

## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | The stock server listens on IPv6 alongside IPv4. | Tasks 1 and 3 implement and prove the real same-port dual-family server. |
| GOAL | Thread answers only IPv6 unicast addressed to its serial. | Task 3 proves the positive path and complete negative matrix over real sockets. |
| REQ | NET-01 — Native dual-family server. | Task 1 owns the backwards-compatible configurable `::1` default, alternate-address propagation, bind order, shared effective port, V6-only configuration and atomicity. |
| REQ | NET-03 — Side-effect-free Thread isolation. | Task 3 validates externally silent rejected traffic and exact IPv6 unicast. |
| REQ | NET-04 — WiFi dual-family compatibility. | Task 3 validates repeated/interleaved traffic and originating-family replies. |
| REQ | NET-05 — Effective endpoint exposure. | Tasks 1–3 publish only a live complete pair and clear both properties on stop/rollback. |
| REQ | HYG-01 — Strongly tracked asynchronous work. | Task 2 integrates tracker admission/drain into dual-family shutdown. |
| RESEARCH | IPv4-first same-port bind with a configurable preconfigured IPv6 socket. | Task 1 follows the recommended socket lifecycle, binds the constructor-selected address and makes port-zero collision handling explicit. |
| RESEARCH | Port-zero service state uses the committed effective port. | Task 1 updates existing devices at commit, uses `_effective_port` for live additions and proves decoded StateService replies never advertise zero. |
| RESEARCH | Atomic rollback and connection-lost-aware shutdown. | Tasks 1–2 implement rollback, ordered drain and bounded closure observation. |
| CONTEXT | D-01, D-02, D-04. | Task 1 preserves the IPv4 alias, adds endpoint properties and keeps the IPv6 transport private. |
| CONTEXT | D-03. | Task 3 proves protocol-owned reply affinity delivered by Plan 02-03. |
| CONTEXT | D-09, D-10, D-11. | Task 1 implements bind order, atomic failure and complete-pair idempotency. |
| CONTEXT | D-12, D-16. | Task 2 implements admission stop, bounded drain/cancel, transport closure and empty final state. |
| SPEC EDGE | NET-01 idempotency and concurrency/effect ordering. | Tasks 1–2 test repeat-safe lifecycle and atomic partial-failure cleanup. |
| SPEC EDGE | NET-05 concurrency/effect ordering. | Tasks 1–3 publish endpoints only for a complete live pair and assert cleared state after stop. |
| SPEC PROHIBITION | None. | No bespoke prohibition applies; functional negatives are executable in `fails_when` and real-socket tests. |
</multi-source-coverage-audit>

<output>
Create `.planning/phases/02-ipv6-transport-and-thread-isolation/02-04-SUMMARY.md` when done.
</output>
