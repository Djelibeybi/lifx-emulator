---
phase: 04-cli-and-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/lifx-emulator/tests/test_run_equivalence.py
  - packages/lifx-emulator/tests/test_startup.py
  - packages/lifx-emulator/src/lifx_emulator_app/__main__.py
  - packages/lifx-emulator/src/lifx_emulator_app/startup/__init__.py
  - packages/lifx-emulator/src/lifx_emulator_app/startup/settings.py
  - packages/lifx-emulator/src/lifx_emulator_app/startup/devices.py
  - packages/lifx-emulator/src/lifx_emulator_app/startup/storage.py
  - packages/lifx-emulator/src/lifx_emulator_app/startup/lifecycle.py
autonomous: true
requirements:
  - HYG-03

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A golden snapshot of (serial, product, firmware, connectivity, order) for a matrix of legacy CLI-flag and YAML combinations is committed as inline test constants before run() or any startup helper is touched (D-13)."
    - "run() in __main__.py keeps the cyclopts command definition and delegates to named device-construction, storage, server-start and shutdown helpers; run() and every new helper have McCabe complexity at most 10 and pass PLR0912/PLR0915, and every new helper passes PLR0913 (R1, D-10)."
    - "_load_merged_config() keeps returning the merged dict (tests call and patch it); run() converts that dict into a frozen RunSettings object carrying resolved values, config-file path and the set of option names supplied on the command line, and every helper receives RunSettings instead of loose locals (D-11)."
    - "Every name that test_cli.py patches in the lifx_emulator_app.__main__ namespace (EmulatedLifxServer, DevicePersistenceAsyncFile, _setup_logging, _load_merged_config, resolve_config_path, load_config) is still looked up in __main__ at call time, and every helper that logs uses the logger instance returned by _setup_logging, so the existing app suite passes unchanged."
    - "Return values are unchanged: config-load failure and the --persistent-scenarios check return False; a device-creation failure and the non-persistent no-devices error return None; a normal shutdown returns None."
    - "A run with no devices keeps its existing behaviour (SPEC empty/R1): with --persistent and empty storage it logs the 'No devices configured. Server will run with no devices.' warning and starts the server with an empty fleet; without --persistent it logs the existing 'No devices configured' error and returns without constructing a server."
    - statement: "Legacy equivalence: the refactored run() reproduces every golden snapshot tuple exactly and the existing test_cli.py, test_config.py and test_export_config.py suites pass without edits (SPEC adjacency/R1)."
      verification: backstop
    - statement: "Shutdown order: storage flush, then server.stop() (which includes mDNS), then API task cancellation, asserted by a test that passes against the unchanged run() and again after the refactor (SPEC ordering/R1)."
      verification: backstop
  artifacts:
    - path: "packages/lifx-emulator/tests/test_run_equivalence.py"
      provides: "Golden snapshot constants, empty-fleet behaviour and shutdown-order proof for run()"
      contains: "GOLDEN_"
    - path: "packages/lifx-emulator/src/lifx_emulator_app/startup/settings.py"
      provides: "Frozen RunSettings with provenance and from_config_dict()"
      contains: "class RunSettings"
    - path: "packages/lifx-emulator/src/lifx_emulator_app/startup/devices.py"
      provides: "SerialAllocator and build_devices() device construction"
      contains: "def build_devices"
    - path: "packages/lifx-emulator/src/lifx_emulator_app/startup/lifecycle.py"
      provides: "API task start, signal wiring, run banner and ordered shutdown"
      contains: "async def shutdown"
    - path: "packages/lifx-emulator/src/lifx_emulator_app/startup/storage.py"
      provides: "Persistence flag validation and deprecation warnings"
      contains: "def check_persistence_flags"
  key_links:
    - from: "packages/lifx-emulator/src/lifx_emulator_app/__main__.py"
      to: "packages/lifx-emulator/src/lifx_emulator_app/startup/settings.py"
      via: "run() calls _load_merged_config() then RunSettings.from_config_dict(cfg, cli_keys)"
      pattern: "RunSettings\\.from_config_dict"
    - from: "packages/lifx-emulator/src/lifx_emulator_app/__main__.py"
      to: "packages/lifx-emulator/src/lifx_emulator_app/startup/devices.py"
      via: "_run_emulator() calls build_devices(settings, allocator, storage, logger)"
      pattern: "build_devices\\("
    - from: "packages/lifx-emulator/src/lifx_emulator_app/__main__.py"
      to: "packages/lifx-emulator/src/lifx_emulator_app/startup/lifecycle.py"
      via: "_run_emulator() awaits serve_until_shutdown(), whose finally block calls shutdown()"
      pattern: "serve_until_shutdown\\("
  prohibitions:
    - statement: "MUST NOT change the devices (serials, products, firmware, WiFi connectivity, order) produced by a CLI or YAML setup that uses none of the new keys (P3, device half; the mDNS half is asserted in 04-04)."
      status: resolved
      verification: test
---

<objective>
Decompose the 567-line `run()` coroutine into named helpers with unchanged behaviour, as the first
plan of the phase and before any new flag exists (HYG-03, SPEC R1, D-10, D-11, D-13).

Purpose: Every later Phase 4 plan adds flags, settings fields and a preflight step. They need a
`run()` that is small enough to extend inside the complexity budget, with a golden record proving
the legacy fleet never moves.

Output: A committed golden snapshot and shutdown-order test, a `lifx_emulator_app/startup/` package
(settings, devices, storage, lifecycle), a thin `run()` plus a few private `__main__` helpers that
must stay patch-visible, and unit tests for the new helpers.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-and-configuration/04-SPEC.md
@.planning/phases/04-cli-and-configuration/04-CONTEXT.md
@.planning/phases/04-cli-and-configuration/04-RESEARCH.md
@.planning/phases/04-cli-and-configuration/04-PATTERNS.md
@packages/lifx-emulator/src/lifx_emulator_app/__main__.py
@packages/lifx-emulator/src/lifx_emulator_app/config.py
@packages/lifx-emulator/tests/test_cli.py

<interfaces>
Current `run()` (`__main__.py:579-1144`): `@app.default async def run(*, config, bind, port,
verbose, persistent, persistent_scenarios, api, api_host, api_port, api_activity, browser, product,
color, color_temperature, infrared, hev, multizone, tile, switch, multizone_zones,
multizone_extended, tile_count, tile_width, tile_height, serial_prefix, serial_start) -> bool | None`.
Every parameter is `Annotated[... | None, cyclopts.Parameter(group=...)] = None`.

`_load_merged_config(**cli_kwargs) -> dict | None` (`__main__.py:472`) pops `config_flag`, calls
`resolve_config_path()` and `load_config()`, then `merge_config()`; adds `devices`, `scenarios` and
`_config_path` keys. It prints errors and returns None on failure. Tests call it directly and assert
on the dict (`TestLoadMergedConfigErrors`, `TestLoadMergedConfigScenarios`); `TestRunWithConfigDevices`
and `TestBrowserFlag` patch it to return `_make_cfg(...)`, a dict holding only the legacy keys.

`merge_config()` legacy keys and defaults (`config.py:290-317`): bind 127.0.0.1, port 56700,
verbose False, persistent False, persistent_scenarios False, api False, api_host 127.0.0.1,
api_port 8080, api_activity True, browser False, products None, color/color_temperature/infrared/
hev/multizone/tile/switch 0, multizone_zones None, multizone_extended True, tile_count/tile_width/
tile_height None, serial_prefix d073d5, serial_start 1, devices None.

Server construction today (`__main__.py:1070-1080`), which tests read positionally
(`call_args[0][0]` devices, `[0][2]` bind, `[0][3]` port) and by keyword (`call_args[1]["scenario_manager"]`):
`EmulatedLifxServer(devices, device_manager, f_bind, f_port, track_activity=f_api_activity if f_api else False,
storage=storage, scenario_manager=scenario_manager, persist_scenarios=f_persistent_scenarios,
scenario_storage=scenario_storage)`.

Patch targets used by test_cli.py (name bindings, must resolve in `__main__` at call time):
`EmulatedLifxServer`, `DevicePersistenceAsyncFile`, `_setup_logging`, `_load_merged_config`,
`resolve_config_path`, `load_config`, `get_registry`. Module-attribute patches that act globally and
therefore survive a move: `lifx_emulator_app.__main__.logging.getLogger`,
`lifx_emulator_app.__main__.logging.basicConfig`, `lifx_emulator_app.__main__.webbrowser.open`,
`lifx_emulator_app.api.run_api_server` (imported locally inside the API branch).

Tests assert log text through `mock_logger.error/warning/info.call_args_list` on the logger that
`_setup_logging()` (or a patched `logging.getLogger`) returns: "Failed to create device",
"Failed to create device from config", "No devices configured", "--persistent is deprecated",
"--persistent-scenarios requires --persistent", "Loaded config from".

Existing shutdown (`__main__.py:1132-1144`): `logger.info("Shutting down server...")`, then
`await storage.shutdown()` if storage, then `await server.stop()`, then `api_task.cancel()` and
`await api_task` swallowing `asyncio.CancelledError`.

cyclopts 4.10.1 turns a `False` return from the async default command into exit code 1 and `None`
into exit code 0 (RESEARCH "cyclopts exit-code behaviour").
</interfaces>
</context>

## Artifacts this phase produces

- `packages/lifx-emulator/tests/test_run_equivalence.py` with module-level `GOLDEN_*` tuple constants, an empty-fleet test pair and a shutdown-order test.
- New package `packages/lifx-emulator/src/lifx_emulator_app/startup/` with barrel `__init__.py` and `__all__`.
- `startup/settings.py`: frozen dataclass `RunSettings` (legacy fields plus `device_definitions`, `scenario_config`, `config_path`, `cli_keys`), classmethod `RunSettings.from_config_dict(cfg, cli_keys)`, property `RunSettings.has_device_config`.
- `startup/devices.py`: class `SerialAllocator` (`SerialAllocator.for_settings()`, `next()`), `build_devices()`, `accept_empty_fleet()` and private definition-application helpers.
- `startup/storage.py`: `check_persistence_flags()`.
- `startup/lifecycle.py`: `start_api_task()`, `install_shutdown_signals()`, `log_running_banner()`, `shutdown()`, `serve_until_shutdown()`.
- `__main__.py` private helpers that stay patch-visible: `_run_emulator()`, `_log_devices()`, `_build_scenario_manager()`, `_create_server()`.
- `packages/lifx-emulator/tests/test_startup.py` unit tests for the startup package.
- No new CLI flags, YAML keys or dependencies.

<tasks>

<task type="tracer">
  <name>Task 1: Commit the golden run() snapshot and shutdown-order proof before touching run()</name>
  <files>packages/lifx-emulator/tests/test_run_equivalence.py</files>
  <read_first>
    - packages/lifx-emulator/src/lifx_emulator_app/__main__.py
    - packages/lifx-emulator/src/lifx_emulator_app/config.py
    - packages/lifx-emulator/tests/test_cli.py
    - .planning/phases/04-cli-and-configuration/04-CONTEXT.md
    - .planning/phases/04-cli-and-configuration/04-RESEARCH.md
  </read_first>
  <action>Per D-13, create `packages/lifx-emulator/tests/test_run_equivalence.py` and commit it on its own (`test(app-cli): capture golden run() device snapshot`) while `__main__.py` and every other file under `packages/lifx-emulator/src` are unchanged. The harness drives the real `run()` end to end: it patches `lifx_emulator_app.__main__.EmulatedLifxServer` with a MagicMock whose `start` is an AsyncMock that sets an `asyncio.Event` and whose `stop` is an AsyncMock, patches `lifx_emulator_app.__main__._setup_logging` to return a MagicMock logger, uses `monkeypatch.chdir(tmp_path)` and `monkeypatch.delenv("LIFX_EMULATOR_CONFIG", raising=False)` so real `resolve_config_path`/`load_config`/`merge_config` run, starts `run(...)` as a task, awaits the started event with a timeout instead of a fixed sleep, cancels the task and awaits it swallowing `asyncio.CancelledError`. A helper `_snapshot(devices)` returns `[(state.serial, state.product, (state.version_major, state.version_minor), str(state.connectivity)) for each device in list order]` taken from `mock_server_class.call_args[0][0]`. Cover this matrix, one parametrised case id each: `color` (color=1); `mixed-counts` (color=2, color_temperature=1, infrared=1, hev=1, multizone=1, tile=1, switch=1); `products-and-counts` (product=[27, 32, 55], color=1); `multizone-options` (multizone=2, multizone_zones=24, multizone_extended=False); `tile-options` (tile=1, tile_count=2, tile_width=16, tile_height=8); `serial-options` (serial_prefix="cafe00", serial_start=100, color=3); `yaml-definitions` (YAML file with color 2 and devices product 27 serial d073d5000002, product 32 zone_count 24, product 38 label Beam, passed via `config=`); `yaml-merge` (YAML products [91], switch 1, serial_start 5, plus CLI color=1); `yaml-empty-devices` (YAML devices [] and color 1); `restore` (persistent=True with `lifx_emulator_app.__main__.DevicePersistenceAsyncFile` patched to return a real `DevicePersistenceAsyncFile(tmp_path)` pre-populated through its public save and flush API with product 27 serial d073d5aa0001 and product 32 serial d073d5aa0002). Derive every expected tuple list by running the harness once against the unmodified `run()` and pasting the printed output verbatim as module-level `GOLDEN_<CASE>` constants; never hand-compute them. Add an empty-fleet pair (SPEC empty/R1): persistent=True with an empty tmp storage asserts the warning "No devices configured. Server will run with no devices." on the mock logger and that the server was constructed with an empty list and started; no flags at all asserts the "No devices configured" error, that the server class was never called and that `run()` returned None. Add `test_shutdown_order`: persistent=True, api=True, color=1, with the storage double's `shutdown`, the mock server's `stop` and a fake `lifx_emulator_app.api.run_api_server` coroutine (which waits forever and records its cancellation before re-raising) all appending to one shared list; after cancelling `run()` the list must equal `["storage.shutdown", "server.stop", "api.cancelled"]`, the existing order at `__main__.py:1135-1144` (not the superseded CONTEXT prose order).</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest packages/lifx-emulator/tests/test_run_equivalence.py -q --no-cov</automated>
    <fails_when>non-zero exit, "no tests ran" in the output, or fewer than 13 tests reported as passed (10 matrix cases, the empty-fleet pair and the shutdown-order test)</fails_when>
    <automated>git diff --quiet HEAD -- packages/lifx-emulator/src</automated>
    <fails_when>exit status 1, meaning a file under packages/lifx-emulator/src differs from HEAD while the golden snapshot is being captured</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '^GOLDEN_' packages/lifx-emulator/tests/test_run_equivalence.py` lists one constant for each of the 10 matrix case ids.
    - `git show --stat --format= HEAD` lists only `packages/lifx-emulator/tests/test_run_equivalence.py`.
    - The test file contains the literal `["storage.shutdown", "server.stop", "api.cancelled"]`.
    - The verify command passes against the unmodified `__main__.py`.
  </acceptance_criteria>
  <done>The golden constants, empty-fleet pair and shutdown-order proof are committed before any source change and pass against today's run().</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract RunSettings and device construction into the startup package</name>
  <files>packages/lifx-emulator/src/lifx_emulator_app/startup/__init__.py, packages/lifx-emulator/src/lifx_emulator_app/startup/settings.py, packages/lifx-emulator/src/lifx_emulator_app/startup/devices.py, packages/lifx-emulator/src/lifx_emulator_app/__main__.py, packages/lifx-emulator/tests/test_startup.py</files>
  <read_first>
    - packages/lifx-emulator/src/lifx_emulator_app/__main__.py
    - packages/lifx-emulator/src/lifx_emulator_app/config.py
    - packages/lifx-emulator/tests/test_run_equivalence.py
    - packages/lifx-emulator/tests/test_cli.py
    - packages/lifx-emulator-core/src/lifx_emulator/devices/__init__.py
    - packages/lifx-emulator-core/src/lifx_emulator/devices/observers.py
  </read_first>
  <behavior>
    - RunSettings.from_config_dict(_make_cfg-style dict) returns a frozen object whose fields equal the dict values; assigning a field raises dataclasses.FrozenInstanceError.
    - device_definitions keeps None and an empty list distinct, so has_device_config is True for YAML `devices: []` and False for an empty flag set.
    - SerialAllocator(prefix d073d5, start 1, reserved {d073d5000002}) yields d073d5000001 then d073d5000003, and reserved matching is case-insensitive.
    - build_devices() returns the same devices as the golden snapshot for every matrix case and returns None after logging "Failed to create device" for product 9999.
  </behavior>
  <action>Per D-10 and D-11, create `lifx_emulator_app/startup/` with a module docstring in each file and a barrel `__init__.py` whose `__all__` lists the public names. In `startup/settings.py` define `@dataclass(frozen=True) class RunSettings` with one field per legacy merged key (bind, port, verbose, persistent, persistent_scenarios, api, api_host, api_port, api_activity, browser, products, color, color_temperature, infrared, hev, multizone, tile, switch, multizone_zones, multizone_extended, tile_count, tile_width, tile_height, serial_prefix, serial_start) plus `device_definitions: tuple[DeviceDefinition, ...] | None`, `scenario_config: ScenariosConfig | None`, `config_path: str | None` and `cli_keys: frozenset[str]` (the option names supplied on the command line, the provenance later plans use to name `--flag` versus YAML key). Add `@classmethod from_config_dict(cls, cfg: Mapping[str, Any], cli_keys: frozenset[str] = frozenset()) -> RunSettings` that reads the legacy keys by subscript, `devices`/`scenarios`/`_config_path` with `.get()`, converts `products` and `devices` to tuples while preserving None, and a property `has_device_config` that reproduces today's `has_any_device_config` expression exactly. `_load_merged_config()` stays in `__main__.py`, keeps its dict return and is not edited. In `startup/devices.py` define `class SerialAllocator` (constructor `prefix, start, reserved: Iterable[str]`, method `next() -> str` reproducing the `get_serial()` closure exactly, classmethod `for_settings(settings)` collecting explicit definition serials lower-cased) and `build_devices(settings, allocator, storage, logger) -> list[EmulatedLifxDevice] | None`, which moves the restore-from-storage branch, the product loop, the seven count loops in their current order and the per-definition loop verbatim, including every log message text and argument form, the deterministic uuid5 namespace `a1b2c3d4-e5f6-7890-abcd-ef1234567890`, zone-colour padding/truncation and the list-products hint. Split the per-definition work into private helpers (for example `_apply_definition`, `_apply_membership`, `_apply_zone_colors`) so each stays within the budget. Add `accept_empty_fleet(settings, logger) -> bool` holding the no-devices warning/error branch unchanged. Every helper that logs takes the `logger` parameter; never create a module-level logger for these messages, because test_cli.py asserts on the logger returned by `_setup_logging`. In `__main__.py`, `run()` builds one dict of its keyword arguments, passes it to `_load_merged_config(**cli)`, returns False on None, builds `RunSettings.from_config_dict(cfg, cli_keys=frozenset(key for key, value in cli.items() if value is not None and key != "config_flag"))` and keeps the rest of today's flow calling `build_devices()`/`accept_empty_fleet()`; `DevicePersistenceAsyncFile()` is still constructed inside `__main__.py`. Write `packages/lifx-emulator/tests/test_startup.py` covering the behaviour bullets with direct calls and no patching.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest packages/lifx-emulator/tests/test_run_equivalence.py packages/lifx-emulator/tests/test_startup.py packages/lifx-emulator/tests/test_cli.py packages/lifx-emulator/tests/test_config.py packages/lifx-emulator/tests/test_export_config.py -q --no-cov</automated>
    <fails_when>non-zero exit, any "failed" or "error" count in the summary line, or "no tests ran"</fails_when>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run ruff check --select C901,PLR0912,PLR0913,PLR0915 packages/lifx-emulator/src/lifx_emulator_app/startup</automated>
    <fails_when>non-zero exit or any C901/PLR0912/PLR0913/PLR0915 line naming a startup module</fails_when>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run ruff check .</automated>
    <fails_when>non-zero exit or any reported lint finding</fails_when>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pyright</automated>
    <fails_when>non-zero exit or an error count other than "0 errors"</fails_when>
  </verify>
  <acceptance_criteria>
    - With `T1` set to the Task 1 commit hash, `git log --format=%h T1..HEAD -- packages/lifx-emulator/tests/test_run_equivalence.py packages/lifx-emulator/tests/test_cli.py packages/lifx-emulator/tests/test_config.py` prints nothing and `git status --porcelain` shows no change to those files.
    - `grep -n "class RunSettings" packages/lifx-emulator/src/lifx_emulator_app/startup/settings.py` finds the class and `grep -n "frozen=True" packages/lifx-emulator/src/lifx_emulator_app/startup/settings.py` finds its decorator.
    - `grep -n "a1b2c3d4-e5f6-7890-abcd-ef1234567890" packages/lifx-emulator/src/lifx_emulator_app/startup/devices.py` finds the namespace.
    - `grep -n "DevicePersistenceAsyncFile()" packages/lifx-emulator/src/lifx_emulator_app/__main__.py` still finds the constructor call.
    - `grep -rn "getLogger" packages/lifx-emulator/src/lifx_emulator_app/startup` prints nothing.
  </acceptance_criteria>
  <done>Settings and device construction live in the startup package, the golden snapshot and legacy suites pass unedited, and the new modules have no complexity or argument-count findings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract storage, server-start and shutdown helpers and enforce the budget on run()</name>
  <files>packages/lifx-emulator/src/lifx_emulator_app/startup/storage.py, packages/lifx-emulator/src/lifx_emulator_app/startup/lifecycle.py, packages/lifx-emulator/src/lifx_emulator_app/startup/__init__.py, packages/lifx-emulator/src/lifx_emulator_app/__main__.py, packages/lifx-emulator/tests/test_startup.py</files>
  <read_first>
    - packages/lifx-emulator/src/lifx_emulator_app/__main__.py
    - packages/lifx-emulator/src/lifx_emulator_app/startup/settings.py
    - packages/lifx-emulator/src/lifx_emulator_app/startup/devices.py
    - packages/lifx-emulator/tests/test_run_equivalence.py
    - packages/lifx-emulator/tests/test_cli.py
    - packages/lifx-emulator/src/lifx_emulator_app/api/app.py
  </read_first>
  <behavior>
    - check_persistence_flags() returns False after logging "--persistent-scenarios requires --persistent" when only persistent_scenarios is set, emits the two existing DeprecationWarning texts when set, and returns True otherwise.
    - shutdown(server, storage, api_task, logger) awaits storage.shutdown(), then server.stop(), then cancels and awaits the API task, and tolerates storage None and api_task None.
    - start_api_task() returns None when settings.api is False and calls webbrowser.open with http://host:port only when api and browser are both True.
  </behavior>
  <action>Per D-10, finish the decomposition. In `startup/storage.py` add `check_persistence_flags(settings, logger) -> bool`, moving the persistent-scenarios check and both `warnings.warn(..., DeprecationWarning, stacklevel=1)` plus `logger.warning` pairs verbatim. In `startup/lifecycle.py` add `start_api_task(server, settings, logger) -> asyncio.Task[None] | None` (keeps the local `from lifx_emulator_app.api import run_api_server` import and the `webbrowser.open(dashboard_url)` call so the existing module-attribute patches still intercept them), `install_shutdown_signals(event: asyncio.Event) -> None` (SIGTERM, SIGINT and SIGBREAK when present, each calling `loop.call_soon_threadsafe(event.set)`), `log_running_banner(settings, logger) -> None` (the three existing banner variants with their exact text and argument form), `async def shutdown(server, storage, api_task, logger) -> None` (log "Shutting down server...", then storage flush, then `server.stop()`, then API task cancel and await swallowing `asyncio.CancelledError`, in exactly that order) and `async def serve_until_shutdown(server, storage, api_task, settings, logger) -> None` that installs signals, logs the banner, awaits the event and always calls `shutdown()` in `finally`. Keep in `__main__.py`, because their names are patched there or they call helpers tests import from there: `_run_emulator(settings, logger) -> bool | None` (constructs `DevicePersistenceAsyncFile()` when persistent, logs the storage path, builds the `SerialAllocator`, calls `build_devices()` and `accept_empty_fleet()`, sets `device.state.port`, calls `_log_devices()`, `_build_scenario_manager()` and `_create_server()`, awaits `server.start()`, calls `start_api_task()` and awaits `serve_until_shutdown()`), `_log_devices(settings, devices, logger)` (uses `_format_capabilities`), `async def _build_scenario_manager(settings, logger)` (persistent-scenario load or `_apply_config_scenarios`, returning one `(scenario_manager, scenario_storage)` tuple) and `_create_server(settings, devices, storage, scenarios)` (four parameters, leaving room for the `mdns_enabled` argument plan 04-04 adds) which unpacks that tuple, constructs `DeviceManager(DeviceRepository())` and calls `EmulatedLifxServer(devices, device_manager, settings.bind, settings.port, track_activity=..., storage=..., scenario_manager=..., persist_scenarios=..., scenario_storage=...)` with the first four arguments positional. `run()` keeps its full cyclopts signature and docstring (the only accepted PLR0913 finding, per the SPEC constraint that the cyclopts signature follows the existing flag pattern) and its body becomes: build the CLI dict, load, build settings, `_setup_logging`, log the config path, `check_persistence_flags`, then `return await _run_emulator(settings, logger)`. `_apply_config_scenarios`, `_scenario_def_to_core`, `_format_capabilities`, `_setup_logging` and `_load_merged_config` keep their names and signatures. If a complexity gate fails, extract another private helper; never add lint suppression comments. Extend `test_startup.py` for the behaviour bullets using doubles passed as arguments.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest packages/lifx-emulator/tests/test_run_equivalence.py packages/lifx-emulator/tests/test_startup.py packages/lifx-emulator/tests/test_cli.py packages/lifx-emulator/tests/test_config.py packages/lifx-emulator/tests/test_export_config.py packages/lifx-emulator/tests/test_websocket.py packages/lifx-emulator/tests/test_api.py -q --no-cov</automated>
    <fails_when>non-zero exit, any "failed" or "error" count in the summary line, or "no tests ran"</fails_when>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run ruff check --select C901,PLR0912,PLR0913,PLR0915 --output-format json --exit-zero packages/lifx-emulator/src/lifx_emulator_app/__main__.py packages/lifx-emulator/src/lifx_emulator_app/config.py packages/lifx-emulator/src/lifx_emulator_app/startup | UV_CACHE_DIR=/tmp/lifx-review-uv uv run python -c "import ast, json, pathlib, sys; allowed = {('__main__.py', 'list_products', 'C901'), ('__main__.py', '_device_state_to_yaml_dict', 'C901'), ('__main__.py', '_device_state_to_yaml_dict', 'PLR0912'), ('__main__.py', 'run', 'PLR0913')}; defs = lambda p: {n.lineno: n.name for n in ast.walk(ast.parse(pathlib.Path(p).read_text())) if isinstance(n, (ast.FunctionDef, ast.AsyncFunctionDef))}; found = [(pathlib.Path(f['filename']).name, defs(f['filename']).get(f['location']['row']), f['code']) for f in json.load(sys.stdin)]; bad = [x for x in found if x not in allowed]; print('unexpected:', bad); sys.exit(1 if bad else 0)"</automated>
    <fails_when>non-zero exit, or an "unexpected:" list that is not empty. The only accepted findings are the pre-existing list_products and _device_state_to_yaml_dict findings and the PLR0913 on run's cyclopts signature; a C901, PLR0912 or PLR0915 finding on run, a PLR0913 on any helper, or any finding in config.py or startup fails</fails_when>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run ruff check .</automated>
    <fails_when>non-zero exit or any reported lint finding</fails_when>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run ruff format --check .</automated>
    <fails_when>non-zero exit or "would reformat" in the output</fails_when>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pyright</automated>
    <fails_when>non-zero exit or an error count other than "0 errors"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n "async def shutdown" packages/lifx-emulator/src/lifx_emulator_app/startup/lifecycle.py` and `grep -n "def check_persistence_flags" packages/lifx-emulator/src/lifx_emulator_app/startup/storage.py` both find their definitions.
    - `grep -n "EmulatedLifxServer(" packages/lifx-emulator/src/lifx_emulator_app/__main__.py` finds exactly one construction, inside `_create_server`.
    - `grep -rnE 'noqa|type: ignore' packages/lifx-emulator/src/lifx_emulator_app/startup packages/lifx-emulator/src/lifx_emulator_app/__main__.py` prints nothing.
    - `test_shutdown_order` and every `GOLDEN_*` case pass unedited after the refactor.
  </acceptance_criteria>
  <done>run() is a thin delegating coroutine, every helper passes the complexity budget, and the golden, shutdown-order and legacy suites all pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arguments and YAML config to run() | User-supplied options become devices, storage paths and bound sockets |
| run() to device-state storage | Shutdown must flush persisted state before the server closes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | startup/devices.py SerialAllocator and build_devices | medium | mitigate | Golden snapshot committed before the refactor (Task 1) pins serial allocation, explicit-serial skipping and order; Tasks 2-3 must reproduce it exactly |
| T-04-02 | Denial of Service | startup/lifecycle.py shutdown | medium | mitigate | test_shutdown_order asserts storage flush, then server.stop(), then API cancel against the pre-refactor code and after |
| T-04-03 | Repudiation | __main__.py error paths | low | mitigate | Existing return values and log texts are preserved; test_cli.py assertions on mock loggers run unedited |
| T-04-04 | Tampering | config.py load_config YAML parsing | low | transfer | yaml.safe_load is unchanged; YAML deserialisation safety is canon owned by /gsd-secure-phase and Bandit (SPEC canon referral) |
</threat_model>

<verification>
- All three task verifies pass.
- On a host where `uv run python -c "import socket; s = socket.socket(socket.AF_INET6, socket.SOCK_DGRAM); s.bind(('::1', 0))"` succeeds, `UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest packages/lifx-emulator/tests -q --no-cov` passes. On a host without IPv6 loopback, `test_cli_validation.py::TestCLIValidation::test_persistent_scenarios_with_persistent_works` already fails before this plan with `OSError: [Errno 97]`; record that it is the only failure and that it is unchanged.
- `git log --format=%s -3` shows the golden snapshot commit before the refactor commits.
</verification>

<success_criteria>
- HYG-03 is satisfied: run() is decomposed into device-construction, storage, server-start and shutdown helpers with unchanged CLI behaviour, and no new flag exists yet.
- SPEC R1 acceptance: Ruff complexity checks for run() and every helper pass without suppressions, the existing app suite passes unchanged, and identical flags produce identical devices.
</success_criteria>

<output>
Create `.planning/phases/04-cli-and-configuration/04-01-SUMMARY.md` when done. Commits use the `app-cli` scope (`test(app-cli): ...`, `refactor(app-cli): ...`) and are signed with `git commit -s`.
</output>
