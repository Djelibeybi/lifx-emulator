---
phase: 04-cli-and-configuration
plan: 07
type: execute
wave: 4
depends_on:
  - 04-04
  - 04-05
files_modified:
  - packages/lifx-emulator/tests/test_cli_docs.py
  - docs/cli/cli-reference.md
  - docs/cli/configuration.md
  - lifx-emulator.example.yaml
autonomous: true
requirements:
  - CFG-01
  - CFG-02

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every new flag (--ipv6-bind, --mdns, --no-mdns, --mdns-ipv4-address, --mdns-ipv6-address, --thread, --thread-product) appears in `lifx-emulator --help` and has its own heading in docs/cli/cli-reference.md, enforced by a test (SPEC R2, R3; CFG-01 'each with a documented flag')."
    - "Every new YAML key (ipv6_bind, mdns, mdns_ipv4_address, mdns_ipv6_address, thread, thread_product at top level; connectivity, mdns, mdns_address per device) is documented in docs/cli/configuration.md and present, commented or live, in lifx-emulator.example.yaml, enforced by a test (SPEC R4 boundary)."
    - "The --mdns documentation states that on Linux zeroconf may answer on an interface other than the selected one, so advertised records can be visible on the local network even with a loopback bind, and that is why mDNS is on automatically only when a Thread device is configured (SPEC boundary, AR-06)."
    - "The docs state the enablement rule (automatic with a Thread device, otherwise --mdns / mdns: true), the WiFi mdns: false opt-out, that a Thread device cannot opt out, that --no-mdns with Thread devices refuses to start, the advertised-address precedence (device mdns_address, then the family's server-level address, then the bind address), the wildcard-bind requirement and the ignored-address warning."
    - "lifx-emulator.example.yaml still loads with load_config and, as shipped, configures no Thread device and leaves mdns unset, so copying it never turns mDNS on (P1, P3)."
  artifacts:
    - path: "packages/lifx-emulator/tests/test_cli_docs.py"
      provides: "Help, reference, configuration and example-file consistency checks"
    - path: "docs/cli/cli-reference.md"
      provides: "Reference entries for every new flag plus the AR-06 note"
      contains: "--mdns-ipv6-address"
    - path: "docs/cli/configuration.md"
      provides: "YAML keys, device fields and the Thread and mDNS rules"
      contains: "mdns_address"
    - path: "lifx-emulator.example.yaml"
      provides: "Commented examples of every new key"
      contains: "thread_product"
  key_links:
    - from: "packages/lifx-emulator/tests/test_cli_docs.py"
      to: "packages/lifx-emulator/src/lifx_emulator_app/__main__.py"
      via: "the test compares `lifx-emulator --help` output with the reference headings"
      pattern: "--help"
  prohibitions: []
---

<objective>
Document every new flag and YAML key, including the AR-06 LAN-visibility note under `--mdns`, update
the example config, and lock the documentation to the CLI with a consistency test (CFG-01
documented flags, CFG-02 keys; SPEC boundaries "CLI docs for every new flag and key" and
"lifx-emulator.example.yaml updated").

Purpose: The enablement rule, the refuse-to-start rule and the wildcard-address rule are easy to
trip over; users need them written down next to the flags, and future flags must not ship without
docs.

Output: `test_cli_docs.py`, new sections in `docs/cli/cli-reference.md` and
`docs/cli/configuration.md`, and commented keys plus a Thread device example in
`lifx-emulator.example.yaml`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-cli-and-configuration/04-SPEC.md
@.planning/phases/04-cli-and-configuration/04-CONTEXT.md
@.planning/phases/04-cli-and-configuration/04-04-SUMMARY.md
@.planning/phases/04-cli-and-configuration/04-05-SUMMARY.md
@docs/cli/cli-reference.md
@docs/cli/configuration.md
@lifx-emulator.example.yaml
@packages/lifx-emulator-core/README.md

<interfaces>
Flags delivered by 04-04 and 04-05 (Server Options group: --ipv6-bind, --mdns/--no-mdns, --mdns-ipv4-address, --mdns-ipv6-address; Device Creation group: --thread, --thread-product). YAML keys: top level ipv6_bind (default ::1), mdns (unset), mdns_ipv4_address, mdns_ipv6_address, thread (default 0), thread_product (default 91); per device connectivity (wifi or thread, lower case, default wifi), mdns (default true, WiFi only may set false), mdns_address (matching-family unicast address, not link-local or scoped).

docs/cli/cli-reference.md uses `### \`--flag <ARG>\`` headings with Default and Example bullets under `## Server Options` and `## Device Type Options`, and `## Complete Examples`. docs/cli/configuration.md has YAML blocks under `### Server Options` and `### Device Creation by Type`, a `### Device Definition Fields` table and `### Common Validation Rules`. lifx-emulator.example.yaml uses commented `# key: value` lines with explanatory comments per section and a commented `devices:` example block.

The core README's "mDNS interface limitation" paragraph is the approved AR-06 wording to paraphrase.
</interfaces>
</context>

## Artifacts this phase produces

- `packages/lifx-emulator/tests/test_cli_docs.py` with parametrised `NEW_FLAGS`, `NEW_TOP_LEVEL_KEYS` and `NEW_DEVICE_KEYS` checks and an example-file safety test.
- `docs/cli/cli-reference.md` headings for `--ipv6-bind <ADDR>`, `--mdns` / `--no-mdns`, `--mdns-ipv4-address <ADDR>`, `--mdns-ipv6-address <ADDR>`, `--thread <COUNT>`, `--thread-product <PID>`, and a "Thread Devices" complete example.
- `docs/cli/configuration.md` key entries, three Device Definition Fields rows, a `## Thread Devices and mDNS` section and new Common Validation Rules.
- `lifx-emulator.example.yaml` commented entries for every new key and a commented Thread device definition.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Lock the Thread count flag and keys to the help output, reference, configuration guide and example file</name>
  <files>packages/lifx-emulator/tests/test_cli_docs.py, docs/cli/cli-reference.md, docs/cli/configuration.md, lifx-emulator.example.yaml</files>
  <read_first>
    - docs/cli/cli-reference.md
    - docs/cli/configuration.md
    - lifx-emulator.example.yaml
    - packages/lifx-emulator/src/lifx_emulator_app/__main__.py
    - packages/lifx-emulator/src/lifx_emulator_app/config.py
    - packages/lifx-emulator/tests/test_cli_validation.py
  </read_first>
  <behavior>
    - For --thread and --thread-product, the flag is in `COLUMNS=200 uv run lifx-emulator --help` output and cli-reference.md has a `###` heading containing it in backticks.
    - For thread and thread_product (top level) and connectivity (per device), configuration.md mentions the key as `key:` or `` `key` `` and the example file has a `key:` line, commented or not.
    - load_config(lifx-emulator.example.yaml) succeeds; merged with no CLI options it has mdns None and thread 0, and no live device definition sets connectivity thread.
  </behavior>
  <action>Create `packages/lifx-emulator/tests/test_cli_docs.py` that locates the repository root from `Path(__file__).resolve().parents[3]`, runs `uv run lifx-emulator --help` once per module (subprocess with `COLUMNS=200`, as `test_cli_validation.py` runs the console script) and defines `NEW_FLAGS`, `NEW_TOP_LEVEL_KEYS` and `NEW_DEVICE_KEYS` lists that start with this slice only (`--thread`, `--thread-product`; `thread`, `thread_product`; `connectivity`). Parametrised tests assert: each flag is in the help text and has a `^###` heading in `docs/cli/cli-reference.md` containing it in backticks followed by a space or backtick (so `--thread` is not satisfied by `--thread-product`); each key appears in `docs/cli/configuration.md` as `key:` or `` `key` `` and in `lifx-emulator.example.yaml` on a line matching optional `#`, optional spaces, then `key:`. Add `test_example_config_keeps_mdns_off`, which loads the example with `load_config`, merges it with `merge_config(cfg, {})`, asserts `mdns` is None and `thread` is 0, and asserts no live device definition has connectivity thread. Then document this slice: in `cli-reference.md` add `### \`--thread <COUNT>\`` and `### \`--thread-product <PID>\`` under Device Type Options (defaults 0 and 91, that Thread devices get firmware 4.200, answer only IPv6 unicast, are discoverable only through mDNS, which turns on automatically, take serials after every other device, and that products whose firmware ceiling is below 4.200, such as the original LIFX Tile (55), are rejected); in `configuration.md` add `thread` and `thread_product` to the Device Creation by Type block and a `connectivity` row to the Device Definition Fields table; in `lifx-emulator.example.yaml` add commented `# thread: 0` and `# thread_product: 91` with explanatory comments and a commented `connectivity: thread` line in the devices example. Use Australian English.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest packages/lifx-emulator/tests/test_cli_docs.py -v --no-cov</automated>
    <fails_when>non-zero exit, any "failed" or "error" count, "no tests ran", or no test id containing "--thread-product" in the verbose (-v) listing</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '^### `--thread <COUNT>`' docs/cli/cli-reference.md` and `grep -n '^### `--thread-product <PID>`' docs/cli/cli-reference.md` each find one heading.
    - `grep -n "test_example_config_keeps_mdns_off" packages/lifx-emulator/tests/test_cli_docs.py` finds the safety test.
    - `grep -nE '^#? *thread_product:' lifx-emulator.example.yaml` finds the key.
  </acceptance_criteria>
  <done>The Thread count slice is documented in all three places and a test fails if any of them drifts from the CLI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document every remaining flag and key, the mDNS rules and the AR-06 note, and extend the consistency test</name>
  <files>packages/lifx-emulator/tests/test_cli_docs.py, docs/cli/cli-reference.md, docs/cli/configuration.md, lifx-emulator.example.yaml</files>
  <read_first>
    - packages/lifx-emulator/tests/test_cli_docs.py
    - docs/cli/cli-reference.md
    - docs/cli/configuration.md
    - lifx-emulator.example.yaml
    - packages/lifx-emulator-core/README.md
    - .planning/phases/04-cli-and-configuration/04-SPEC.md
  </read_first>
  <behavior>
    - NEW_FLAGS covers all seven flags, NEW_TOP_LEVEL_KEYS covers ipv6_bind, mdns, mdns_ipv4_address, mdns_ipv6_address, thread and thread_product, and NEW_DEVICE_KEYS covers connectivity, mdns and mdns_address; every parametrised case passes.
    - The --mdns section of cli-reference.md mentions Linux and the local network.
    - The example-file safety test still passes.
  </behavior>
  <action>Extend the three lists in `test_cli_docs.py` to the full set and add a test that the `--mdns` section of `cli-reference.md` (from its heading to the next `###`) contains "Linux" and "network". In `cli-reference.md` under Server Options add `### \`--ipv6-bind <ADDR>\`` (default ::1, the IPv6 socket is always on, a :: bind needs --mdns-ipv6-address for advertised Thread devices), `### \`--mdns\` / \`--no-mdns\`` (the SPEC R5 rule: on automatically when any Thread device is configured, otherwise off unless --mdns; when on every device is advertised unless a WiFi device sets mdns: false; --no-mdns with a Thread device exits with an error naming the Thread serials before binding; plus an admonition paraphrasing the core README's AR-06 wording that on Linux zeroconf may answer mDNS queries on an interface other than the selected one, so records can be visible on the local network even with a loopback bind, which is why mDNS is not on by default for WiFi-only fleets), `### \`--mdns-ipv4-address <ADDR>\`` and `### \`--mdns-ipv6-address <ADDR>\`` (the precedence device mdns_address, then this server-level address for its family, then the bind address; required for wildcard binds; validation rules; ignored with a warning when mDNS is off); add a "Thread Devices" entry under Complete Examples (for example `lifx-emulator --thread 2 --api` and `lifx-emulator --color 1 --thread 1 --ipv6-bind :: --mdns-ipv6-address fd00::10`). In `configuration.md` add the four server keys to the Server Options block, `mdns` and `mdns_address` rows to the Device Definition Fields table, a `## Thread Devices and mDNS` section with a complete YAML example (a WiFi device opting out with mdns: false and a Thread device with its own mdns_address) and the same rules and AR-06 note, and Common Validation Rules entries for exact-case connectivity, Thread devices cannot set mdns false, mdns_address family and scope rules, and ipv6_bind must be an IPv6 address. In `lifx-emulator.example.yaml` add commented `# ipv6_bind: "::1"`, `# mdns: true`, `# mdns_ipv4_address: ...` and `# mdns_ipv6_address: ...` with explanatory comments, and `mdns: false` / `mdns_address:` lines in the commented devices example; leave every new key commented so the shipped example never enables mDNS. Use Australian English throughout.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest packages/lifx-emulator/tests/test_cli_docs.py packages/lifx-emulator/tests/test_config.py -v --no-cov</automated>
    <fails_when>non-zero exit, any "failed" or "error" count, "no tests ran", or no test id containing "mdns_ipv6_address" in the verbose (-v) listing</fails_when>
    <automated>UV_CACHE_DIR=/tmp/lifx-review-uv uv run ruff check .</automated>
    <fails_when>non-zero exit or any reported lint finding</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '^### `--mdns-ipv4-address <ADDR>`\|^### `--mdns-ipv6-address <ADDR>`\|^### `--ipv6-bind <ADDR>`' docs/cli/cli-reference.md` finds three headings, and `grep -n '^### `--mdns` / `--no-mdns`' docs/cli/cli-reference.md` finds one.
    - `grep -n "^## Thread Devices and mDNS" docs/cli/configuration.md` finds the section.
    - `grep -nE '^# *mdns_ipv6_address:' lifx-emulator.example.yaml` finds the commented key.
    - `test_example_config_keeps_mdns_off` passes after the edits.
  </acceptance_criteria>
  <done>Every new flag and key is documented with the enablement, refusal, precedence and AR-06 guidance, and the docs cannot drift from the CLI unnoticed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation and example config to user behaviour | Users copy the example file and follow the flag docs when exposing devices |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-25 | Information Disclosure | lifx-emulator.example.yaml | medium | mitigate | Every new key stays commented and test_example_config_keeps_mdns_off proves the shipped example never enables mDNS or creates a Thread device |
| T-04-26 | Information Disclosure | docs/cli/cli-reference.md --mdns section | medium | mitigate | The AR-06 note warns that records may be visible on the local network on Linux; a test asserts the note stays in the --mdns section |
| T-04-27 | Repudiation | docs drift from CLI | low | mitigate | test_cli_docs.py fails when a flag lacks a heading or a key lacks configuration and example entries |
</threat_model>

<verification>
- Both task verifies pass.
- `COLUMNS=200 UV_CACHE_DIR=/tmp/lifx-review-uv uv run lifx-emulator --help` lists all seven new flags.
- Phase gate: on a host where binding a UDP socket to `::1` succeeds (or on the hosted Ubuntu and macOS CI legs), `UV_CACHE_DIR=/tmp/lifx-review-uv uv run pytest -q` passes with the 80% coverage gate, and `ruff check .`, `ruff format --check .` and `pyright` pass.
</verification>

<success_criteria>
- CFG-01's "each with a documented flag" and the SPEC documentation and example-file boundaries are met and guarded by a test.
</success_criteria>

<output>
Create `.planning/phases/04-cli-and-configuration/04-07-SUMMARY.md` when done. Commits use the `app-cli` scope (for example `docs(app-cli): document Thread, IPv6 and mDNS options`) and are signed with `git commit -s`.
</output>
